---
phase: 02-homepage
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/neighborhood-showcase.tsx
  - src/components/why-tauro.tsx
  - src/components/testimonials.tsx
  - src/data/homepage-neighborhoods.ts
  - src/data/testimonials.ts
autonomous: true
status: COMPLETED

must_haves:
  truths:
    - "Neighborhoods section shows 6 clickable cards with images, names, and listing counts"
    - "Clicking a neighborhood card navigates to /neighborhoods/{slug}"
    - "Why Tauro section displays 4 brand differentiator cards with icons"
    - "Testimonials section shows 3 client reviews with 5-star ratings"
  artifacts:
    - path: "src/components/neighborhood-showcase.tsx"
      provides: "6 neighborhood cards in responsive grid with image overlays"
      exports: ["default"]
    - path: "src/components/why-tauro.tsx"
      provides: "4 brand differentiator cards with Lucide icons and TiltCard"
      exports: ["default"]
    - path: "src/components/testimonials.tsx"
      provides: "3 testimonial cards with star ratings"
      exports: ["default"]
    - path: "src/data/homepage-neighborhoods.ts"
      provides: "HomepageNeighborhood type and 6 neighborhood objects"
      exports: ["HomepageNeighborhood", "homepageNeighborhoods"]
    - path: "src/data/testimonials.ts"
      provides: "Testimonial type and 3 testimonial objects"
      exports: ["Testimonial", "testimonials"]
  key_links:
    - from: "src/components/neighborhood-showcase.tsx"
      to: "src/data/homepage-neighborhoods.ts"
      via: "import"
      pattern: "import.*homepageNeighborhoods.*from.*data/homepage-neighborhoods"
    - from: "src/components/neighborhood-showcase.tsx"
      to: "/neighborhoods/{slug}"
      via: "Link href"
      pattern: "href=.*neighborhoods/"
    - from: "src/components/testimonials.tsx"
      to: "src/data/testimonials.ts"
      via: "import"
      pattern: "import.*testimonials.*from.*data/testimonials"
---

<objective>
Build the Neighborhoods showcase, Why Tauro brand section, and Testimonials as standalone components with separated data files.

Purpose: These three sections form the middle of the homepage -- neighborhood discovery drives area page traffic, the brand section builds trust, and testimonials provide social proof. Each is a self-contained component with its own data source.
Output: Three section components and two data files ready for page assembly in Plan 04.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/app/(site)/page.tsx
@src/components/ui/tilt-card.tsx
@src/lib/utils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create NeighborhoodShowcase component and data file</name>
  <files>src/components/neighborhood-showcase.tsx, src/data/homepage-neighborhoods.ts</files>
  <action>
1. Create `src/data/homepage-neighborhoods.ts`:
   - Define and export interface `HomepageNeighborhood`: `{ name: string; slug: string; description: string; image: string; listings: number }`
   - Export `homepageNeighborhoods` array with exactly 6 neighborhoods:
     - Center City (slug: "center-city", 42 listings, image: `https://images.unsplash.com/photo-1569761316261-9a8696fa2ca3?w=800&q=80`, desc: "The beating heart of Philadelphia -- walkable, vibrant, and full of culture.")
     - Rittenhouse (slug: "rittenhouse", 28 listings, image: `https://images.unsplash.com/photo-1600596542815-ffad4c1539a9?w=800&q=80`, desc: "Philadelphia's most prestigious address with tree-lined streets and luxury living.")
     - Fishtown (slug: "fishtown", 35 listings, image: `https://images.unsplash.com/photo-1600585154340-be6161a56a0c?w=800&q=80`, desc: "Creative energy meets industrial charm in Philly's hottest neighborhood.")
     - Northern Liberties (slug: "northern-liberties", 19 listings, image: `https://images.unsplash.com/photo-1600607687939-ce8a6c25118c?w=800&q=80`, desc: "Urban sophistication with boutique restaurants and converted lofts.")
     - Old City (slug: "old-city", 15 listings, image: `https://images.unsplash.com/photo-1560448204-e02f11c3d0e2?w=800&q=80`, desc: "Where history meets modern living -- cobblestone streets and gallery nights.")
     - Chestnut Hill (slug: "chestnut-hill", 22 listings, image: `https://images.unsplash.com/photo-1512917774080-9991f1c4c750?w=800&q=80`, desc: "Suburban charm within city limits -- gardens, boutiques, and top schools.")

2. Create `src/components/neighborhood-showcase.tsx` (server component, no "use client"):
   - Imports: `Link` from `next/link`, `Image` from `next/image`, `MapPin` from `lucide-react`, `homepageNeighborhoods` from `@/data/homepage-neighborhoods`
   - Section wrapper: `border-t border-border/40 bg-midnight py-20`
   - Inner container: `mx-auto max-w-7xl px-4 sm:px-6 lg:px-8`
   - Center header:
     - Kicker: "Explore Philadelphia" -- `font-label text-sm font-semibold uppercase tracking-[0.2em] text-gold`
     - H2: "Premier Neighborhoods" -- `mt-2 font-heading text-3xl font-bold text-white sm:text-4xl`
     - Subtitle: "From the brownstones of Rittenhouse to the lofts of Fishtown -- discover what makes each Philadelphia neighborhood unique." -- `mx-auto mt-4 max-w-xl text-muted-foreground`
   - Grid: `mt-12 grid gap-5 sm:grid-cols-2 lg:grid-cols-3`
   - Each card: `<Link key={hood.slug} href={/neighborhoods/${hood.slug}}>` with classes `group relative overflow-hidden rounded-xl border border-border/40 transition-all hover:border-gold/40 hover:shadow-xl`
     - Image container: `relative aspect-[16/10] overflow-hidden`
       - `<Image>` with `src={hood.image}`, `alt={hood.name}`, `fill`, `sizes="(max-width: 640px) 100vw, (max-width: 1024px) 50vw, 33vw"`, `className="object-cover transition-transform duration-500 group-hover:scale-105"`
       - Gradient overlay: `absolute inset-0 bg-gradient-to-t from-near-black/90 via-near-black/30 to-transparent`
     - Bottom overlay: `absolute inset-x-0 bottom-0 p-5`
       - Row: MapPin icon (`size-4 text-gold`) + H3 name (`font-heading text-lg font-bold text-white`)
       - Description: `mt-1 text-sm text-white/70`
       - Listing count: `mt-2 text-xs font-semibold text-gold` -- "{n} Active Listings"
   - Export as `default function NeighborhoodShowcase()`
  </action>
  <verify>Run `npx tsc --noEmit` -- should pass. Verify both files exist and exports resolve.</verify>
  <done>NeighborhoodShowcase renders 6 clickable neighborhood cards with images, gradient overlays, names, descriptions, and listing counts. Each links to /neighborhoods/{slug}.</done>
</task>

<task type="auto">
  <name>Task 2: Create WhyTauro and Testimonials components</name>
  <files>src/components/why-tauro.tsx, src/components/testimonials.tsx, src/data/testimonials.ts</files>
  <action>
1. Create `src/data/testimonials.ts`:
   - Define and export interface `Testimonial`: `{ quote: string; name: string; role: string; rating: number }`
   - Export `testimonials` array with 3 entries:
     - Sarah & Michael Chen, role: "Homebuyers -- Rittenhouse Square", rating: 5, quote: "Tauro made our first home purchase in Rittenhouse completely seamless. Their knowledge of the Philadelphia market is unmatched."
     - David Okafor, role: "Seller -- Fishtown", rating: 5, quote: "We listed with three other brokerages before finding Tauro. They sold our Fishtown townhouse in 6 days, $40K over asking."
     - Maria & James Patterson, role: "Investors -- Center City", rating: 5, quote: "The level of service and market insight Tauro provides is on par with the best brokerages in New York. Philadelphia is lucky to have them."

2. Create `src/components/why-tauro.tsx` (server component):
   - Import icons from `lucide-react`: Home, TrendingUp, Users, Shield
   - Import `TiltCard` from `@/components/ui/tilt-card`
   - Define `whyTauro` array INSIDE the component file (not a data file -- it contains icon references):
     - Home icon: "Curated Portfolio" -- "We don't list everything -- we list the right things. Every property is vetted for quality, value, and investment potential."
     - TrendingUp icon: "Market Intelligence" -- "Hyperlocal data across 15 Philadelphia neighborhoods. We know pricing trends before they hit the market."
     - Users icon: "Concierge Service" -- "Dedicated agents who know your name, your goals, and your timeline. Not a call center -- a partnership."
     - Shield icon: "Proven Results" -- "98% client satisfaction, $2.1B in total volume, and a reputation built on delivering what we promise."
   - Section: `bg-near-black py-20`
   - Center header:
     - Kicker: "The Tauro Difference" -- `font-label text-sm font-semibold uppercase tracking-[0.2em] text-gold`
     - H2: "Why Philadelphia Trusts Tauro" -- `mt-2 font-heading text-3xl font-bold text-white sm:text-4xl`
     - Subtitle: "We're not the biggest brokerage in Philadelphia. We're the most intentional. Every client, every listing, every deal -- handled with precision." -- `mx-auto mt-4 max-w-2xl text-muted-foreground`
   - Grid: `mt-14 grid gap-8 sm:grid-cols-2 lg:grid-cols-4`
   - Each card: `<TiltCard key={item.title} maxTilt={6}>` wrapping a div with:
     - Outer: `group rounded-xl border border-border/40 bg-midnight p-6 transition-all hover:border-gold/30 hover:shadow-lg`
     - Icon container: `flex size-12 items-center justify-center rounded-lg bg-gold/10` with `<item.icon className="size-6 text-gold" />`
     - Title: `mt-4 font-heading text-lg font-bold text-white`
     - Description: `mt-2 text-sm leading-relaxed text-muted-foreground`
   - Export as `default function WhyTauro()`

3. Create `src/components/testimonials.tsx` (server component):
   - Import `testimonials` from `@/data/testimonials`, `Star` from `lucide-react`
   - Section: `border-t border-border/40 bg-midnight py-20`
   - Center header:
     - Kicker: "Client Stories" -- `font-label text-sm font-semibold uppercase tracking-[0.2em] text-gold`
     - H2: "What Our Clients Say" -- `mt-2 font-heading text-3xl font-bold text-white sm:text-4xl`
   - Grid: `mt-12 grid gap-6 md:grid-cols-3`
   - Each testimonial card: `rounded-xl border border-border/40 bg-near-black p-6`
     - Star row: `Array.from({ length: testimonial.rating })` mapped to `<Star className="size-4 fill-gold text-gold" />`
     - Blockquote: `mt-4 text-sm leading-relaxed text-white/80` with curly quotes around the text
     - Divider: `mt-4 border-t border-border/40 pt-4`
     - Name: `text-sm font-semibold text-white`
     - Role: `text-xs text-muted-foreground`
   - Export as `default function Testimonials()`
  </action>
  <verify>Run `npx tsc --noEmit` -- should pass. Verify all three files exist and exports resolve.</verify>
  <done>WhyTauro renders 4 brand cards with icons and TiltCard hover. Testimonials renders 3 review cards with gold star ratings. Data is separated into src/data/testimonials.ts.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- NeighborhoodShowcase renders 6 cards linking to /neighborhoods/{slug}
- WhyTauro renders 4 brand differentiator cards with Lucide icons
- Testimonials renders 3 review cards with filled gold stars
- All data files export typed arrays
</verification>

<success_criteria>
- All 3 section components are self-contained and import their own data
- Neighborhood cards navigate to correct /neighborhoods/{slug} routes
- WhyTauro uses TiltCard with maxTilt={6} for subtle hover effect
- Testimonials show gold filled stars matching rating value
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-homepage/02-03-SUMMARY.md`
</output>
