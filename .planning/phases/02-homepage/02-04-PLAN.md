---
phase: 02-homepage
plan: 04
type: execute
wave: 2
status: COMPLETED
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - src/components/homepage-ctas.tsx
  - src/app/(site)/page.tsx
autonomous: true

must_haves:
  truths:
    - "Buyer CTA card displays with background image, gradient, heading, and gold button linking to /properties"
    - "Seller CTA card displays with background image, gradient, heading, and outline button linking to /sell"
    - "Both CTA buttons have shimmer-gold hover effect"
    - "Homepage page.tsx is a clean composition of 7 section components (~25-30 lines)"
    - "All 7 sections render in order: Hero, Stats, Featured, Neighborhoods, WhyTauro, Testimonials, CTAs"
    - "No inline data arrays remain in page.tsx"
  artifacts:
    - path: "src/components/homepage-ctas.tsx"
      provides: "Buyer and seller CTA cards with background images"
      exports: ["default"]
    - path: "src/app/(site)/page.tsx"
      provides: "Clean homepage composition importing all 7 section components"
      exports: ["default", "metadata"]
  key_links:
    - from: "src/components/homepage-ctas.tsx"
      to: "/properties"
      via: "Link href on buyer CTA"
      pattern: "href=.*properties"
    - from: "src/components/homepage-ctas.tsx"
      to: "/sell"
      via: "Link href on seller CTA"
      pattern: "href=.*sell"
    - from: "src/app/(site)/page.tsx"
      to: "src/components/hero.tsx"
      via: "component import and render"
      pattern: "<Hero"
    - from: "src/app/(site)/page.tsx"
      to: "src/components/homepage-ctas.tsx"
      via: "component import and render"
      pattern: "<HomepageCTAs"
---

<objective>
Create the buyer/seller CTA section and assemble all homepage sections into a clean page composition.

Purpose: This plan creates the final homepage section (CTAs) and rewrites page.tsx to be a clean composition of imported section components. After this plan, the homepage file is under 30 lines with zero inline data or complex JSX -- just imports and component composition.
Output: HomepageCTAs component and final clean page.tsx.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-homepage/02-01-SUMMARY.md
@.planning/phases/02-homepage/02-02-SUMMARY.md
@.planning/phases/02-homepage/02-03-SUMMARY.md
@src/app/(site)/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HomepageCTAs component</name>
  <files>src/components/homepage-ctas.tsx</files>
  <action>
Create `src/components/homepage-ctas.tsx` as a server component (no "use client"):

Imports: `Link` from `next/link`, `Image` from `next/image`, `ArrowRight` from `lucide-react`

Section wrapper: `bg-near-black py-20`
Inner container: `mx-auto max-w-7xl px-4 sm:px-6 lg:px-8`
Grid: `grid gap-6 md:grid-cols-2`

**Buyer CTA card:**
- Outer: `relative overflow-hidden rounded-2xl border border-border/40`
- Background image: `<Image src="https://images.unsplash.com/photo-1600585154340-be6161a56a0c?w=800&q=80" alt="Luxury home interior" fill sizes="(max-width: 768px) 100vw, 50vw" className="object-cover" />`
- Gradient overlay: `absolute inset-0 bg-gradient-to-r from-near-black/90 via-near-black/70 to-near-black/40`
- Content: `relative z-10 p-8 sm:p-10`
  - Kicker: "For Buyers" -- `font-label text-sm font-semibold uppercase tracking-[0.2em] text-gold`
  - H3: "Find Your Dream Home" -- `mt-2 font-heading text-2xl font-bold text-white sm:text-3xl`
  - Description: "Browse curated listings, explore neighborhoods, and let our agents guide you to the perfect Philadelphia property." -- `mt-3 max-w-sm text-sm leading-relaxed text-white/70`
  - Button: `<Link href="/properties">` -- `shimmer-gold mt-6 inline-flex items-center gap-2 rounded-lg bg-gold px-6 py-3 text-sm font-semibold text-near-black transition-all hover:bg-gold-light hover:shadow-lg`
    - Text: "Browse Properties" + `<ArrowRight className="size-4" />`

**Seller CTA card:**
- Same outer structure as buyer
- Background image: `https://images.unsplash.com/photo-1600607687939-ce8a6c25118c?w=800&q=80`, alt: "Modern home exterior"
- Same gradient overlay
- Content:
  - Kicker: "For Sellers" -- same gold label style
  - H3: "List With Tauro" -- same heading style
  - Description: "Get a free home valuation, premium marketing, and an agent who knows your neighborhood inside and out." -- same text style
  - Button: `<Link href="/sell">` -- `shimmer-gold mt-6 inline-flex items-center gap-2 rounded-lg border-2 border-gold px-6 py-3 text-sm font-semibold text-gold transition-all hover:bg-gold hover:text-near-black`
    - Text: "Get a Free Valuation" + `<ArrowRight className="size-4" />`

Export as `default function HomepageCTAs()`.
  </action>
  <verify>Run `npx tsc --noEmit` -- should pass. Verify homepage-ctas.tsx exists and exports default.</verify>
  <done>HomepageCTAs renders two side-by-side CTA cards with background images, gradient overlays, and shimmer-gold buttons linking to /properties and /sell.</done>
</task>

<task type="auto">
  <name>Task 2: Assemble final homepage page.tsx</name>
  <files>src/app/(site)/page.tsx</files>
  <action>
Rewrite `src/app/(site)/page.tsx` to be a clean composition file. The ENTIRE file should be approximately 25-30 lines:

```tsx
import type { Metadata } from "next";
import Hero from "@/components/hero";
import StatsBar from "@/components/stats-bar";
import FeaturedProperties from "@/components/featured-properties";
import NeighborhoodShowcase from "@/components/neighborhood-showcase";
import WhyTauro from "@/components/why-tauro";
import Testimonials from "@/components/testimonials";
import HomepageCTAs from "@/components/homepage-ctas";

export const metadata: Metadata = {
  title: "Premium Philadelphia Real Estate",
  description:
    "Discover luxury homes in Philadelphia with Tauro. Browse premium properties, explore neighborhoods, and connect with top agents across Center City, Rittenhouse, Fishtown, and more.",
};

export default function HomePage() {
  return (
    <>
      <Hero />
      <StatsBar />
      <FeaturedProperties />
      <NeighborhoodShowcase />
      <WhyTauro />
      <Testimonials />
      <HomepageCTAs />
    </>
  );
}
```

IMPORTANT requirements:
- Remove ALL inline section JSX (hero, stats, featured, neighborhoods, why-tauro, testimonials, CTAs)
- Remove ALL data arrays that were previously defined inline (neighborhoods, testimonials, whyTauro, featuredProperties)
- Remove ALL unused imports (lucide icons, next/image, next/link, property data imports, formatPrice)
- The ONLY imports should be: `Metadata` type and the 7 section components
- Keep the `metadata` export with SEO title and description
- Title should use short form "Premium Philadelphia Real Estate" (root layout template appends "| Tauro" -- see decision SEO-TITLE-TEMPLATE-01)
  </action>
  <verify>
Run `npx next build` -- should compile without errors.
Run `npx tsc --noEmit` -- should pass.
Verify page.tsx is under 30 lines.
Verify page.tsx has no inline JSX sections, no data arrays, and only imports Metadata + 7 components.
  </verify>
  <done>Homepage page.tsx is a clean ~25-line composition file. All 7 sections render in order via imported components. No inline data or complex JSX remains. Build passes.</done>
</task>

</tasks>

<verification>
- `npx next build` passes with zero errors
- Homepage loads with all 7 sections in correct order: Hero, Stats, Featured Properties, Neighborhoods, Why Tauro, Testimonials, CTAs
- Search bar navigates to /properties?q={query}
- "View All Properties" navigates to /properties
- Neighborhood cards navigate to /neighborhoods/{slug}
- Buyer CTA navigates to /properties
- Seller CTA navigates to /sell
- shimmer-gold hover works on CTA buttons
- TiltCard hover works on featured properties and why-tauro cards
- page.tsx is under 30 lines with only component imports
</verification>

<success_criteria>
- HomepageCTAs renders buyer/seller cards with working links and shimmer effect
- page.tsx is a clean composition (~25 lines) with zero inline logic
- Full homepage renders identically to the current version
- All navigation links work correctly
- Build compiles without errors
- No visual regression
</success_criteria>

<output>
After completion, create `.planning/phases/02-homepage/02-04-SUMMARY.md`
</output>
