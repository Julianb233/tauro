# Plan: Featured Properties & Neighborhoods Sections
**Phase:** 2 -- Homepage
**Plan ID:** 02-02
**Status:** PENDING
**Depends On:** 02-01 (hero/stats extraction establishes component pattern)
**Wave:** 1
**Autonomous:** true

## Goal

Extract the Featured Properties section and Philadelphia Neighborhoods showcase from the monolithic homepage into dedicated, reusable components with separated data files. The featured properties grid uses the existing `PropertyCard` component wrapped in `TiltCard` for 3D hover micro-interaction. The neighborhoods section renders clickable cards linking to `/neighborhoods/{slug}`.

## Requirements Addressed

- **HOME-02**: Featured properties carousel/grid (curated static data)
- **HOME-03**: Philadelphia neighborhoods showcase section

## Tasks

### Task 1: Create Featured Properties Section Component (auto)

**Files:**
- `src/components/featured-properties.tsx` (create)
- `src/data/featured-properties.ts` (create)

**Action:**

1. Create `src/data/featured-properties.ts`:
   - Import `properties` from `@/data/properties`
   - Export `featuredProperties = properties.slice(0, 3)` -- matching current behavior
   - Add a TODO comment: `// Upgrade to properties.filter(p => p.featured) when featured flag is added`

2. Create `src/components/featured-properties.tsx`:
   - Extract the "Featured Listings" section from `page.tsx` (the block with `HOME-02` comment, currently lines ~172-200)
   - Imports: `featuredProperties` from `@/data/featured-properties`, `PropertyCard` from `@/components/PropertyCard`, `TiltCard` from `@/components/ui/tilt-card`, `Link` from `next/link`, `ArrowRight` from `lucide-react`
   - Section wrapper: `bg-near-black py-20`
   - Header row: `flex flex-col items-center justify-between gap-4 sm:flex-row`
     - Left: "Featured Listings" kicker (`font-label text-sm uppercase tracking-[0.2em] text-gold`) + "Exceptional Properties" heading (`font-heading text-3xl sm:text-4xl`)
     - Right: "View All Properties" link to `/properties` with ArrowRight icon, gold text with hover translate-x animation
   - Grid: `mt-12 grid gap-6 sm:grid-cols-2 lg:grid-cols-3`
   - Each property: `<TiltCard key={property.id}><PropertyCard property={property} /></TiltCard>`
   - Export as `default function FeaturedProperties()`

**Verify:**
- `npx tsc --noEmit` -- no type errors
- Component file exists and exports default

**Done:**
- `src/components/featured-properties.tsx` renders 3 property cards with TiltCard 3D hover
- `src/data/featured-properties.ts` provides curated property selection
- "View All Properties" link points to `/properties`

---

### Task 2: Create Neighborhood Showcase and Wire Both into Homepage (auto)

**Files:**
- `src/components/neighborhood-showcase.tsx` (create)
- `src/data/homepage-neighborhoods.ts` (create)
- `src/app/(site)/page.tsx` (modify)

**Action:**

1. Create `src/data/homepage-neighborhoods.ts`:
   - Define and export interface `HomepageNeighborhood`: `{ name: string; slug: string; description: string; image: string; listings: number }`
   - Export `homepageNeighborhoods` array with the 6 neighborhoods currently hardcoded in `page.tsx`:
     - Center City (slug: center-city, 42 listings, "The beating heart of Philadelphia -- walkable, vibrant, and full of culture.")
     - Rittenhouse (slug: rittenhouse, 28 listings, "Philadelphia's most prestigious address with tree-lined streets and luxury living.")
     - Fishtown (slug: fishtown, 35 listings, "Creative energy meets industrial charm in Philly's hottest neighborhood.")
     - Northern Liberties (slug: northern-liberties, 19 listings, "Urban sophistication with boutique restaurants and converted lofts.")
     - Old City (slug: old-city, 15 listings, "Where history meets modern living -- cobblestone streets and gallery nights.")
     - Chestnut Hill (slug: chestnut-hill, 22 listings, "Suburban charm within city limits -- gardens, boutiques, and top schools.")
   - Use the exact Unsplash image URLs currently in `page.tsx` for each neighborhood
   - Note: This is a curated homepage subset, separate from the full `src/data/neighborhoods.ts` used on area pages

2. Create `src/components/neighborhood-showcase.tsx`:
   - Extract the "Neighborhoods" section from `page.tsx` (the block with `HOME-03` comment, currently lines ~202-253)
   - Import `homepageNeighborhoods` from `@/data/homepage-neighborhoods`, `Link` from `next/link`, `Image` from `next/image`, `MapPin` from `lucide-react`
   - Section wrapper: `border-t border-border/40 bg-midnight py-20`
   - Center header: "Explore Philadelphia" kicker + "Premier Neighborhoods" heading + subtitle paragraph
   - Grid: `mt-12 grid gap-5 sm:grid-cols-2 lg:grid-cols-3`
   - Each card: `<Link href={/neighborhoods/${hood.slug}}>` with `group` class, `overflow-hidden rounded-xl border border-border/40`, hover border-gold transition
     - Image container: `relative aspect-[16/10] overflow-hidden` with `<Image>` using `fill`, `sizes="(max-width: 640px) 100vw, (max-width: 1024px) 50vw, 33vw"`, hover scale-105 transition (500ms)
     - Gradient overlay on image: `bg-gradient-to-t from-near-black/90 via-near-black/30 to-transparent`
     - Bottom overlay content: MapPin icon + neighborhood name + description + "{n} Active Listings" in gold
   - Export as `default function NeighborhoodShowcase()`

3. Update `src/app/(site)/page.tsx`:
   - Add imports: `FeaturedProperties` from `@/components/featured-properties`, `NeighborhoodShowcase` from `@/components/neighborhood-showcase`
   - Replace inline Featured Listings section with `<FeaturedProperties />`
   - Replace inline Neighborhoods section with `<NeighborhoodShowcase />`
   - Remove the `neighborhoods` array constant (now in data file)
   - Remove `const featuredProperties = properties.slice(0, 3)` (now in data file)
   - Remove unused imports: `MapPin` from lucide (if not used elsewhere), `properties`/`formatPrice` from data (if not used elsewhere in page)

**Verify:**
- `npx next build` compiles without errors
- Homepage shows 3 featured property cards with TiltCard hover effect
- Homepage shows 6 neighborhood cards with images and overlay text
- Clicking a neighborhood card navigates to `/neighborhoods/{slug}`
- Clicking "View All Properties" navigates to `/properties`

**Done:**
- Featured properties render via `<FeaturedProperties />` with TiltCard micro-interactions
- Neighborhoods render via `<NeighborhoodShowcase />` with image cards
- All inline data arrays removed from `page.tsx`
- No visual regression

## Acceptance Criteria

- [ ] `src/components/featured-properties.tsx` exists with property grid + TiltCard wrapping
- [ ] `src/components/neighborhood-showcase.tsx` exists with 6 neighborhood cards
- [ ] `src/data/featured-properties.ts` exports curated property array
- [ ] `src/data/homepage-neighborhoods.ts` exports 6 typed neighborhood objects
- [ ] PropertyCard and TiltCard render correctly in featured section
- [ ] Neighborhood cards link to `/neighborhoods/{slug}` routes
- [ ] `page.tsx` no longer contains inline `neighborhoods` or `featuredProperties` arrays
- [ ] `npx next build` passes with no errors
- [ ] No visual regression from current homepage

## Files to Create/Modify

| File | Action |
|------|--------|
| `src/components/featured-properties.tsx` | Create |
| `src/components/neighborhood-showcase.tsx` | Create |
| `src/data/featured-properties.ts` | Create |
| `src/data/homepage-neighborhoods.ts` | Create |
| `src/app/(site)/page.tsx` | Modify -- replace inline sections, remove data constants |
