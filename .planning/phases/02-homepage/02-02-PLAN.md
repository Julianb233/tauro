---
status: COMPLETED
phase: 02-homepage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/PropertyCard.tsx
  - src/components/featured-properties.tsx
  - src/data/properties.ts
  - src/data/featured-properties.ts
autonomous: true

must_haves:
  truths:
    - "Featured properties section shows 3 property cards in a responsive grid"
    - "Each property card displays image, price, beds/baths/sqft, address, and status badge"
    - "Property cards link to their detail page at /properties/{slug}"
    - "View All Properties link navigates to /properties"
    - "Cards have hover effects (image scale, border gold glow)"
  artifacts:
    - path: "src/components/PropertyCard.tsx"
      provides: "Reusable property card with image, price, stats, address"
      exports: ["default"]
    - path: "src/components/featured-properties.tsx"
      provides: "Featured properties section with heading and 3-card grid"
      exports: ["default"]
    - path: "src/data/properties.ts"
      provides: "Property type and static property data array"
      exports: ["Property", "properties", "formatPrice", "formatPriceFull", "getPropertyBySlug"]
    - path: "src/data/featured-properties.ts"
      provides: "Curated subset of properties for homepage"
      exports: ["featuredProperties"]
  key_links:
    - from: "src/components/featured-properties.tsx"
      to: "src/data/featured-properties.ts"
      via: "import"
      pattern: "import.*featuredProperties.*from.*data/featured-properties"
    - from: "src/components/featured-properties.tsx"
      to: "src/components/PropertyCard.tsx"
      via: "component render in map"
      pattern: "<PropertyCard"
    - from: "src/components/PropertyCard.tsx"
      to: "/properties/{slug}"
      via: "Link href"
      pattern: "href=.*properties/"
---

<objective>
Build the Featured Properties section and reusable PropertyCard component with static property data.

Purpose: The featured properties section is the primary conversion driver below the hero -- showing 3 curated listings with rich cards that entice visitors to browse. The PropertyCard component is reused across the entire site (listings page, neighborhood pages, search results).
Output: PropertyCard component, FeaturedProperties section component, and static data files.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/data/properties.ts
@src/data/featured-properties.ts
@src/components/ui/tilt-card.tsx
@src/lib/utils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PropertyCard component and verify property data</name>
  <files>src/components/PropertyCard.tsx, src/data/properties.ts, src/data/featured-properties.ts</files>
  <action>
Verify `src/data/properties.ts` exports the `Property` interface with fields: id, slug, address, city, state, zip, neighborhood, price, beds, baths, sqft, lotSqft, yearBuilt, status, propertyType, images, description, features, agent, lat, lng, openHouse?, videoUrl?, virtualTourUrl?. Also verify `formatPrice`, `formatPriceFull`, and `getPropertyBySlug` are exported. The file should contain 16 Philadelphia property listings. Do NOT modify this file.

Verify `src/data/featured-properties.ts` exports `featuredProperties = properties.slice(0, 3)` with a TODO comment about upgrading to a featured flag filter. Do NOT modify this file.

Create or verify `src/components/PropertyCard.tsx` (it already exists -- verify it matches spec and fix if needed):
- Imports: `Image` from `next/image`, `Link` from `next/link`, `Property, formatPrice` from `@/data/properties`, `cn` from `@/lib/utils`
- Props: `{ property: Property }`
- Outer wrapper: `<Link href={/properties/${property.slug}}>` with classes `group block overflow-hidden rounded-xl border border-border bg-card transition-all hover:border-gold/40 hover:shadow-lg`
- Image section: `relative aspect-[4/3] overflow-hidden bg-muted`
  - `<Image>` with `src={property.images[0]}`, `alt={property.address}`, `fill`, `className="object-cover transition-transform duration-500 group-hover:scale-105"`, `sizes="(max-width: 768px) 100vw, (max-width: 1200px) 50vw, 33vw"`
  - Status badge top-left: conditional color by status using `cn()`:
    - Active: `bg-emerald-600`
    - New: `bg-gold text-near-black`
    - Open House: `bg-blue-600`
    - Pending: `bg-orange-500`
    - Default: `bg-muted-foreground`
  - Photo count badge bottom-right: `bg-black/60 px-2 py-1 text-xs text-white backdrop-blur-sm`
- Details `div.p-4`:
  - Price: `font-heading text-xl font-bold text-gold` using `formatPrice(property.price)`
  - Stats: `{beds} BD | {baths} BA | {sqft.toLocaleString()} SF` -- pipe separators in `text-border` color
  - Address: `mt-2 font-medium text-foreground`
  - Location: `text-sm text-muted-foreground` -- "{city}, {state} {zip}"
  - Open house (conditional): `mt-2 text-xs font-semibold text-gold` -- "Open House: {openHouse}"

Export as `default function PropertyCard({ property }: { property: Property })`.
  </action>
  <verify>Run `npx tsc --noEmit` -- should pass. Verify PropertyCard exports default and accepts Property prop.</verify>
  <done>PropertyCard renders a rich, clickable card with image, status badge, price, stats, and address. Links to /properties/{slug}.</done>
</task>

<task type="auto">
  <name>Task 2: Create FeaturedProperties section component</name>
  <files>src/components/featured-properties.tsx</files>
  <action>
Create `src/components/featured-properties.tsx` as a server component (no "use client"):

1. Imports: `Link` from `next/link`, `ArrowRight` from `lucide-react`, `featuredProperties` from `@/data/featured-properties`, `PropertyCard` from `@/components/PropertyCard`, `TiltCard` from `@/components/ui/tilt-card`

2. Section wrapper: `bg-near-black py-20`
3. Inner container: `mx-auto max-w-7xl px-4 sm:px-6 lg:px-8`
4. Header row: `flex flex-col items-center justify-between gap-4 sm:flex-row`
   - Left side div:
     - Kicker: "Featured Listings" -- `font-label text-sm font-semibold uppercase tracking-[0.2em] text-gold`
     - H2: "Exceptional Properties" -- `mt-2 font-heading text-3xl font-bold text-white sm:text-4xl`
   - Right side: `<Link href="/properties">` with text "View All Properties" + `<ArrowRight className="size-4 transition-transform group-hover:translate-x-1" />`
     - Link classes: `group flex items-center gap-2 text-sm font-semibold text-gold transition-colors hover:text-gold-light`

5. Card grid: `mt-12 grid gap-6 sm:grid-cols-2 lg:grid-cols-3`
   - Map over `featuredProperties`: `<TiltCard key={property.id}><PropertyCard property={property} /></TiltCard>`

Export as `default function FeaturedProperties()`.
  </action>
  <verify>Run `npx tsc --noEmit` -- should pass. Verify featured-properties.tsx exists, imports resolve, renders 3 cards.</verify>
  <done>FeaturedProperties renders a section heading with "View All" link and 3 property cards wrapped in TiltCard for 3D hover micro-interaction.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- PropertyCard is importable and renders with any Property object
- FeaturedProperties shows exactly 3 cards from static data
- "View All Properties" links to /properties
- TiltCard wrapping produces hover tilt effect
</verification>

<success_criteria>
- PropertyCard is a reusable component accepting any Property object
- Featured section renders 3 curated properties with TiltCard hover effects
- All navigation links are correct (/properties, /properties/{slug})
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-homepage/02-02-SUMMARY.md`
</output>
