---
phase: 02-homepage
plan: 02
type: execute
wave: 1
depends_on: ["02-01"]
files_modified:
  - src/app/(site)/page.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Search bar navigates to /properties?q={query} on submit"
    - "Hero section uses Next.js Image component for optimized loading"
    - "Featured property cards have 3D tilt micro-interaction via TiltCard component"
    - "Why Tauro feature cards use TiltCard component for interactive 3D hover"
    - "Seller CTA links to /sell not /contact"
    - "Neighborhood cards link to /areas/{slug} not /neighborhoods/{slug}"
  artifacts:
    - path: "src/app/(site)/page.tsx"
      provides: "Refined homepage with micro-interactions and correct routing"
  key_links:
    - from: "src/app/(site)/page.tsx"
      to: "/properties?q={query}"
      via: "search form onSubmit with router.push"
      pattern: "router\\.push.*properties"
    - from: "src/app/(site)/page.tsx"
      to: "TiltCard component"
      via: "import and wrapping property cards and why-tauro cards"
      pattern: "import.*TiltCard"
---

<objective>
Refine the existing homepage to close quality gaps: make the search bar functional, integrate TiltCard micro-interactions on property and feature cards, fix routing links to match downstream phase conventions, and optimize the hero image loading.

Purpose: The homepage exists but lacks functional search, proper micro-interactions from Phase 1, and has incorrect link targets that won't match Phase 3/5 routing.
Output: Updated page.tsx with working search, TiltCard integration, and corrected links.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-homepage/02-01-SUMMARY.md
@src/app/(site)/page.tsx
@src/components/ui/tilt-card.tsx
@src/components/ui/gold-shimmer.tsx
@src/components/PropertyCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make search bar functional and fix hero image</name>
  <files>src/app/(site)/page.tsx</files>
  <action>
  The homepage must become a client component (add "use client" at top) to support interactivity OR extract the hero search into a separate client component. The cleaner approach: create a small HeroSearch client component inline or extract to a separate file.

  PREFERRED APPROACH: Extract a `HeroSearchBar` client component to keep the page as a server component.

  Create `src/components/HeroSearchBar.tsx`:
  - "use client" directive
  - Uses `useState` for search query, `useRouter` from next/navigation
  - On form submit (or button click), navigates to `/properties?q={encodeURIComponent(query)}`
  - If query is empty, navigates to `/properties`
  - Same visual styling as current search bar (rounded-xl, border-white/10, bg-near-black/60, backdrop-blur-md)
  - Search icon (lucide Search), gold shimmer-gold button

  In `src/app/(site)/page.tsx`:
  - Replace the existing search bar div with `<HeroSearchBar />`
  - Replace `<img>` hero background with Next.js `<Image>` component using `fill`, `priority`, `className="object-cover"`, and `sizes="100vw"` for optimized loading
  - Import Image from "next/image"
  - Import HeroSearchBar from "@/components/HeroSearchBar"
  </action>
  <verify>
  Run `npx next build` or `npx next lint` to confirm no build errors.
  Verify HeroSearchBar.tsx exists and exports a client component.
  Verify page.tsx imports Image from next/image and HeroSearchBar.
  </verify>
  <done>
  Search bar is a functional client component that navigates to /properties?q={query}.
  Hero image uses Next.js Image with priority loading for LCP optimization.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate TiltCard micro-interactions and fix link targets</name>
  <files>src/app/(site)/page.tsx</files>
  <action>
  In `src/app/(site)/page.tsx`, make these targeted changes:

  1. FEATURED PROPERTIES (HOME-02): Wrap each PropertyCard in a TiltCard component.
     - Import `{ TiltCard }` from "@/components/ui/tilt-card"
     - In the featured properties grid, wrap: `<TiltCard key={property.id}><PropertyCard property={property} /></TiltCard>`
     - Remove the key from PropertyCard since TiltCard now has it

  2. WHY TAURO (HOME-04): Replace the `card-tilt` CSS class with the TiltCard React component for proper mouse-tracking 3D effect.
     - Wrap each feature card div in `<TiltCard key={item.title} maxTilt={6}>`
     - Remove the `card-tilt` class from the inner div since TiltCard handles the transform
     - Keep all other styling (group, rounded-xl, border, bg-midnight, p-6, etc.)

  3. NEIGHBORHOODS (HOME-03): Change link href from `/neighborhoods/${hood.slug}` to `/areas/${hood.slug}` to match Phase 5 area pages routing convention.

  4. SELLER CTA (HOME-06): Change the seller CTA Link href from `/contact` to `/sell` to match Phase 7 seller guide / Phase 6 seller inquiry page. Add `shimmer-gold` class to the seller CTA button (currently only has border style, no shimmer).

  5. Convert all remaining `<img>` tags in the page to Next.js `<Image>` components:
     - Neighborhood card images: use `fill` prop with parent `relative` container
     - CTA background images: use `fill` prop with parent `relative` container
     - Add `sizes` prop appropriate to each context
     - Keep all existing className styling

  IMPORTANT: The page imports TiltCard which is a "use client" component. Since TiltCard is imported as a child component (not used as a hook), the page can remain a Server Component. This is fine -- Server Components can render Client Components as children.

  However, if the linter or build complains, add "use client" to page.tsx as a fallback.
  </action>
  <verify>
  Run `npx next build` to confirm clean build.
  Grep for `/neighborhoods/` in page.tsx -- should return 0 results (all changed to /areas/).
  Grep for `href="/contact"` in the CTA section -- should be `/sell` instead.
  Grep for `TiltCard` in page.tsx -- should appear in imports and in both featured properties and why-tauro sections.
  Grep for `<img` in page.tsx -- should return 0 results (all converted to Next.js Image).
  </verify>
  <done>
  TiltCard wraps property cards (3D mouse-tracking on hover) and Why Tauro feature cards.
  Neighborhood links point to /areas/{slug}.
  Seller CTA links to /sell with shimmer-gold effect.
  All images use Next.js Image for optimized loading.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npx next build` succeeds with no errors
2. `grep -c "TiltCard" src/app/(site)/page.tsx` returns 2+ (import + usage)
3. `grep -c "/areas/" src/app/(site)/page.tsx` returns 6 (one per neighborhood)
4. `grep -c 'href="/sell"' src/app/(site)/page.tsx` returns 1
5. `grep -c "next/image" src/app/(site)/page.tsx` returns 1 (import)
6. HeroSearchBar.tsx exists with "use client" and router.push logic
</verification>

<success_criteria>
- Search bar navigates to /properties?q={query} on submit
- Hero uses Next.js Image with priority for LCP
- PropertyCards wrapped in TiltCard for 3D hover effect
- Why Tauro cards use TiltCard instead of CSS-only card-tilt
- All neighborhood links point to /areas/{slug}
- Seller CTA links to /sell with shimmer-gold class
- All images use Next.js Image component
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/02-homepage/02-02-SUMMARY.md`
</output>
