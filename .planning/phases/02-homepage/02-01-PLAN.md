---
phase: 02-homepage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/hero.tsx
  - src/components/HeroSearchBar.tsx
  - src/components/stats-bar.tsx
autonomous: true
status: COMPLETED
completed_at: 2026-03-18

must_haves:
  truths:
    - "Hero section fills the entire viewport with a background image and gradient overlay"
    - "Animated headline renders with gold accent text"
    - "Search bar accepts text and navigates to /properties?q={query} on submit"
    - "Stats bar displays 4 metrics in a responsive grid below the hero"
    - "Scroll hint animates with a bounce effect"
  artifacts:
    - path: "src/components/hero.tsx"
      provides: "Full-viewport cinematic hero section"
      exports: ["default"]
    - path: "src/components/HeroSearchBar.tsx"
      provides: "Client-side search bar with router navigation"
      exports: ["default"]
    - path: "src/components/stats-bar.tsx"
      provides: "Stats bar with 4 key metrics"
      exports: ["default"]
  key_links:
    - from: "src/components/hero.tsx"
      to: "src/components/HeroSearchBar.tsx"
      via: "component import"
      pattern: "import HeroSearchBar"
    - from: "src/components/HeroSearchBar.tsx"
      to: "/properties"
      via: "router.push on form submit"
      pattern: "router\\.push.*properties"
---

<objective>
Build the cinematic full-viewport hero section, search bar, and stats bar as standalone components.

Purpose: The hero is the first thing visitors see -- it must feel premium, cinematic, and conversion-focused. This plan creates the three components that form the top of the homepage (hero + search + stats) without modifying the page file itself.
Output: Three standalone components ready for page assembly in Plan 04.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/app/(site)/page.tsx
@src/components/ui/tilt-card.tsx
@src/components/ui/gold-shimmer.tsx
@src/lib/utils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Hero component with background image and gradient overlay</name>
  <files>src/components/hero.tsx, src/components/HeroSearchBar.tsx</files>
  <action>
Create `src/components/hero.tsx` as a server-compatible component (no "use client"). Structure:

1. Full-viewport section: `relative flex min-h-screen items-center justify-center overflow-hidden pt-16`
2. Background image using `next/image` with these exact props:
   - `src="https://images.unsplash.com/photo-1560518883-ce09059eeffa?w=1920&q=80"`
   - `alt="Philadelphia skyline"`
   - `fill`, `priority`, `sizes="100vw"`, `className="object-cover"`
   - Add a comment: `{/* Swap to <video> for cinematic reel later */}`
3. Gradient overlay: `absolute inset-0 bg-gradient-to-b from-near-black/70 via-near-black/50 to-near-black`
4. Content container: `relative z-10 mx-auto max-w-7xl px-4 py-32 text-center sm:px-6 lg:px-8`
   - Kicker: "Premium Philadelphia Real Estate" -- `font-label text-sm font-semibold uppercase tracking-[0.25em] text-gold mb-4`
   - H1: "Find Your Place" + `<br />` + `<span className="text-gold">in Philadelphia</span>` -- `font-heading text-4xl font-bold leading-tight text-white sm:text-5xl md:text-6xl lg:text-7xl`
   - Subtitle: "Discover luxury homes and condos across Philadelphia's most coveted neighborhoods with Tauro's expert agents." -- `mx-auto mt-6 max-w-2xl text-lg text-white/70`
   - Render `<HeroSearchBar />` imported from `@/components/HeroSearchBar`
   - Scroll hint: bouncing pill with `mt-16 animate-bounce`, outer `mx-auto h-8 w-5 rounded-full border-2 border-white/30`, inner dot `mx-auto mt-1.5 h-2 w-1 rounded-full bg-gold`

Export as `default function Hero()`.

The existing `HeroSearchBar.tsx` is already correct -- it's a "use client" component with useState for query, useRouter from next/navigation, and a form that pushes to `/properties?q=`. Do NOT modify it. Just verify it exists and imports cleanly.
  </action>
  <verify>Run `npx tsc --noEmit` -- should pass with no errors. Verify `src/components/hero.tsx` exists.</verify>
  <done>Hero component renders full-viewport section with image, gradient, headline, search bar, and scroll hint. HeroSearchBar is unchanged and functional.</done>
</task>

<task type="auto">
  <name>Task 2: Create StatsBar component</name>
  <files>src/components/stats-bar.tsx</files>
  <action>
Create `src/components/stats-bar.tsx` as a server component. Structure:

1. Define a `stats` array with 4 items:
   - `{ value: "500+", label: "Properties Sold" }`
   - `{ value: "15", label: "Neighborhoods" }`
   - `{ value: "$2.1B", label: "Total Volume" }`
   - `{ value: "98%", label: "Client Satisfaction" }`

2. Section wrapper: `border-y border-border/40 bg-midnight`
3. Inner grid: `mx-auto grid max-w-7xl grid-cols-2 gap-8 px-4 py-14 sm:px-6 md:grid-cols-4 lg:px-8`
4. Each stat item:
   - Value: `font-heading text-3xl font-bold text-gold sm:text-4xl`
   - Label: `mt-1 text-sm text-muted-foreground`

Export as `default function StatsBar()`.
  </action>
  <verify>Run `npx tsc --noEmit` -- should pass. Verify `src/components/stats-bar.tsx` exists and exports default.</verify>
  <done>StatsBar renders 4 gold-highlighted metrics in a responsive 2-col (mobile) / 4-col (desktop) grid.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- All three component files exist and export defaults
- HeroSearchBar uses "use client" and has router navigation logic
</verification>

<success_criteria>
- Hero component is a self-contained section filling the viewport
- Stats bar displays 4 metrics with gold text and muted labels
- Search bar navigates to /properties with query param on submit
- All components are server-compatible except HeroSearchBar (client)
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-homepage/02-01-SUMMARY.md`
</output>
