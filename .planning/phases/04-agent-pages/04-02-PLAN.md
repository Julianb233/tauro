---
phase: 04-agent-pages
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/components/AgentCard.tsx
  - src/app/(site)/agents/page.tsx
autonomous: true

must_haves:
  truths:
    - "Visiting /agents shows a grid of all agent cards"
    - "Each agent card displays photo, name, title, and contact link"
    - "Clicking an agent card navigates to /agents/{slug}"
    - "Page follows the dark/gold Tauro brand system"
  artifacts:
    - path: "src/components/AgentCard.tsx"
      provides: "Reusable agent card component"
      min_lines: 30
    - path: "src/app/(site)/agents/page.tsx"
      provides: "Team page with agent grid"
      min_lines: 50
  key_links:
    - from: "src/app/(site)/agents/page.tsx"
      to: "src/data/agents.ts"
      via: "import { agents } from '@/data/agents'"
      pattern: "import.*agents.*from.*@/data/agents"
    - from: "src/components/AgentCard.tsx"
      to: "/agents/[slug]"
      via: "Link href to agent profile"
      pattern: "href=.*agents/.*slug"
---

<objective>
Build the team page at `/agents` with a responsive grid of agent cards.

Purpose: This is the main team showcase page (AGENT-01). Navbar already links to `/agents`. Each card links to the individual profile page.

Output: `AgentCard.tsx` component and `/agents` page.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/data/agents.ts — Agent type and agents array (created in 04-01).
@src/components/PropertyCard.tsx — Follow this exact pattern for card structure: Link wrapping, image with hover scale, info below.
@src/app/(site)/contact/page.tsx — Follow hero section pattern: dark bg, gold accent text, heading, subtitle.
@src/app/globals.css — Available classes: `shimmer-gold`, `card-tilt`, `font-label`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AgentCard component</name>
  <files>src/components/AgentCard.tsx</files>
  <action>
Create `src/components/AgentCard.tsx` following the `PropertyCard.tsx` pattern:

```
- Import Link from "next/link" and Agent type from "@/data/agents"
- Accept `{ agent: Agent }` prop
- Outer: `<Link href={`/agents/${agent.slug}`}>` with same classes as PropertyCard (group, rounded-xl, border, bg-card, hover:border-gold/40, hover:shadow-lg)
- Add `card-tilt` class to the outer Link for 3D hover effect
- Photo section: `aspect-[3/4]` (portrait) with overflow-hidden, bg-muted
  - <img> with agent.photo, alt={agent.fullName}, object-cover, group-hover:scale-105 transition
- Info section below photo (p-5):
  - Agent name: `font-heading text-lg font-bold text-white`
  - Title: `text-sm text-gold font-label uppercase tracking-wider mt-1`
  - Short bio: `text-sm text-muted-foreground mt-3 line-clamp-2`
  - Stats row (mt-4): Show `stats.propertiesSold` sold and `stats.totalVolume` volume in a flex row with divider
    - Use `text-xs text-muted-foreground` with gold numbers
  - Contact row (mt-4, pt-4, border-t border-border/40): phone and email as small text
    - Phone: `text-sm text-white/80`
    - Email: `text-sm text-gold hover:underline` (wrap email in a span, NOT a nested <a> since outer is Link — use onClick stopPropagation with window.location.href for email)
  - Bottom: "View Profile" text with ArrowRight icon, opacity-0 group-hover:opacity-100 transition (like contact page link pattern)
```

Do NOT use nested `<a>` tags inside the Link (invalid HTML). For the email click, either just display it as text or use an onClick with e.stopPropagation() + e.preventDefault() + window.location.href = `mailto:${agent.email}`.
  </action>
  <verify>Run `npx tsc --noEmit` — component compiles without errors.</verify>
  <done>AgentCard component renders agent photo, name, title, short bio, stats, contact info, and links to agent profile page.</done>
</task>

<task type="auto">
  <name>Task 2: Create /agents team page</name>
  <files>src/app/(site)/agents/page.tsx</files>
  <action>
Create `src/app/(site)/agents/page.tsx` as a server component (no "use client"):

**Metadata export:**
```typescript
export const metadata: Metadata = {
  title: "Our Agents | Tauro",
  description: "Meet the Tauro team — Philadelphia's premier luxury real estate agents.",
};
```

**Page structure** — follow contact page hero pattern:

1. **Hero section** (same structure as contact page hero):
   - `section.relative.overflow-hidden.bg-near-black.pb-20.pt-32`
   - Gradient overlay: `bg-gradient-to-b from-midnight/60 to-near-black`
   - Gold label: "Our Team"
   - Heading: "Meet the Tauro Agents"
   - Subtitle: "Our agents bring deep Philadelphia market expertise, premium service, and a track record of results."

2. **Team grid section:**
   - `section.bg-midnight.py-20`
   - `mx-auto max-w-7xl px-4 sm:px-6 lg:px-8`
   - Grid: `grid gap-8 sm:grid-cols-2 lg:grid-cols-4`
   - Map over `agents` array rendering `<AgentCard agent={agent} />` for each

3. **CTA section** (below grid):
   - Rounded card with border, bg-near-black, centered text
   - Heading: "Interested in Joining Tauro?"
   - Subtitle: "We're always looking for exceptional agents who share our commitment to excellence."
   - Gold CTA button linking to `/join` with text "Learn More" and shimmer-gold class
   - This drives traffic to the Join Our Team page (04-04)

Import agents from `@/data/agents` and AgentCard from `@/components/AgentCard`.
  </action>
  <verify>Run `npx tsc --noEmit`. Then run `npm run build` (or `npx next build`) and confirm no errors. Visit `/agents` in dev to verify rendering.</verify>
  <done>/agents page renders hero + grid of 4 agent cards + join CTA. Each card links to /agents/{slug}. Page has proper metadata.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `/agents` route resolves and renders (check with `npm run build` or dev server)
- 4 agent cards display in a responsive grid
- Each card shows photo, name, title, stats
- Cards link to `/agents/{slug}`
- Join CTA links to `/join`
- Hero section matches brand styling (dark bg, gold accents)
</verification>

<success_criteria>
Team page at /agents shows responsive grid of agent cards with proper Tauro branding. Cards link to individual profiles. Join CTA links to /join.
</success_criteria>

<output>
After completion, create `.planning/phases/04-agent-pages/04-02-SUMMARY.md`
</output>
