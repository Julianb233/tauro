---
phase: 04-agent-pages
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/app/(site)/agents/[slug]/page.tsx
  - src/app/(site)/agents/[slug]/AgentProfileClient.tsx
autonomous: true

must_haves:
  truths:
    - "Individual agent profile page renders at /agents/[slug] with full bio and photo"
    - "Stats bar shows properties sold, total volume, avg days on market, years experience"
    - "Active listings section shows property cards for agent's listings"
    - "Awards section displays agent's awards and recognition"
    - "Video introduction embeds YouTube iframe when agent has videoIntroUrl"
    - "Contact form captures name, email, phone, message and submits to /api/leads"
    - "Specialties and neighborhoods render as pill/tag elements"
  artifacts:
    - path: "src/app/(site)/agents/[slug]/page.tsx"
      provides: "Server component with generateStaticParams, generateMetadata, renders AgentProfileClient"
      exports: ["generateStaticParams", "generateMetadata", "default"]
    - path: "src/app/(site)/agents/[slug]/AgentProfileClient.tsx"
      provides: "Client component with full agent profile and contact form"
  key_links:
    - from: "src/app/(site)/agents/[slug]/page.tsx"
      to: "src/data/agents.ts"
      via: "getAgentBySlug for metadata and data"
      pattern: "getAgentBySlug"
    - from: "src/app/(site)/agents/[slug]/AgentProfileClient.tsx"
      to: "/api/leads"
      via: "fetch POST for contact form submission"
      pattern: "fetch.*api/leads"
    - from: "src/app/(site)/agents/[slug]/AgentProfileClient.tsx"
      to: "src/data/properties.ts"
      via: "import properties to filter active listings"
      pattern: "import.*properties.*from.*@/data/properties"
    - from: "src/app/(site)/agents/[slug]/AgentProfileClient.tsx"
      to: "src/components/PropertyCard.tsx"
      via: "renders PropertyCard for active listings"
      pattern: "PropertyCard"
---

<objective>
Build the individual agent profile page at /agents/[slug] with full bio, stats, listings, awards, video embed, and contact form.

Purpose: Give each agent a rich, trust-building profile page that showcases their expertise and provides a direct lead capture form.
Output: Server page.tsx with SSG + metadata, and AgentProfileClient.tsx with all profile sections and contact form.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-agent-pages/04-01-SUMMARY.md
@src/data/agents.ts (Agent type, getAgentBySlug, agents array)
@src/data/properties.ts (properties array for active listings)
@src/components/PropertyCard.tsx (card component for listings)
@src/app/api/leads/route.ts (LeadPayload interface, POST endpoint)
@src/app/globals.css (brand tokens)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server page with SSG and metadata</name>
  <files>src/app/(site)/agents/[slug]/page.tsx</files>
  <action>
Create the server component page that handles static generation and metadata.

**generateStaticParams:**
- Import agents array, return agents.map(a => ({ slug: a.slug }))

**generateMetadata:**
- Params type: { params: Promise<{ slug: string }> } (Next.js 15 async params pattern)
- Await params, call getAgentBySlug(slug)
- If not found, return { title: "Agent Not Found" }
- Return title: "{fullName} -- {title}", description: first 120 chars of bio + "Contact for expert real estate guidance."

**Default export:**
- Async function, await params, get agent by slug
- If not found, call notFound() from next/navigation
- Render <AgentProfileClient slug={slug} />

Import AgentProfileClient from "./AgentProfileClient".
  </action>
  <verify>Run `npx tsc --noEmit` -- no type errors. Check that generateStaticParams returns array of slug objects for all 6 agents.</verify>
  <done>Server page handles SSG for all 6 agent slugs, generates proper metadata with agent name and bio excerpt, renders client component.</done>
</task>

<task type="auto">
  <name>Task 2: Agent profile client component with all sections and contact form</name>
  <files>src/app/(site)/agents/[slug]/AgentProfileClient.tsx</files>
  <action>
Create the full agent profile as a client component ("use client" for form interactivity).

**Props:** { slug: string }
**Setup:** Call getAgentBySlug(slug), if not found call notFound(). Filter properties array by agent.activeListingIds.

**Form state management:**
- FormData interface: { firstName, lastName, email, phone, message } all strings
- FormState type: "idle" | "submitting" | "success" | "error"
- useState for form, state, errorMsg

**Sections (in order):**

1. **Hero/Header** (bg-near-black, pt-32 pb-16):
   - "Back to Team" link with ArrowLeft icon -> /agents
   - 3-column grid (lg:grid-cols-3):
     - Left (lg:col-span-1): Agent photo in rounded-2xl container with gold border (border-gold/20), aspect-[3/4], Next.js Image fill + object-cover
     - Right (lg:col-span-2): fullName (font-heading text-4xl), title (font-label uppercase text-gold), license number (text-xs text-muted-foreground), full bio paragraph, language pills (rounded-full border, bg-midnight), social links (Instagram + LinkedIn icons in bordered squares with gold hover), contact buttons row (phone with border outline + email with solid gold bg)

2. **Stats Bar** (border-y border-border/40, bg-midnight, py-12):
   - 4-column grid (grid-cols-2 lg:grid-cols-4):
     - Properties Sold (Home icon), Total Volume (TrendingUp icon), Avg Days on Market (Clock icon), Years Experience (Award icon)
   - Each stat: icon (text-gold/60), value (font-heading text-3xl text-gold), label (font-label uppercase text-xs text-muted-foreground)

3. **Specialties & Neighborhoods** (bg-near-black, py-16):
   - 2-column grid (lg:grid-cols-2)
   - Specialties: heading + flex-wrap gap-2 pills (rounded-full, border-gold/30, bg-gold/5, text-gold)
   - Neighborhoods: heading + flex-wrap gap-2 pills (rounded-full, border-border/40, text-white/80)

4. **Awards & Recognition** (bg-midnight, py-16, conditional on awards.length > 0):
   - Heading, grid of award cards (sm:grid-cols-2 lg:grid-cols-3)
   - Each card: Award icon (text-gold), title (font-semibold text-white), issuer + year

5. **Video Introduction** (bg-near-black, py-16, conditional on videoIntroUrl truthy):
   - Play icon + heading
   - YouTube iframe in aspect-video container with rounded-xl border

6. **Active Listings** (bg-midnight, py-16, conditional on activeListings.length > 0):
   - Heading with agent.firstName + "'s Active Listings" + count badge (bg-gold/10 text-gold)
   - Grid of PropertyCard components (sm:grid-cols-2 lg:grid-cols-3)

7. **Contact Form** (bg-near-black, py-16):
   - Centered (max-w-2xl), heading "Contact {firstName}"
   - Form card (rounded-2xl, border, bg-midnight, p-8, shadow-xl)
   - Success state: gold CheckCircle icon, "Message Sent to {firstName}!" heading, confirmation text, "Send Another Message" button
   - Error state: destructive alert with AlertCircle icon and errorMsg
   - Form fields: firstName + lastName (2-col grid), email, phone, message (textarea rows=5)
   - All required fields marked with gold asterisk
   - Input styling: border-border/40, bg-midnight, focus:border-gold/60 focus:ring-gold/20
   - Submit button: full-width, bg-gold, disabled state during submitting
   - Privacy policy link at bottom

**Form submission handler:**
- preventDefault, set state to "submitting"
- Build LeadPayload with type: "agent-contact", include agentName and agentSlug
- POST to /api/leads with JSON body
- On success: setState("success"), reset form
- On error: setState("error"), set errorMsg from response or fallback

**Icons to import from lucide-react:** Phone, Mail, Award, Play, Home, TrendingUp, Clock, CheckCircle, AlertCircle, ArrowLeft, Instagram, Linkedin
  </action>
  <verify>Run `npm run build` -- page compiles for all 6 agent slugs. Visit /agents/julian-bradley -- full profile renders with photo, bio, stats, specialties, awards, video embed, listings, contact form. Submit contact form -- POST to /api/leads returns 200. Visit /agents/marcus-thompson -- no video section (videoIntroUrl is null), no active listings section (empty activeListingIds).</verify>
  <done>Agent profile page renders all sections conditionally: hero with photo/bio/contact, stats bar, specialties/neighborhoods, awards, video, active listings, contact form. Form submits to /api/leads with agent-contact type.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- /agents/julian-bradley shows full profile with video, listings, awards, and contact form
- /agents/marcus-thompson correctly hides video and listings sections (null/empty data)
- Contact form submits to /api/leads and shows success state
- generateStaticParams produces all 6 agent slugs
- Metadata includes agent name and bio excerpt
- Back to Team link navigates to /agents
</verification>

<success_criteria>
- All 6 agent profile pages render at /agents/[slug] with correct data
- Profile shows: photo, name, title, license, bio, languages, social, contact buttons, stats, specialties, neighborhoods, awards, video, active listings, contact form
- Conditional sections (video, listings, awards) only show when data exists
- Contact form validates, submits to API, shows success/error states
- SSG works via generateStaticParams for all agents
</success_criteria>

<output>
After completion, create `.planning/phases/04-agent-pages/04-02-SUMMARY.md`
</output>
