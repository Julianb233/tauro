---
status: COMPLETED
phase: 04-agent-pages
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/app/(site)/join/page.tsx
autonomous: true

must_haves:
  truths:
    - "Join Our Team page renders at /join with hero, benefits grid, and application form"
    - "Application form captures first name, last name, email, phone, PA license number, years of experience, current brokerage, why join, and additional notes"
    - "Form submits to /api/leads with type agent-application"
    - "Benefits section showcases 6 reasons to join Tauro"
    - "Requirements section lists what Tauro looks for in agents"
  artifacts:
    - path: "src/app/(site)/join/page.tsx"
      provides: "Join Our Team page with benefits grid and application form"
  key_links:
    - from: "src/app/(site)/join/page.tsx"
      to: "/api/leads"
      via: "fetch POST for application submission"
      pattern: "fetch.*api/leads"
    - from: "src/app/(site)/join/page.tsx"
      to: "src/app/api/leads/route.ts"
      via: "imports LeadPayload type"
      pattern: "import.*LeadPayload.*from.*@/app/api/leads"
---

<objective>
Build the "Join Our Team" page where prospective realtors can learn about Tauro's benefits and submit an application.

Purpose: Recruit new agents by showcasing the Tauro value proposition and providing a frictionless application form that feeds into the CRM.
Output: Full /join page with hero, benefits grid, requirements list, and application form.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/app/api/leads/route.ts (LeadPayload interface with agent-application type)
@src/app/globals.css (brand tokens)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Join Our Team page with benefits and application form</name>
  <files>src/app/(site)/join/page.tsx</files>
  <action>
Create the complete Join Our Team page as a client component ("use client" for form).

**Form state management (same pattern as agent contact form):**
- FormData: { firstName, lastName, email, phone, licenseNumber, yearsExperience, currentBrokerage, whyJoin, message }
- FormState: "idle" | "submitting" | "success" | "error"

**Static data arrays (defined above component):**

Benefits array (6 items with icon, title, description):
1. TrendingUp - "Competitive Splits" - industry-leading commission structure
2. Building2 - "Premium Brand" - leverage the Tauro luxury brand
3. Zap - "Cutting-Edge Tech" - CRM, marketing automation, AI tools
4. GraduationCap - "Ongoing Training" - weekly masterclasses, mentorship
5. Handshake - "Lead Generation" - inbound leads from web presence
6. Users - "Collaborative Culture" - team-first environment

Experience levels: ["Less than 1 year", "1-3 years", "3-5 years", "5-10 years", "10+ years"]

Requirements list:
1. "Active PA real estate license (or willingness to transfer)"
2. "Strong work ethic and client-first mentality"
3. "Excellent communication skills"
4. "Desire to grow within a team environment"

**Sections:**

1. **Hero** (bg-near-black, pt-32 pb-20, gradient overlay):
   - "Careers" label (uppercase, text-gold, tracking-[0.25em])
   - h1: "Join the Tauro Team" (font-heading, text-4xl sm:text-5xl lg:text-6xl)
   - Subtitle about building Philadelphia's elite team

2. **Why Agents Choose Tauro** (bg-midnight, py-20):
   - Centered "The Tauro Advantage" label + "Why Agents Choose Tauro" heading
   - 3-column grid (sm:grid-cols-2 lg:grid-cols-3) of benefit cards
   - Each card: bordered (border-border/40), bg-near-black, icon in gold circle (bg-gold/10), title (font-heading text-lg), description (text-muted-foreground)
   - Hover: border-gold/30 + shadow-lg

3. **Application Form** (bg-near-black, py-20):
   - 2-column layout (lg:grid-cols-2):
   - **Left column:** "Ready to Make a Move?" heading, recruiting pitch paragraph, "What We Look For" heading with requirements list (each with CheckCircle icon in gold)
   - **Right column:** Form card (rounded-2xl, border, bg-midnight, p-8, shadow-xl)

   **Form fields:**
   - "Apply Now" heading + subtitle
   - Error alert (same pattern as agent contact form)
   - First Name + Last Name (2-col grid) -- required
   - Email -- required
   - Phone -- required
   - PA License Number -- required, placeholder "RS-XXXXXX"
   - Years of Experience -- required, select dropdown with experience levels, bg-near-black for options
   - Current Brokerage -- optional
   - "Why do you want to join Tauro?" -- optional, textarea rows=4
   - Additional Notes -- optional, textarea rows=3, placeholder mentions resume/portfolio link
   - Submit button: full-width bg-gold, "Submit Application" / "Submitting..."
   - Privacy policy link

   **Success state:**
   - Gold CheckCircle icon, "Application Submitted!" heading
   - Confirmation text about 2-3 business day review
   - "Submit Another Application" button

**Form submission:**
- Build LeadPayload with type: "agent-application"
- Include all fields: firstName, lastName, email, phone, licenseNumber, yearsExperience, currentBrokerage, whyJoin, message
- POST to /api/leads
- Handle success/error same as agent contact form

**Input styling consistency:**
- Use bg-near-black (not bg-midnight) for inputs since form card bg is bg-midnight -- ensures contrast
- All other input styles match: border-border/40, focus:border-gold/60, focus:ring-gold/20

**Icons from lucide-react:** Users, TrendingUp, Award, Briefcase, GraduationCap, CheckCircle, AlertCircle, Building2, Handshake, Zap
  </action>
  <verify>Run `npm run build` -- page compiles. Visit /join -- hero, 6 benefit cards, requirements list, and application form all render. Fill out form and submit -- POST to /api/leads returns 200, success state shows. Verify select dropdown works for years of experience.</verify>
  <done>Join page shows hero, 6 benefit cards in grid, requirements checklist, and fully functional application form. Form submits to /api/leads with agent-application type and shows success/error states.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- /join page renders hero, benefits grid, requirements, and application form
- All 6 benefit cards display with correct icons, titles, descriptions
- Requirements list shows 4 items with gold checkmarks
- Form validates required fields (firstName, lastName, email, phone, licenseNumber, yearsExperience)
- Years of experience uses select dropdown with 5 options
- Form submits to /api/leads with type "agent-application" and all fields
- Success state shows confirmation message
- Error state shows error alert
- Input backgrounds (bg-near-black) contrast against form card (bg-midnight)
</verification>

<success_criteria>
- /join page is fully functional with hero, benefits, requirements, and application form
- Application form captures: name, email, phone, PA license, experience, brokerage, motivation, notes
- Form submits to existing /api/leads endpoint with agent-application type
- Visual design matches Tauro brand: dark backgrounds, gold accents, proper typography
- Success and error states provide clear feedback
</success_criteria>

<output>
After completion, create `.planning/phases/04-agent-pages/04-03-SUMMARY.md`
</output>
