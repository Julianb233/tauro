---
phase: 04-agent-pages
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/app/(site)/agents/[slug]/page.tsx
  - src/app/api/leads/route.ts
autonomous: true

must_haves:
  truths:
    - "Visiting /agents/julian-bradley shows Julian's full profile page"
    - "Profile page shows bio, stats, awards, active listings, and video intro"
    - "Agent contact form captures name, email, phone, message and submits to /api/leads"
    - "Invalid slugs return 404"
    - "Active listings section shows real PropertyCard components for the agent's listings"
  artifacts:
    - path: "src/app/(site)/agents/[slug]/page.tsx"
      provides: "Individual agent profile page with all sections"
      min_lines: 100
    - path: "src/app/api/leads/route.ts"
      provides: "Updated LeadPayload with agent-contact type"
      contains: "agent-contact"
  key_links:
    - from: "src/app/(site)/agents/[slug]/page.tsx"
      to: "src/data/agents.ts"
      via: "getAgentBySlug lookup"
      pattern: "getAgentBySlug"
    - from: "src/app/(site)/agents/[slug]/page.tsx"
      to: "src/data/properties.ts"
      via: "filter properties by agent's activeListingIds"
      pattern: "properties\\.filter"
    - from: "src/app/(site)/agents/[slug]/page.tsx"
      to: "/api/leads"
      via: "fetch POST with type agent-contact"
      pattern: "fetch.*api/leads"
---

<objective>
Build individual agent profile pages at `/agents/[slug]` with full bio, stats, awards, video intro, active listings, and a contact form.

Purpose: This is the most complex page covering AGENT-02, AGENT-03, AGENT-05, and AGENT-06. Each agent gets a rich profile that builds trust and captures leads routed to that specific agent.

Output: Dynamic `[slug]/page.tsx` with all profile sections and an agent-specific contact form.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/data/agents.ts — Agent type, agents array, getAgentBySlug (created in 04-01).
@src/data/properties.ts — Property type, properties array for active listings display.
@src/components/PropertyCard.tsx — Reuse for active listings grid.
@src/app/(site)/contact/page.tsx — Follow exact form pattern: FormState type, FormData interface, initialForm, handleChange, handleSubmit with fetch to /api/leads, success/error states, input styling classes.
@src/app/api/leads/route.ts — Extend LeadPayload type to support "agent-contact" type with agentName/agentSlug fields.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update LeadPayload to support agent-contact type</name>
  <files>src/app/api/leads/route.ts</files>
  <action>
Update the existing `LeadPayload` interface and `buildGhlContact` function in `src/app/api/leads/route.ts`:

1. Add `"agent-contact"` to the `type` union: `type: "contact" | "showing" | "seller" | "agent-contact"`
2. Add optional fields to LeadPayload:
   - `agentName?: string;`  (the agent being contacted)
   - `agentSlug?: string;`
3. In `buildGhlContact`, add tag for agent-contact: `if (data.type === "agent-contact") tags.push("agent-contact");`
4. Add custom fields mapping: `if (data.agentName) customFields.agent_name = data.agentName;`

This is a minimal change — just extending the existing union type and adding 2 optional fields + 2 lines in the builder function. Do NOT rewrite the file, only add these specific changes.
  </action>
  <verify>Run `npx tsc --noEmit` — existing form pages still compile. Run `curl -X POST http://localhost:3000/api/leads -H "Content-Type: application/json" -d '{"type":"agent-contact","firstName":"Test","lastName":"User","email":"test@test.com","phone":"2155551234","agentName":"Julian Bradley"}'` returns 200.</verify>
  <done>LeadPayload now supports "agent-contact" type with agentName and agentSlug fields. Existing forms unaffected.</done>
</task>

<task type="auto">
  <name>Task 2: Create agent profile page with all sections</name>
  <files>src/app/(site)/agents/[slug]/page.tsx</files>
  <action>
Create `src/app/(site)/agents/[slug]/page.tsx`. This page has a server component wrapper for metadata and a client component for the contact form.

**Approach:** Make the page a server component that renders static sections, and extract the contact form into a client component within the same file (or inline "use client" component).

Actually, since the page needs a contact form with state, use this pattern:
- The page file is `"use client"` (like contact/page.tsx)
- Use `generateMetadata` in a separate layout or skip metadata on this dynamic page (Next.js 16 allows metadata only in server components)

**Simpler approach:** Make the whole page `"use client"` like the contact page does. Skip the metadata export (or add it via a separate `layout.tsx` — not needed for MVP).

**File structure:**

```typescript
"use client";

import { useState } from "react";
import { useParams } from "next/navigation";
import { notFound } from "next/navigation";
import Link from "next/link";
import {
  Phone, Mail, MapPin, Award, Play, Home, TrendingUp,
  Users, Clock, CheckCircle, AlertCircle, ArrowLeft,
  Instagram, Linkedin
} from "lucide-react";
import { agents, getAgentBySlug } from "@/data/agents";
import { properties } from "@/data/properties";
import PropertyCard from "@/components/PropertyCard";
import type { LeadPayload } from "@/app/api/leads/route";
```

**Page sections (top to bottom):**

**1. Agent lookup + 404:**
```typescript
const params = useParams();
const agent = getAgentBySlug(params.slug as string);
if (!agent) notFound();
```

**2. Hero / Header section:**
- `bg-near-black pt-32 pb-16`
- Back link: "Back to Team" with ArrowLeft, links to `/agents`
- Two-column layout (lg:grid-cols-3):
  - Left col (lg:col-span-1): Agent photo in rounded-2xl overflow-hidden, aspect-[3/4], border border-gold/20
  - Right col (lg:col-span-2):
    - Name: `font-heading text-4xl font-bold text-white`
    - Title: `text-gold font-label uppercase tracking-wider text-sm mt-2`
    - License: `text-xs text-muted-foreground mt-1` showing license number
    - Bio: `text-muted-foreground mt-6 leading-relaxed whitespace-pre-line` rendering agent.bio
    - Languages: small pills/badges showing each language
    - Social links: icon buttons for instagram/linkedin (open in new tab)
    - Contact buttons row (mt-6): Phone button (gold outline) + Email button (gold filled)

**3. Stats bar:**
- `bg-midnight py-12 border-y border-border/40`
- 4-column grid showing stats:
  - Properties Sold: `agent.stats.propertiesSold`+
  - Total Volume: `agent.stats.totalVolume`
  - Avg Days on Market: `agent.stats.avgDaysOnMarket`
  - Years Experience: `agent.stats.yearsExperience`
- Each stat: large gold number (`font-heading text-3xl text-gold`) + label below (`text-xs text-muted-foreground font-label uppercase tracking-wider`)

**4. Specialties & Neighborhoods:**
- `bg-near-black py-16`
- Two-column grid:
  - Specialties: heading + pills/badges for each specialty (rounded-full border border-gold/30 bg-gold/5 text-gold text-sm px-4 py-1.5)
  - Neighborhoods: heading + pills for each neighborhood (same styling but border-border/40 text-white/80)

**5. Awards section** (AGENT-05) — only render if agent.awards.length > 0:
- `bg-midnight py-16`
- Section heading: "Awards & Recognition"
- Grid of award cards (sm:grid-cols-2 lg:grid-cols-3):
  - Each card: rounded-xl border border-border/40 bg-near-black p-6
  - Award icon (lucide Award) in gold
  - Title: `text-white font-semibold`
  - Issuer + Year: `text-sm text-muted-foreground`

**6. Video Introduction** (AGENT-06) — only render if agent.videoIntroUrl is not null:
- `bg-near-black py-16`
- Section heading: "Video Introduction" with Play icon
- YouTube embed in responsive 16:9 container:
  - `aspect-video rounded-xl overflow-hidden border border-border/40`
  - `<iframe src={agent.videoIntroUrl} allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowFullScreen className="h-full w-full" />`

**7. Active Listings section** — only render if agent has active listings:
- `bg-midnight py-16`
- Filter properties: `const activeListings = properties.filter(p => agent.activeListingIds.includes(p.id));`
- Section heading: `${agent.firstName}'s Active Listings` with count badge
- Grid: `grid gap-6 sm:grid-cols-2 lg:grid-cols-3`
- Map activeListings through `<PropertyCard property={p} />` (reuse existing component)

**8. Contact Form section** (AGENT-03):
- `bg-near-black py-16`
- Section heading: `Contact ${agent.firstName}`
- Follow EXACT same form pattern as `/contact/page.tsx`:
  - FormState type: `"idle" | "submitting" | "success" | "error"`
  - FormData interface with firstName, lastName, email, phone, message
  - handleSubmit sends to `/api/leads` with payload:
    ```typescript
    const payload: LeadPayload = {
      type: "agent-contact",
      firstName: form.firstName,
      lastName: form.lastName,
      email: form.email,
      phone: form.phone,
      message: form.message,
      agentName: agent.fullName,
      agentSlug: agent.slug,
    };
    ```
  - Success state shows "Message Sent to {agent.firstName}!" with same CheckCircle pattern
  - Error state shows same AlertCircle error banner
  - Same input styling classes: `w-full rounded-lg border border-border/40 bg-midnight px-4 py-2.5 text-sm text-white placeholder:text-muted-foreground focus:border-gold/60 focus:outline-none focus:ring-2 focus:ring-gold/20`
  - Same gold submit button styling
  - Privacy policy link at bottom
- Form should be in a max-w-2xl centered container with the rounded card treatment (rounded-2xl border border-border/40 bg-near-black p-8 shadow-xl — same as contact page form card, BUT since this section bg is already near-black, use bg-midnight for the form card instead)

**Styling notes:**
- Use consistent section padding: `py-16` for content sections
- Alternate backgrounds: near-black and midnight for visual rhythm
- All section headings: `font-heading text-2xl font-bold text-white` with optional gold decoration
- Max width container on all sections: `mx-auto max-w-7xl px-4 sm:px-6 lg:px-8`
  </action>
  <verify>
Run `npx tsc --noEmit` — no type errors.
Visit `/agents/julian-bradley` in dev server — all 8 sections render.
Visit `/agents/nonexistent` — returns 404.
Submit the contact form — network tab shows POST to /api/leads with type "agent-contact".
  </verify>
  <done>
Individual agent profile page renders at /agents/{slug} with: hero with photo + bio, stats bar, specialties + neighborhoods, awards section, video embed, active listings with PropertyCard components, and working contact form that submits to /api/leads with agent-contact type. Invalid slugs return 404.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `/agents/julian-bradley` renders full profile with all sections
- `/agents/sofia-martinez` renders (different data, different listings)
- `/agents/invalid-slug` returns 404
- Active listings section shows PropertyCard components for the agent's listed properties
- Video embed renders for agents with videoIntroUrl, hidden for those without
- Awards section renders for agents with awards, hidden for those without
- Contact form submits successfully with agent-contact type
- All sections use consistent Tauro dark/gold branding
</verification>

<success_criteria>
Agent profile pages render at /agents/[slug] with all 6 requirement sections (bio, stats, awards, video, listings, contact form). Form submissions reach /api/leads with agent routing info. 404 for invalid slugs.
</success_criteria>

<output>
After completion, create `.planning/phases/04-agent-pages/04-03-SUMMARY.md`
</output>
