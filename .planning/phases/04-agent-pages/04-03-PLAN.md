---
phase: 04-agent-pages
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(site)/careers/page.tsx
  - src/components/application-form.tsx
autonomous: true

must_haves:
  truths:
    - "Visiting /careers renders a 'Join Our Team' page with benefits section and application form"
    - "Application form collects: first name, last name, email, phone, license number, years of experience, current brokerage, resume upload field, and 'why Tauro' statement"
    - "Form submits to /api/leads with type 'agent-application' and all collected fields"
    - "Benefits section showcases 6 reasons to join Tauro (commission, brand, tech, training, leads, culture)"
    - "Page has a requirements/qualifications checklist"
  artifacts:
    - path: "src/app/(site)/careers/page.tsx"
      provides: "Join Our Team / careers page with hero, benefits, requirements, and application form"
      min_lines: 40
    - path: "src/components/application-form.tsx"
      provides: "Realtor application form component with all required fields"
      min_lines: 80
  key_links:
    - from: "src/components/application-form.tsx"
      to: "/api/leads"
      via: "fetch POST on form submit"
      pattern: "fetch.*api/leads"
---

<objective>
Create the "Join Our Team" realtor application page where prospective agents can apply to join Tauro.

Purpose: Delivers AGENT-04 — agent recruitment page. This page serves as a recruiting tool, showcasing Tauro's value proposition for agents and collecting qualified applications with license and experience info.
Output: A careers page with benefits showcase and a comprehensive application form.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/app/(site)/agents/page.tsx (the team page links to this careers page via "Join Our Team" CTA)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the careers page with benefits section</name>
  <files>src/app/(site)/careers/page.tsx</files>
  <action>
Create `src/app/(site)/careers/page.tsx`. This can be a "use client" page (form interactivity) or split into server page + client form component.

**Metadata** (if server component, export metadata; if client component, use a layout.tsx or head.tsx):
- title: "Join Our Team | Tauro"
- description: "Apply to join Philadelphia's premier luxury real estate team. Competitive splits, cutting-edge tech, and a collaborative culture."

**Page sections:**

1. **Hero** (bg-near-black, pb-20 pt-32, gradient overlay):
   - "Careers" label (gold, uppercase, tracking-[0.25em])
   - h1: "Join the Tauro Team" (font-heading, text-4xl/5xl/6xl responsive)
   - Subtitle: "We're building Philadelphia's most elite real estate team. If you're driven, client-focused, and ready to elevate your career, we want to hear from you."

2. **Why Agents Choose Tauro** (bg-midnight, py-20):
   - Section header: "The Tauro Advantage" label + "Why Agents Choose Tauro" h2
   - 6 benefit cards in a grid (sm:grid-cols-2 lg:grid-cols-3):
     1. Competitive Splits (TrendingUp icon) — "Industry-leading commission structure that rewards your hard work and growth."
     2. Premium Brand (Building2 icon) — "Leverage the Tauro name — a brand synonymous with luxury Philadelphia real estate."
     3. Cutting-Edge Tech (Zap icon) — "CRM, marketing automation, and AI tools to help you close more deals, faster."
     4. Ongoing Training (GraduationCap icon) — "Weekly masterclasses, mentorship programs, and access to top-producer coaching."
     5. Lead Generation (Handshake icon) — "Inbound leads from our premium web presence and marketing campaigns."
     6. Collaborative Culture (Users icon) — "A team-first environment where agents support each other's success."
   - Each card: rounded-xl, border border-border/40, bg-near-black, p-6, hover:border-gold/30
   - Icon in a 10x10 rounded-lg bg-gold/10 container

3. **Application section** (bg-near-black, py-20):
   - 2-column grid (lg:grid-cols-2):
   - Left column: "Ready to Make a Move?" heading, description paragraph, "What We Look For" checklist:
     - Active PA real estate license (or willingness to transfer)
     - Strong work ethic and client-first mentality
     - Excellent communication skills
     - Desire to grow within a team environment
     (Each item with CheckCircle icon in gold)
   - Right column: Render `<ApplicationForm />` component

Import icons from lucide-react: TrendingUp, Building2, Zap, GraduationCap, Handshake, Users, CheckCircle.
  </action>
  <verify>Run `npx tsc --noEmit` — no type errors. Visit /careers in dev server — hero, benefits grid, and form section all render.</verify>
  <done>Careers page renders with hero, 6 benefit cards, requirements checklist, and application form section.</done>
</task>

<task type="auto">
  <name>Task 2: Create the realtor application form component</name>
  <files>src/components/application-form.tsx</files>
  <action>
Create `src/components/application-form.tsx` as a "use client" component.

**Form state**: idle | submitting | success | error (same pattern as agent-contact-form).

**Form fields** (in order):
1. First Name (required, text, autoComplete="given-name")
2. Last Name (required, text, autoComplete="family-name")
   — Render in a 2-column grid row (grid gap-4 sm:grid-cols-2)
3. Email Address (required, email, autoComplete="email")
4. Phone Number (required, tel, autoComplete="tel")
5. PA License Number (required, text, placeholder "RS-XXXXXX")
6. Years of Experience (required, select dropdown with options: "Less than 1 year", "1-3 years", "3-5 years", "5-10 years", "10+ years")
7. Current Brokerage (optional, text, placeholder "e.g., Keller Williams")
8. Resume Upload (optional, file input, accept=".pdf,.doc,.docx")
   — Style the file input to match dark theme. Use a label-as-button pattern: visually hidden input + styled label that looks like a button/drop zone. Show selected filename when a file is chosen.
   — Note: For MVP, this is a UI-only field. The file won't actually upload anywhere yet — just capture the filename in the form state. Add a comment noting this needs a file upload endpoint in a future phase.
9. Why do you want to join Tauro? (optional, textarea, 4 rows)
10. Additional Notes (optional, textarea, 3 rows, placeholder "Link to your resume, portfolio, or anything else...")

**Submit handler**:
- POST to `/api/leads` with JSON body:
  ```
  {
    type: "agent-application",
    firstName, lastName, email, phone,
    licenseNumber, yearsExperience, currentBrokerage,
    whyJoin, message (from additional notes),
    resumeFilename (if file selected, just the name)
  }
  ```
- Success state: CheckCircle icon, "Application Submitted!" heading, "Thank you for your interest in joining Tauro. Our team lead will review your application and reach out within 2-3 business days.", "Submit Another Application" reset button
- Error state: red banner with AlertCircle icon and error message

**Input styling** (consistent with all other Tauro forms):
- `w-full rounded-lg border border-border/40 bg-near-black px-4 py-2.5 text-sm text-white placeholder:text-muted-foreground focus:border-gold/60 focus:outline-none focus:ring-2 focus:ring-gold/20`
- Note: use bg-near-black (not bg-midnight) for inputs on this page since the form container is bg-midnight

**Select styling**: Same as inputs but add `className="bg-near-black"` to option elements for dark background in dropdown.

**Submit button**: `w-full rounded-lg bg-gold px-6 py-3 text-sm font-semibold text-near-black transition-all hover:bg-gold-light hover:shadow-lg disabled:cursor-not-allowed disabled:opacity-60`

**Form container**: rounded-2xl border border-border/40 bg-midnight p-8 shadow-xl

Include "Apply Now" heading and "Fill out the form below and we'll be in touch." subtext at top of form.
Privacy policy link at bottom.
  </action>
  <verify>Run `npx tsc --noEmit` — no type errors. Run `npm run build` — careers page builds. Test form: fill all fields, click submit — form transitions through states correctly.</verify>
  <done>Application form collects all required fields (name, email, phone, license, experience, brokerage, resume, why statement). Form submits to /api/leads. Success and error states display correctly.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npm run build` succeeds
- /careers page renders hero, 6 benefit cards, requirements checklist, and application form
- Form validates required fields (firstName, lastName, email, phone, licenseNumber, yearsExperience)
- Form submit POSTs to /api/leads with type "agent-application"
- Success state shows confirmation message
- Resume upload field accepts PDF/DOC files and shows filename
- Page is linked from /agents team page "Join Our Team" CTA
</verification>

<success_criteria>
- AGENT-04 satisfied: "Join Our Team" page with application form collecting name, license info, experience, resume, and why statement
- Form follows same patterns as other Tauro forms (agent contact, property contact)
- Benefits section effectively markets Tauro to prospective agents
- All styling consistent with Tauro dark theme
</success_criteria>

<output>
After completion, create `.planning/phases/04-agent-pages/04-03-SUMMARY.md`
</output>
