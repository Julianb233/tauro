---
phase: 04-agent-pages
plan: 01
type: execute
wave: 1
depends_on: []
status: COMPLETED
files_modified:
  - src/data/agents.ts
  - src/components/AgentCard.tsx
  - src/app/(site)/agents/page.tsx
autonomous: true

must_haves:
  truths:
    - "Team page renders a responsive grid of agent cards"
    - "Each card shows agent photo, name, title, and contact info"
    - "Clicking a card links to /agents/[slug]"
    - "Agent data is typed with TypeScript interface and contains 6 sample agents"
  artifacts:
    - path: "src/data/agents.ts"
      provides: "Agent type definition and 6 seed agents with full data"
      exports: ["Agent", "agents", "getAgentBySlug", "getAgentById"]
    - path: "src/components/AgentCard.tsx"
      provides: "Reusable agent card component with photo, name, title, hover overlay"
    - path: "src/app/(site)/agents/page.tsx"
      provides: "Team page with hero, aggregate stats, agent grid, and join CTA"
  key_links:
    - from: "src/app/(site)/agents/page.tsx"
      to: "src/data/agents.ts"
      via: "import agents array"
      pattern: "import.*agents.*from.*@/data/agents"
    - from: "src/app/(site)/agents/page.tsx"
      to: "src/components/AgentCard.tsx"
      via: "renders AgentCard for each agent"
      pattern: "AgentCard"
    - from: "src/components/AgentCard.tsx"
      to: "/agents/[slug]"
      via: "Link href"
      pattern: "href=.*agents/"
---

<objective>
Create the agent data model with 6 seed agents and the team page showing a responsive card grid.

Purpose: Establish the agent data layer and team showcase -- the foundation for individual profiles and contact forms in subsequent plans.
Output: Agent TypeScript interface, 6 sample Philadelphia agents, AgentCard component, and /agents team page with hero section and grid.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/data/properties.ts (reference for data file conventions)
@src/components/PropertyCard.tsx (reference for card component patterns)
@src/app/(site)/layout.tsx (site layout with Navbar + Footer)
@src/app/globals.css (brand tokens: near-black, midnight, gold, gold-light, font-heading, font-label)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agent data model and seed data</name>
  <files>src/data/agents.ts</files>
  <action>
Create the Agent TypeScript interface and 6 sample Philadelphia-area agents.

**Agent interface fields:**
- id: string, slug: string
- firstName, lastName, fullName: string
- title: string (e.g. "Founding Partner & Lead Agent")
- email: string (format: firstname@tauro.realty), phone: string (format: (215) 555-XXXX)
- photo: string (Unsplash portrait URLs with ?w=600)
- bio: string (2-3 sentences, detailed), shortBio: string (1 sentence for card hover)
- specialties: string[] (3-5 items like "Luxury Homes", "Condos & Lofts")
- neighborhoods: string[] (4-5 Philadelphia neighborhoods each)
- stats: { propertiesSold: number, totalVolume: string, avgDaysOnMarket: number, yearsExperience: number }
- awards: { title: string, year: number, issuer: string }[]
- videoIntroUrl: string | null (YouTube embed URL or null)
- activeListingIds: string[] (cross-ref to properties data)
- soldListingIds: string[]
- social: { instagram?: string, linkedin?: string, facebook?: string }
- languages: string[]
- licenseNumber: string (format: RS-XXXXXX)

**6 agents to create:**
1. Julian Bradley - Founding Partner, 15yr exp, luxury/historic, Rittenhouse/Society Hill, 182 sold, $120M+
2. Sofia Martinez - Senior Buyer Specialist, 8yr, condos/first-time, Fishtown/Graduate Hospital, 97 sold, $65M+, bilingual English/Spanish
3. Marcus Thompson - New Development Specialist, 5yr, new construction/townhouses, Brewerytown/Point Breeze, 53 sold, $35M+
4. Ava Chen - Relocation Specialist, 6yr, corporate relocation/suburban, Chestnut Hill/Main Line, 67 sold, $42M+, bilingual English/Mandarin
5. Damon Reeves - Luxury Listing Specialist, 10yr, staging/marketing, Rittenhouse/Washington Square, 124 sold, $89M+
6. Priya Kapoor - Investment Specialist, 7yr, multi-family/1031 exchanges, Brewerytown/Francisville, 86 sold, $52M+, trilingual English/Hindi/Punjabi

Give 3 agents videoIntroUrl (YouTube embed placeholder) and 3 agents null.
Give first 2 agents activeListingIds referencing property IDs ["1","3","5"] and ["2","4","6"].

**Export helper functions:**
- getAgentBySlug(slug: string): Agent | undefined
- getAgentById(id: string): Agent | undefined
  </action>
  <verify>Run `npx tsc --noEmit` -- no type errors in agents.ts. Verify 6 agents exported with all required fields.</verify>
  <done>Agent interface is fully typed, 6 diverse agents exist with realistic Philadelphia data, helper functions exported.</done>
</task>

<task type="auto">
  <name>Task 2: AgentCard component and team page</name>
  <files>src/components/AgentCard.tsx, src/app/(site)/agents/page.tsx</files>
  <action>
**AgentCard component** (src/components/AgentCard.tsx):
- "use client" directive (needs hover interaction)
- Props: { agent: Agent }
- Wrap entire card in Next.js Link to /agents/[agent.slug]
- Portrait photo (aspect-[3/4]) using Next.js Image with object-cover
- Subtle grayscale filter (grayscale-[20%]) that removes on hover
- Bottom gradient overlay showing agent.fullName (font-heading, white) and agent.title (font-label, gold, uppercase tracking)
- Gold divider line (h-px w-12 bg-gold/60) between name and title
- Hover overlay slides up (translate-y-full -> translate-y-0 on group-hover) with backdrop-blur
- Hover overlay content: shortBio (line-clamp-3), stats row (propertiesSold + totalVolume), phone, email (onClick with stopPropagation to prevent Link navigation), "View Profile" CTA with ArrowRight icon
- Use transition-all duration-500 for smooth animations
- Card border: border-border/20, hover: border-gold/40 with subtle gold shadow

**Email click handler pattern** (avoids nested anchor in Link):
Use a span with role="button" and onClick that calls e.stopPropagation() + e.preventDefault() then window.location.href = mailto:...
Also add onKeyDown handler for Enter/Space for accessibility.

**Team page** (src/app/(site)/agents/page.tsx):
- Server component (no "use client")
- Export metadata: title "Our Agents", description about Tauro team
- Three sections:

1. **Hero section** (bg-near-black, pt-32 pb-20):
   - Gold accent line (h-px w-16 bg-gold/60) + "Our Team" label (uppercase, tracking-[0.25em], text-gold)
   - h1: "Philadelphia's Finest Real Estate Professionals" (font-heading, text-4xl sm:text-5xl lg:text-6xl)
   - Subtitle paragraph (text-white/60)
   - Aggregate stats row: total properties sold (sum from all agents), "$500M+" volume, average years experience

2. **Team Grid section** (bg-midnight, py-20):
   - "Meet the Team" heading with gold divider
   - Responsive grid: grid-cols-1 sm:grid-cols-2 lg:grid-cols-3, gap-6 lg:gap-8
   - Map agents array to AgentCard components

3. **Join CTA section** (bg-midnight, pb-20):
   - Centered card (rounded-2xl, border border-border/40, bg-near-black, p-12)
   - "Interested in Joining Tauro?" heading
   - Subtitle text
   - Gold CTA button linking to /join with ArrowRight icon, shimmer-gold class
  </action>
  <verify>Run `npm run build` or `npx next build` -- page compiles. Visit /agents in dev -- grid renders 6 cards with photos, names, titles. Hover shows overlay with bio and stats. Email click opens mailto without navigating away.</verify>
  <done>Team page shows responsive 3-column grid of 6 agent cards. Each card has photo, name, title, hover overlay with bio/stats/contact. Join CTA links to /join.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- /agents page renders with 6 agent cards in responsive grid
- Agent cards show photo, name, title; hover reveals bio, stats, contact
- Email link on card hover opens mailto without page navigation
- Aggregate stats in hero are computed from agent data
- Join CTA links to /join
</verification>

<success_criteria>
- Agent TypeScript interface covers all fields (bio, stats, awards, video, social, etc.)
- 6 diverse Philadelphia agents with realistic data
- Team page has hero with aggregate stats, responsive card grid, join CTA
- AgentCard component handles hover overlay with smooth animation
- All links work (/agents/[slug], /join, mailto, tel)
</success_criteria>

<output>
After completion, create `.planning/phases/04-agent-pages/04-01-SUMMARY.md`
</output>
