---
phase: 04-agent-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/agents.ts
  - src/app/(site)/agents/page.tsx
  - src/components/agent-card.tsx
autonomous: true

must_haves:
  truths:
    - "Agent data exports a typed array of 5-8 agent objects with full profile fields (bio, stats, specialties, awards, video URL, listing IDs)"
    - "Each agent has a unique slug suitable for URL routing"
    - "Team page renders a responsive grid of agent cards with photo, name, title, and contact link"
    - "Agent cards link to individual agent profile pages via /agents/[slug]"
    - "Team page includes a 'Join Our Team' CTA linking to /careers"
  artifacts:
    - path: "src/data/agents.ts"
      provides: "Agent interface, agents array, getAgentBySlug, getAgentById helpers"
      exports: ["Agent", "agents", "getAgentBySlug", "getAgentById"]
    - path: "src/app/(site)/agents/page.tsx"
      provides: "Team grid page listing all agents"
      min_lines: 40
    - path: "src/components/agent-card.tsx"
      provides: "Reusable agent card component"
      min_lines: 30
  key_links:
    - from: "src/app/(site)/agents/page.tsx"
      to: "src/data/agents.ts"
      via: "imports agents array"
      pattern: "import.*agents.*from.*@/data/agents"
    - from: "src/components/agent-card.tsx"
      to: "src/data/agents.ts"
      via: "imports Agent type"
      pattern: "import.*Agent.*from.*@/data/agents"
    - from: "src/components/agent-card.tsx"
      to: "/agents/[slug]"
      via: "Link href to agent profile"
      pattern: "href=.*agents/.*slug"
---

<objective>
Create the agent data layer and team page with agent cards.

Purpose: Establishes the data foundation for all agent pages and delivers the team grid (AGENT-01). Every subsequent plan in this phase depends on the Agent type and agents array defined here.
Output: Static agent data file (5-8 Philadelphia-based sample agents), a team page with responsive grid, and a reusable agent card component.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/data/properties.ts (follow this pattern for agent data — typed interface + exported array + helper functions)
@src/components/PropertyCard.tsx (follow this pattern for AgentCard — Next/Image, Link, Tailwind, dark theme)
@src/app/(site)/layout.tsx (site layout wrapper)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create agent data with TypeScript interface and 5-8 sample agents</name>
  <files>src/data/agents.ts</files>
  <action>
Create `src/data/agents.ts` following the same pattern as `src/data/properties.ts`.

Define an `Agent` interface with these fields:
- id: string
- slug: string (kebab-case of full name, used for URL routing)
- firstName: string
- lastName: string
- fullName: string
- title: string (e.g., "Founding Partner & Lead Agent", "Senior Agent & Buyer Specialist")
- email: string (format: firstname@tauro.realty)
- phone: string (format: (215) 555-XXXX)
- photo: string (use Unsplash portrait URLs with ?w=600)
- bio: string (2-4 sentence professional bio)
- shortBio: string (1 sentence for card display)
- specialties: string[] (3-5 items like "Luxury Homes", "First-Time Buyers", "Investment Properties")
- neighborhoods: string[] (3-5 Philadelphia neighborhoods)
- stats: { propertiesSold: number, totalVolume: string, avgDaysOnMarket: number, yearsExperience: number }
- awards: { title: string, year: number, issuer: string }[] (1-3 awards per agent, some agents can have 0)
- videoIntroUrl: string | null (YouTube embed URL for some agents, null for others)
- activeListingIds: string[] (reference IDs from properties.ts — assign 2-3 to lead agents, empty for newer agents)
- soldListingIds: string[] (fictional sold property IDs like "101", "102")
- social: { instagram?: string, linkedin?: string, facebook?: string }
- languages: string[]
- licenseNumber: string (format: RS-XXXXXX)

Create 5-8 agents with diverse Philadelphia specialties:
1. Julian Bradley — Founding Partner, luxury/historic homes, Rittenhouse/Society Hill, 15yr exp
2. Sofia Martinez — Senior Agent, buyer specialist, condos/townhouses, bilingual English/Spanish, 8yr exp
3. Marcus Thompson — New development specialist, architecture background, Brewerytown/Point Breeze, 5yr exp
4. Ava Chen — Relocation specialist, suburban estates, bilingual English/Mandarin, 6yr exp
5. (Add 1-4 more agents to reach 5-8 total — vary experience levels, specialties, neighborhoods)

Export helper functions:
- `getAgentBySlug(slug: string): Agent | undefined`
- `getAgentById(id: string): Agent | undefined`

Use realistic Philadelphia neighborhoods (Rittenhouse Square, Fishtown, Northern Liberties, Old City, Center City, Chestnut Hill, Manayunk, Brewerytown, Point Breeze, etc.).
  </action>
  <verify>Run `npx tsc --noEmit` — no type errors. Verify file exports Agent type, agents array, and both helper functions.</verify>
  <done>agents.ts exports a typed Agent interface, 5-8 sample agents with full profile data, and getAgentBySlug/getAgentById helpers.</done>
</task>

<task type="auto">
  <name>Task 2: Create agent card component and team grid page</name>
  <files>src/components/agent-card.tsx, src/app/(site)/agents/page.tsx</files>
  <action>
**AgentCard component** (`src/components/agent-card.tsx`):
- Accept `{ agent: Agent }` prop
- Render as a Link to `/agents/${agent.slug}`
- Show agent photo (Next/Image, aspect-[3/4], object-cover, hover scale-105 transition)
- Show fullName (font-heading, text-lg, font-bold, text-white)
- Show title (text-sm, uppercase tracking-wider, text-gold)
- Show shortBio (text-sm, text-muted-foreground, line-clamp-2)
- Show stats row: propertiesSold + totalVolume with gold accent
- Show contact info: phone + email (email opens mailto on click, prevent link navigation with stopPropagation)
- Show "View Profile" with ArrowRight icon on hover (opacity-0 -> opacity-100 transition)
- Use card-tilt class for 3D tilt micro-interaction
- Follow dark theme: bg-card, border-border, hover:border-gold/40

**Team page** (`src/app/(site)/agents/page.tsx`):
- Export metadata: title "Our Agents | Tauro", description about Philadelphia agents
- Hero section: gradient background (bg-near-black), "Our Team" label (gold, uppercase, tracking-[0.25em]), h1 "Meet the Tauro Agents", subtitle paragraph
- Grid section: responsive grid (sm:grid-cols-2 lg:grid-cols-4), map over agents array rendering AgentCard for each
- "Join Our Team" CTA section at bottom: rounded-2xl card with border, h2 heading, description, gold CTA button linking to /join (or /careers depending on route)
- Use max-w-7xl container with standard padding (px-4 sm:px-6 lg:px-8)
  </action>
  <verify>Run `npm run build` — page builds without errors. Visit /agents in dev server — grid renders all agents. Click an agent card — navigates to /agents/[slug].</verify>
  <done>Team page shows responsive grid of agent cards with photos, names, titles, stats, and contact info. Cards link to individual profiles. Join CTA is visible at bottom.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `npm run build` succeeds
- /agents page renders all agents in a responsive grid
- Each agent card shows photo, name, title, short bio, stats, contact info
- Clicking a card navigates to /agents/[slug]
- "Join Our Team" CTA is visible and links correctly
- Page looks correct on mobile (1 col) and desktop (4 col grid)
</verification>

<success_criteria>
- AGENT-01 satisfied: Team page with grid of agent cards (photo, name, title, contact)
- Agent data layer established for use by Plan 04-02 (profile pages)
- 5-8 agents with diverse specialties and Philadelphia neighborhoods
- All components follow existing design patterns (dark theme, gold accents, Tailwind)
</success_criteria>

<output>
After completion, create `.planning/phases/04-agent-pages/04-01-SUMMARY.md`
</output>
