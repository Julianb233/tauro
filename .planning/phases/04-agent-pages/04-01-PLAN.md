---
phase: 04-agent-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/agents.ts
autonomous: true

must_haves:
  truths:
    - "Agent data exports a typed array of agent objects with all profile fields"
    - "Each agent has a unique slug for URL routing"
    - "Agent data includes listings references that link to properties data"
  artifacts:
    - path: "src/data/agents.ts"
      provides: "Agent interface, agents array, helper functions"
      exports: ["Agent", "agents", "getAgentBySlug"]
  key_links:
    - from: "src/data/agents.ts"
      to: "src/data/properties.ts"
      via: "agent.listings references property IDs"
      pattern: "listings.*string\\[\\]"
---

<objective>
Create the static agent data file with TypeScript interfaces and agent profile data.

Purpose: All agent pages (team grid, individual profiles) depend on this data. It follows the same pattern as `src/data/properties.ts` — typed interface + exported array + helper functions.

Output: `src/data/agents.ts` with Agent type, 4 agent profiles, and lookup helpers.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/data/properties.ts — Follow exact same pattern: interface at top, exported array, helper functions at bottom.
@src/app/api/leads/route.ts — LeadPayload type has `agentPreference` field already.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Agent data file with types and static data</name>
  <files>src/data/agents.ts</files>
  <action>
Create `src/data/agents.ts` following the exact pattern of `src/data/properties.ts`.

**Agent interface** — define these fields:
```typescript
export interface Agent {
  id: string;
  slug: string;                    // URL-safe: "julian-bradley"
  firstName: string;
  lastName: string;
  fullName: string;                // "Julian Bradley"
  title: string;                   // "Founding Partner & Lead Agent"
  email: string;
  phone: string;
  photo: string;                   // Unsplash headshot URL ?w=600
  bio: string;                     // 2-3 paragraph bio
  shortBio: string;                // 1 sentence for card hover
  specialties: string[];           // ["Luxury Homes", "Waterfront", ...]
  neighborhoods: string[];         // ["Rittenhouse Square", "Society Hill", ...]
  stats: {
    propertiesSold: number;
    totalVolume: string;           // "$45M+"
    avgDaysOnMarket: number;
    yearsExperience: number;
  };
  awards: {
    title: string;
    year: number;
    issuer: string;                // "Philadelphia Association of Realtors"
  }[];
  videoIntroUrl: string | null;    // YouTube embed URL or null
  activeListingIds: string[];      // References to properties.ts IDs
  soldListingIds: string[];        // References to sold property IDs
  social: {
    instagram?: string;
    linkedin?: string;
    facebook?: string;
  };
  languages: string[];             // ["English", "Spanish"]
  licenseNumber: string;           // "RS-XXXXXX"
}
```

**Create 4 agents** using Philadelphia-appropriate data:

1. **Julian Bradley** (slug: "julian-bradley") — Founding Partner & Lead Agent. Specializes in luxury homes and historic properties. 15 years experience, $120M+ volume, 180+ properties sold. Photo: `https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?w=600`. Has YouTube video intro: `https://www.youtube.com/embed/dQw4w9WgXcQ`. activeListingIds: ["1", "3", "5"] (matches properties.ts). 3 awards. Languages: English.

2. **Sofia Martinez** (slug: "sofia-martinez") — Senior Agent & Buyer Specialist. Specializes in condos, first-time buyers, investment properties. 8 years experience, $65M+ volume, 95+ properties sold. Photo: `https://images.unsplash.com/photo-1494790108377-be9c29b29330?w=600`. Has YouTube video intro. activeListingIds: ["2", "4", "6"]. 2 awards. Languages: English, Spanish.

3. **Marcus Thompson** (slug: "marcus-thompson") — Agent & New Development Specialist. Specializes in new construction and townhouses. 5 years experience, $35M+ volume, 50+ properties sold. Photo: `https://images.unsplash.com/photo-1472099645785-5658abf4ff4e?w=600`. videoIntroUrl: null. activeListingIds: []. 1 award. Languages: English.

4. **Ava Chen** (slug: "ava-chen") — Agent & Relocation Specialist. Specializes in relocations and suburban estates. 6 years experience, $42M+ volume, 65+ properties sold. Photo: `https://images.unsplash.com/photo-1438761681033-6461ffad8d80?w=600`. videoIntroUrl: null. activeListingIds: []. 2 awards. Languages: English, Mandarin.

Each agent should have:
- A 2-3 sentence `bio` that sounds professional and Philadelphia-specific (mention neighborhoods like Rittenhouse Square, Society Hill, Fishtown, Center City, Main Line, etc.)
- A `shortBio` of 1 sentence for card display
- 3-5 specialties
- 3-5 neighborhoods
- Realistic `stats`
- Philadelphia-appropriate `awards` (e.g., "Top Producer - Philadelphia Association of Realtors", "Five Star Professional Award")
- A `licenseNumber` like "RS-XXXXXX" (PA format)
- Social links (instagram, linkedin)

**Helper functions** (follow `formatPrice` pattern):
```typescript
export function getAgentBySlug(slug: string): Agent | undefined {
  return agents.find((a) => a.slug === slug);
}

export function getAgentById(id: string): Agent | undefined {
  return agents.find((a) => a.id === id);
}
```
  </action>
  <verify>
Run `npx tsc --noEmit` to confirm types compile. Run `node -e "const a = require('./src/data/agents'); console.log(a.agents.length)"` — but since this is TS with ESM, instead verify by checking `npx tsc --noEmit` passes with zero errors.
  </verify>
  <done>
`src/data/agents.ts` exists with Agent interface, 4 complete agent objects, `getAgentBySlug()` and `getAgentById()` helpers. TypeScript compiles without errors.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- File exports: Agent type, agents array, getAgentBySlug, getAgentById
- Each agent has all required fields populated (no empty strings, no missing arrays)
- activeListingIds reference valid IDs from properties.ts
</verification>

<success_criteria>
Agent data file exists and compiles. All 4 agents have complete profiles. Helper functions exported.
</success_criteria>

<output>
After completion, create `.planning/phases/04-agent-pages/04-01-SUMMARY.md`
</output>
