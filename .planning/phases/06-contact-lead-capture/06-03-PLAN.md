---
phase: 06-contact-lead-capture
plan: 03
type: execute
wave: 1
depends_on: []
status: COMPLETED
files_modified:
  - src/app/(site)/sell/page.tsx
  - src/components/seller-inquiry-form.tsx
  - src/app/api/leads/route.ts
autonomous: true

must_haves:
  truths:
    - "User can access /sell page and see 'List Your Home with Tauro' content"
    - "Seller inquiry form collects: name, email, phone, home address, beds, baths, sqft, timeline, reason for selling"
    - "Form submits to /api/leads with type 'seller'"
    - "API route handles all lead types (contact, showing, seller) and forwards to GoHighLevel webhook"
    - "GHL webhook integration gracefully degrades when GHL_WEBHOOK_URL env var is not set"
    - "API validates required fields and email format, returns proper error responses"
  artifacts:
    - path: "src/app/(site)/sell/page.tsx"
      provides: "Seller inquiry page - List Your Home with Tauro"
      exports: ["default"]
    - path: "src/components/seller-inquiry-form.tsx"
      provides: "Seller inquiry form with home details and valuation request"
      exports: ["SellerInquiryForm"]
    - path: "src/app/api/leads/route.ts"
      provides: "Unified lead API route with GHL webhook integration"
      exports: ["POST", "LeadPayload"]
  key_links:
    - from: "src/components/seller-inquiry-form.tsx"
      to: "/api/leads"
      via: "fetch POST with LeadPayload type 'seller'"
      pattern: "fetch.*api/leads"
    - from: "src/app/api/leads/route.ts"
      to: "GoHighLevel"
      via: "POST to GHL_WEBHOOK_URL env var"
      pattern: "process\\.env\\.GHL_WEBHOOK_URL"
    - from: "src/app/(site)/sell/page.tsx"
      to: "src/components/seller-inquiry-form.tsx"
      via: "renders SellerInquiryForm component"
      pattern: "SellerInquiryForm"
---

<objective>
Create the seller inquiry page ("List Your Home with Tauro"), the seller inquiry form component, and ensure the leads API route handles all form types with GoHighLevel webhook integration.

Purpose: The seller inquiry page captures high-value seller leads. Home sellers considering listing their property fill out a form with their property details and contact info. This data flows to GoHighLevel CRM via webhook for agent follow-up. The API route must handle contact, showing, and seller lead types consistently. Requirements: LEAD-03, LEAD-06.

Output: `/sell` page with seller form, and verified `/api/leads` route with GHL integration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/app/api/leads/route.ts (existing LeadPayload interface and POST handler with GHL webhook)
@src/app/(site)/contact/page.tsx (form pattern reference)
@src/app/(site)/book-tour/page.tsx (form pattern reference)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SellerInquiryForm component</name>
  <files>src/components/seller-inquiry-form.tsx</files>
  <action>
Create `src/components/seller-inquiry-form.tsx` as a CLIENT component ("use client").

Import LeadPayload from `@/app/api/leads/route`.

**State:**
- `formData`: `{ firstName, lastName, email, phone, homeAddress, beds, baths, sqft, timeline, reason, message }` (all strings)
- `formState`: `"idle" | "submitting" | "success" | "error"`

**Form fields organized in sections:**

**Section 1: "Your Information"**
- firstName + lastName (side by side on sm+)
- email (type="email")
- phone (type="tel")

**Section 2: "Property Details"**
- homeAddress (text input, label: "Home Address", placeholder: "123 Main St, Philadelphia, PA")
- beds (select dropdown: "1", "2", "3", "4", "5", "6+")
- baths (select dropdown: "1", "1.5", "2", "2.5", "3", "3.5", "4+")
- sqft (text input, placeholder: "e.g., 2,000")
- beds + baths side by side on sm+

**Section 3: "Selling Details"**
- timeline (select dropdown: "As soon as possible", "1-3 months", "3-6 months", "6-12 months", "Just exploring")
- reason (select dropdown: "Relocating", "Upgrading", "Downsizing", "Investment property", "Estate sale", "Other")
- message (textarea, label: "Anything else we should know?", 4 rows)

**Validation:** firstName, lastName, email, phone, homeAddress required. Email format validation.

**Submit:** POST to `/api/leads` with:
```typescript
{
  type: "seller" as const,
  firstName, lastName, email, phone, message,
  homeAddress, beds, baths, sqft, timeline, reason,
}
```

**Input styling:** Same as ContactForm and TourBookingForm: `bg-near-black border border-border rounded-lg px-4 py-3 text-foreground focus:border-gold focus:ring-1 focus:ring-gold`. Section headings: `font-heading text-lg font-bold text-foreground` with `border-b border-border/40 pb-2 mb-4`.

**Success state:** CheckCircle icon, "Inquiry Received!" heading, "A Tauro agent will review your property details and contact you within 24 hours with a personalized market analysis." text, "Submit Another" button.

**Error state:** AlertCircle icon, "Something went wrong" text, "Try Again" button.
  </action>
  <verify>
`npx tsc --noEmit` passes. Component exports SellerInquiryForm.
  </verify>
  <done>
- seller-inquiry-form.tsx exports SellerInquiryForm as "use client" component
- Form has 3 sections: visitor info, property details, selling details
- Collects: name, contact, address, beds/baths/sqft, timeline, reason, message
- Validates required fields + email format
- Submits to /api/leads with type "seller"
- Success/error states with appropriate messaging
  </done>
</task>

<task type="auto">
  <name>Task 2: Create seller inquiry page and verify API route</name>
  <files>src/app/(site)/sell/page.tsx, src/app/(site)/sell/layout.tsx, src/app/api/leads/route.ts</files>
  <action>
**layout.tsx** for metadata:
```typescript
import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Sell Your Home",
  description: "List your home with Tauro Real Estate. Get a free market analysis, expert pricing strategy, and premium marketing for your Philadelphia property.",
};
export default function SellLayout({ children }: { children: React.ReactNode }) {
  return children;
}
```

**page.tsx** ("use client"):

1. **Hero section**: Dark bg, `pt-32 pb-16`. Gold label "Sell with Confidence". H1 "List Your Home with Tauro" with `font-heading text-4xl sm:text-5xl font-bold`. Subtext: "Get the most from your Philadelphia property with Tauro's premium marketing, expert pricing, and dedicated agent support."

2. **Value proposition section**: 4 feature cards in responsive grid (`sm:grid-cols-2 lg:grid-cols-4`). Each card: icon in gold/10 circle, heading, description.
   - Home icon: "Expert Pricing" / "Data-driven pricing strategy to maximize your return"
   - TrendingUp icon: "Market Analysis" / "Comprehensive neighborhood and market insights"
   - Shield icon: "Premium Marketing" / "Professional photography, staging, and targeted campaigns"
   - Star icon: "Dedicated Agent" / "Your personal Tauro agent from listing to close"
   Each card: `bg-card border border-border/40 rounded-xl p-6`

3. **Form section**: `bg-midnight` section. Centered `max-w-2xl` container with `bg-card border border-border/40 rounded-xl p-8`. Heading "Request Your Free Home Valuation" above form. Render `<SellerInquiryForm />`.

4. **Process section**: "How It Works" with 3 numbered steps:
   - Step 1: "Submit Your Details" - "Tell us about your home and timeline"
   - Step 2: "Receive Your Analysis" - "Get a personalized market analysis within 24 hours"
   - Step 3: "Meet Your Agent" - "Connect with a Tauro specialist for your neighborhood"
   Styled as horizontal (lg) or vertical (mobile) timeline with gold number badges.

5. **CTA section**: "Ready to Get Started?" with link to scroll to form section or contact page.

Icons from lucide-react: Home, TrendingUp, Shield, Star.

**API route verification:** Review `src/app/api/leads/route.ts` to ensure it:
- Exports `LeadPayload` interface with seller fields: `homeAddress`, `beds`, `baths`, `sqft`, `timeline`, `reason`
- `buildGhlContact` maps seller fields to GHL custom fields
- POST handler validates required fields, basic email check
- Forwards to `GHL_WEBHOOK_URL` env var when set
- Returns success with message when GHL not configured (dev mode)
- Returns 422 for validation errors, 502 for GHL failures, 500 for unexpected errors

If the API route already handles all of this (it does based on the existing implementation), do NOT modify it. Only update if any seller-specific fields are missing from the LeadPayload interface.
  </action>
  <verify>
1. `npx tsc --noEmit` passes
2. `npm run build` succeeds
3. Visit /sell -- shows hero, value props, seller form, process steps
4. Submit form with valid data -- posts to /api/leads with type "seller"
5. API returns 200 with success message (GHL not configured in dev)
6. Verify API handles missing fields: `curl -X POST http://localhost:3000/api/leads -H "Content-Type: application/json" -d '{}'` returns 422
  </verify>
  <done>
- /sell page renders with hero, value proposition cards, seller inquiry form, process steps
- SellerInquiryForm collects all property and contact details
- Form posts to /api/leads with type "seller"
- API route handles contact, showing, and seller types with GHL webhook
- GHL integration gracefully degrades when not configured
- Build passes
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. `npm run build` succeeds
3. /sell page loads with all sections: hero, value props, form, process steps
4. Form validates required fields (name, email, phone, address)
5. Valid submission hits /api/leads with type "seller" and shows success
6. API handles all three lead types: contact, showing, seller
7. curl test: POST to /api/leads with empty body returns 422
8. curl test: POST with valid seller payload returns 200
</verification>

<success_criteria>
- /sell page with "List Your Home with Tauro" content and seller inquiry form
- Form collects: name, contact info, address, beds/baths/sqft, timeline, reason, message
- Submissions POST to /api/leads with type "seller"
- API route handles all lead types with GHL webhook (degrades gracefully without config)
- API validates inputs and returns proper error codes
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/06-contact-lead-capture/06-03-SUMMARY.md`
</output>
