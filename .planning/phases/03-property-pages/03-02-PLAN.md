---
phase: 03-property-pages
plan: 02
type: execute
wave: 2
status: COMPLETED
depends_on: ["03-01"]
files_modified:
  - src/components/PropertyCard.tsx
  - src/app/properties/layout.tsx
  - src/app/properties/page.tsx
autonomous: true

must_haves:
  truths:
    - "Property listings page at /properties displays a responsive grid: 1 column on mobile, 2 on tablet (sm), 3 on desktop (lg)"
    - "Each PropertyCard shows the hero image, price (formatted), beds/baths/sqft stats, full address, and status badge"
    - "Cards link to /properties/[slug] detail pages"
    - "Status badges use color coding: Active=emerald, New=gold, Open House=blue, Pending=orange"
    - "Photo count badge appears on card image"
    - "Page header shows listing count (e.g. '16 listings available')"
    - "Empty state message displays when no properties match (for future filter integration)"
  artifacts:
    - path: "src/components/PropertyCard.tsx"
      provides: "Reusable property card component"
      exports: ["default"]
      min_lines: 40
    - path: "src/app/properties/page.tsx"
      provides: "Property listing page with grid view"
      exports: ["default"]
      min_lines: 30
    - path: "src/app/properties/layout.tsx"
      provides: "Properties section layout with metadata"
      contains: "Properties for Sale"
  key_links:
    - from: "src/app/properties/page.tsx"
      to: "src/data/properties.ts"
      via: "import { properties }"
      pattern: "import.*properties.*from.*data/properties"
    - from: "src/app/properties/page.tsx"
      to: "src/components/PropertyCard.tsx"
      via: "component render in grid"
      pattern: "<PropertyCard"
    - from: "src/components/PropertyCard.tsx"
      to: "/properties/[slug]"
      via: "Next.js Link component"
      pattern: "href=.*properties/"
---

<objective>
Build the property listing page with responsive grid and PropertyCard component.

Purpose: This is the primary browsing experience -- users land on /properties and see a beautiful grid of luxury listings. The card and page must work standalone before filters and map view are layered on.

Output: PropertyCard component, properties listing page with responsive grid, and layout with metadata.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/data/properties.ts (Property type and data from 03-01)
@src/lib/utils.ts (cn utility for className merging)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PropertyCard component</name>
  <files>src/components/PropertyCard.tsx</files>
  <action>
Create `src/components/PropertyCard.tsx` as a server component (no "use client"):

- Accept `{ property: Property }` prop
- Wrap entire card in `<Link href={/properties/${property.slug}}>` from next/link
- Card structure:
  - Image container with `aspect-[4/3]` using Next.js `<Image>` with `fill` and `object-cover`
  - Status badge overlay (top-left): colored by status (Active=bg-emerald-600, New=bg-gold text-near-black, Open House=bg-blue-600, Pending=bg-orange-500)
  - Photo count badge overlay (bottom-right): e.g. "6 photos" with bg-black/60 backdrop-blur
  - Image hover: `group-hover:scale-105` with `transition-transform duration-500`
  - Below image: price (gold, font-heading, bold), beds/baths/sqft row with pipe separators, address, city/state/zip
  - If openHouse exists, show "Open House: {date}" in gold text
- Card border: `border border-border bg-card rounded-xl` with `hover:border-gold/40 hover:shadow-lg`
- Use responsive image sizes: `(max-width: 768px) 100vw, (max-width: 1200px) 50vw, 33vw`
- Import `formatPrice` from data/properties for price display
  </action>
  <verify>Run `npx tsc --noEmit` to confirm no type errors.</verify>
  <done>PropertyCard renders image, price, stats, address, status badge, and links to detail page.</done>
</task>

<task type="auto">
  <name>Task 2: Create properties listing page with grid and layout</name>
  <files>src/app/properties/layout.tsx, src/app/properties/page.tsx</files>
  <action>
1. **Layout** (`src/app/properties/layout.tsx`):
   - Export metadata: title "Properties for Sale", description about browsing luxury Philadelphia properties
   - Render children pass-through

2. **Page** (`src/app/properties/page.tsx`):
   - Mark as `"use client"` (will need client interactivity for filters/map in next plans)
   - Wrap in `<Suspense>` for useSearchParams compatibility
   - Page header section with `<h1>Properties</h1>` and listing count text
   - Grid/map view toggle buttons (LayoutGrid and Map icons from lucide-react) -- map view can show a placeholder message for now ("Map view coming soon") since PropertyMap is built in plan 05
   - Responsive grid: `grid gap-6 sm:grid-cols-2 lg:grid-cols-3`
   - Render `<PropertyCard>` for each property
   - Empty state: centered message "No properties match your filters." with a clear filters button (wire to actual filters in plan 03)
   - Add `pt-16` to account for fixed navbar
  </action>
  <verify>
Run `npm run build` to verify page compiles and renders. Check that /properties route exists in build output.
  </verify>
  <done>
/properties page renders a responsive grid of property cards with header showing count, and layout provides SEO metadata.
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes without errors
- /properties route appears in build output
- PropertyCard accepts Property type and renders all required fields
- Grid uses responsive columns (1/2/3)
</verification>

<success_criteria>
Property listing page at /properties displays all properties in a responsive grid with fully styled cards showing image, price, stats, address, and status badges.
</success_criteria>

<output>
After completion, create `.planning/phases/03-property-pages/03-02-SUMMARY.md`
</output>
