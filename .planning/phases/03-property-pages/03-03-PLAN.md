---
status: COMPLETED
phase: 03-property-pages
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/PropertyFilters.tsx
  - src/app/properties/page.tsx
autonomous: true

must_haves:
  truths:
    - "Filter bar provides dropdowns for: min price, max price, beds, baths, min sqft, max sqft, area (neighborhood), property type, and status"
    - "Sort dropdown offers: Price High to Low (default), Price Low to High, Newest, Largest"
    - "Active filter count badge shows on mobile toggle button"
    - "Clear All button appears when any non-default filter is active"
    - "Filter bar collapses on mobile behind a toggle button, always visible on desktop (lg)"
    - "Area dropdown is populated from neighborhoods data"
    - "Selecting a filter updates the URL search params and filters the property grid"
  artifacts:
    - path: "src/components/PropertyFilters.tsx"
      provides: "Filter bar component with all filter controls"
      exports: ["default", "FilterState", "defaultFilters"]
      min_lines: 150
  key_links:
    - from: "src/components/PropertyFilters.tsx"
      to: "src/data/neighborhoods.ts"
      via: "import { neighborhoods }"
      pattern: "import.*neighborhoods.*from.*data/neighborhoods"
    - from: "src/app/properties/page.tsx"
      to: "src/components/PropertyFilters.tsx"
      via: "component render with filter state"
      pattern: "<PropertyFilters"
---

<objective>
Build the advanced property filter bar and integrate client-side filtering into the listings page.

Purpose: Filters are the primary way users narrow listings by price, size, neighborhood, and type. URL-driven filter state ensures shareable, bookmarkable filtered views.

Output: `PropertyFilters.tsx` component and updated listings page with full filtering logic.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@src/data/neighborhoods.ts (neighborhood names for area dropdown)
@src/data/properties.ts (Property type for propertyType union values)
@src/app/properties/page.tsx (listing page to integrate filters into)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PropertyFilters component</name>
  <files>src/components/PropertyFilters.tsx</files>
  <action>
Create `src/components/PropertyFilters.tsx` as a `"use client"` component:

1. **Export FilterState interface**:
   ```typescript
   export interface FilterState {
     priceMin: string;
     priceMax: string;
     beds: string;
     baths: string;
     sqftMin: string;
     sqftMax: string;
     area: string;
     propertyType: string;
     status: string;
     sort: string;
   }
   ```

2. **Export defaultFilters constant** with all empty strings except `sort: "price-desc"`

3. **Props**: `{ filters: FilterState; onChange: (key: keyof FilterState, value: string) => void; onClear: () => void }`

4. **Mobile behavior**:
   - Toggle button with SlidersHorizontal icon from lucide-react
   - Shows active filter count badge when filters are applied
   - Sort dropdown visible on mobile header row
   - Filter grid hidden by default, shown on toggle

5. **Desktop behavior (lg+)**:
   - All filters visible in a horizontal flex row
   - Sort dropdown at the end with "Clear All" button

6. **Filter controls** (all `<select>` dropdowns):
   - Min Price: No Min, $500K, $1M, $2M, $3M, $5M
   - Max Price: No Max, $1M, $2M, $3M, $5M, $10M
   - Beds: Any, 1+, 2+, 3+, 4+, 5+
   - Baths: Any, 1+, 2+, 3+, 4+
   - Min Sqft: No Min, 1000, 1500, 2000, 3000, 4000, 5000
   - Max Sqft: No Max, 2000, 3000, 4000, 5000, 7000, 10000
   - Area: All Areas + map over `neighborhoods` array using `n.propertyFilter` as value and `n.name` as label
   - Property Type: All Types, Single Family, Condo, Townhouse, Multi-Family, Land
   - Status: All, Active, New, Open House, Pending

7. **Styling**: Dark theme -- `border-border bg-card` selects, `focus:border-gold focus:ring-gold`, labels in `text-muted-foreground`

8. **Active count calculation**: Count entries where value differs from defaultFilters, excluding "sort"
  </action>
  <verify>Run `npx tsc --noEmit` to confirm types are correct and component compiles.</verify>
  <done>
PropertyFilters component exports FilterState type and defaultFilters, renders all 9 filter dropdowns plus sort, with responsive mobile/desktop layout and active count badge.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate filters into properties listing page</name>
  <files>src/app/properties/page.tsx</files>
  <action>
Update `src/app/properties/page.tsx` to wire in PropertyFilters with URL-driven state:

1. **Import** PropertyFilters, FilterState, and defaultFilters from `@/components/PropertyFilters`

2. **Read filter state from URL search params** using `useSearchParams()`:
   - Map each FilterState key to a search param (use "type" param for propertyType)
   - Default to empty strings, sort defaults to "price-desc"
   - Wrap in `useMemo` keyed on searchParams

3. **updateFilter callback** (useCallback):
   - Create new URLSearchParams from current
   - Set or delete the param based on value
   - Use `router.replace()` with `{ scroll: false }` to update URL without navigation

4. **clearFilters callback**: `router.replace("/properties", { scroll: false })`

5. **Client-side filtering logic** (useMemo):
   - Filter by priceMin/priceMax (numeric comparison)
   - Filter by beds/baths (>= comparison)
   - Filter by sqftMin/sqftMax (numeric comparison)
   - Filter by area (exact match on `property.neighborhood`)
   - Filter by propertyType (exact match)
   - Filter by status (exact match)
   - Sort by: price-desc, price-asc, newest (yearBuilt desc), sqft (sqft desc)

6. **Render** `<PropertyFilters>` between header and grid, passing filters/onChange/onClear

7. **Update listing count** to show `filtered.length` instead of total
  </action>
  <verify>Run `npm run build` to verify page compiles. Check that URL params are read from searchParams.</verify>
  <done>
Filters are wired into the listing page with URL-driven state. Selecting a filter updates the URL, filters the property list, and updates the count. Clear All resets everything.
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes
- FilterState and defaultFilters are exported from PropertyFilters
- All 9 filter types are rendered as select dropdowns
- Filtering logic covers all fields with correct comparison operators
- URL params sync bidirectionally with filter state
</verification>

<success_criteria>
Users can filter properties by price range, beds, baths, sqft range, neighborhood area, property type, and status. Filters persist in URL params and can be cleared. Sort works across all options.
</success_criteria>

<output>
After completion, create `.planning/phases/03-property-pages/03-03-SUMMARY.md`
</output>
