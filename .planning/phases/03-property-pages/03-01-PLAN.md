---
phase: 03-property-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/properties.ts
autonomous: true

must_haves:
  truths:
    - "Property interface exports all required fields: id, slug, address, city, state, zip, neighborhood, price, beds, baths, sqft, lotSqft, yearBuilt, status, propertyType, images, description, features, agent, lat, lng, openHouse, videoUrl, virtualTourUrl"
    - "At least 8 curated Philadelphia listings exist with realistic addresses, prices, coordinates, and neighborhood names"
    - "Each listing has 3-6 Unsplash images, a detailed description, and features split into interior/exterior/community arrays"
    - "Helper functions formatPrice, formatPriceFull, and getPropertyBySlug are exported and work correctly"
    - "Property status is typed as union: Active | Pending | Open House | New"
    - "Property type is typed as union: Single Family | Condo | Townhouse | Multi-Family | Land"
  artifacts:
    - path: "src/data/properties.ts"
      provides: "Property type definition, 8-16 curated Philadelphia listings, helper functions"
      exports: ["Property", "properties", "formatPrice", "formatPriceFull", "getPropertyBySlug"]
  key_links:
    - from: "src/data/properties.ts"
      to: "Unsplash CDN"
      via: "image URLs with ?w=1200 sizing"
      pattern: "images\\.unsplash\\.com"
---

<objective>
Create the static property data model and seed data for all property pages.

Purpose: This is the foundation that every other property plan depends on -- the TypeScript interface defines the shape, and the seed data populates every view (grid, map, detail, filters).

Output: `src/data/properties.ts` with Property interface, 8-16 curated Philadelphia listings spanning multiple neighborhoods and property types, and helper functions.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/data/neighborhoods.ts (neighborhood names and propertyFilter values for area matching)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Property interface and seed data</name>
  <files>src/data/properties.ts</files>
  <action>
Create `src/data/properties.ts` with:

1. **Property interface** with these exact fields:
   - `id: string` -- unique identifier
   - `slug: string` -- URL-safe slug (e.g. "1820-rittenhouse-sq-philadelphia")
   - `address: string` -- street address
   - `city: string` -- always "Philadelphia"
   - `state: string` -- always "PA"
   - `zip: string` -- valid Philly zip codes
   - `neighborhood: string` -- must match values from `neighborhoods.ts` propertyFilter field
   - `price: number` -- in dollars (e.g. 3250000)
   - `beds: number`, `baths: number`, `sqft: number`, `lotSqft: number` (0 for condos)
   - `yearBuilt: number`
   - `status: "Active" | "Pending" | "Open House" | "New"`
   - `propertyType: "Single Family" | "Condo" | "Townhouse" | "Multi-Family" | "Land"`
   - `images: string[]` -- 3-6 Unsplash URLs with `?w=1200` suffix
   - `description: string` -- 2-3 sentence luxury marketing copy
   - `features: { interior: string[]; exterior: string[]; community: string[] }` -- 4-6 items each
   - `agent: { name: string; phone: string; email: string; photo: string }` -- use 2-3 fictional agents
   - `lat: number`, `lng: number` -- real Philadelphia coordinates for that neighborhood
   - `openHouse?: string` -- e.g. "Sat, Mar 22 - 1:00 PM - 4:00 PM"
   - `videoUrl?: string` -- YouTube embed URL (optional, on 3-4 listings)
   - `virtualTourUrl?: string` -- Matterport URL (optional, on 3-4 listings)

2. **Seed data**: 8-16 curated listings covering:
   - Neighborhoods: Rittenhouse, Old City, Center City, Fishtown, South Philly, Northern Liberties, University City, Manayunk, Chestnut Hill, Brewerytown, Point Breeze, Kensington, West Philly
   - Price range: $375K to $6.8M
   - Mix of all property types and statuses
   - Use real Unsplash luxury home/interior photos (vary URLs across listings)

3. **Helper functions**:
   - `formatPrice(price: number): string` -- returns "$3.25M" or "$895,000" format
   - `formatPriceFull(price: number): string` -- returns "$3,250,000" format
   - `getPropertyBySlug(slug: string): Property | undefined`
  </action>
  <verify>
Run `npx tsc --noEmit` to verify no type errors. Verify the file exports Property type and properties array by checking the import resolves.
  </verify>
  <done>
src/data/properties.ts exists with a fully typed Property interface, 8+ Philadelphia listings with realistic data, lat/lng coordinates, and all helper functions. TypeScript compiles without errors.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `properties.length >= 8`
- Every property has all required fields (no undefined except optional fields)
- Neighborhood values match those in neighborhoods.ts
- Lat/lng coordinates are in Philadelphia area (39.9-40.1, -75.1 to -75.25)
</verification>

<success_criteria>
Property data model and seed data are complete, typed, and ready for consumption by grid, map, filter, and detail components.
</success_criteria>

<output>
After completion, create `.planning/phases/03-property-pages/03-01-SUMMARY.md`
</output>
