---
phase: 03-property-pages
plan: 05
type: execute
wave: 2
depends_on: [03-04]
files_modified: [src/components/PropertyFilters.tsx, src/app/properties/page.tsx]
autonomous: true

must_haves:
  truths:
    - "Area filter dropdown shows Philadelphia neighborhood names (Center City, Rittenhouse, Fishtown, etc.) not San Diego names"
    - "Selecting an area filter correctly shows only properties in that neighborhood"
    - "Clearing filters resets area to All Areas and shows all 16+ properties"
    - "URL params sync correctly: ?area=Fishtown shows only Fishtown properties"
  artifacts:
    - path: "src/components/PropertyFilters.tsx"
      provides: "Area dropdown with Philly neighborhood options"
      contains: "Center City"
    - path: "src/app/properties/page.tsx"
      provides: "Filter logic using property.neighborhood field"
      contains: "p.neighborhood"
  key_links:
    - from: "src/app/properties/page.tsx"
      to: "src/data/properties.ts"
      via: "filter on property.neighborhood instead of property.city"
      pattern: "p\\.neighborhood"
    - from: "src/components/PropertyFilters.tsx"
      to: "src/data/neighborhoods.ts"
      via: "dynamically generates area options from neighborhoods data"
      pattern: "neighborhoods.*map|propertyFilter"
---

<objective>
Fix the area filter to use Philadelphia neighborhoods instead of San Diego areas, and update filter logic to use the new `neighborhood` field on properties.

Purpose: The area filter currently shows San Diego locations (Mission Beach, La Jolla, etc.) and filters on `city` which is always "Philadelphia" -- making it useless. This plan connects the filter to real neighborhood data.

Output: Working area filter with Philly neighborhoods that correctly filters the property grid and map views.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@src/data/properties.ts (has neighborhood field from 03-04)
@src/data/neighborhoods.ts (provides neighborhood names)
@src/components/PropertyFilters.tsx
@src/app/properties/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update PropertyFilters area dropdown with Philly neighborhoods</name>
  <files>src/components/PropertyFilters.tsx</files>
  <action>
  Replace the hardcoded San Diego area options with Philadelphia neighborhood options derived from the neighborhoods data.

  1. Import `neighborhoods` from `@/data/neighborhoods` at the top of the file.

  2. Replace the Area select options (currently Mission Beach, La Jolla, San Diego, Carlsbad) with dynamically generated options from the neighborhoods array:
     ```
     <option value="">All Areas</option>
     {neighborhoods.map((n) => (
       <option key={n.id} value={n.propertyFilter}>{n.name}</option>
     ))}
     ```

  This ensures the filter values match the `neighborhood` field on properties (both use `propertyFilter` values like "Center City", "Rittenhouse", "Fishtown", etc.).
  </action>
  <verify>
  `grep "Mission Beach\|La Jolla\|San Diego\|Carlsbad" src/components/PropertyFilters.tsx` should return zero results.
  `grep "neighborhoods" src/components/PropertyFilters.tsx` should find the import.
  </verify>
  <done>Area dropdown shows 15 Philadelphia neighborhoods dynamically from neighborhoods data, no San Diego references remain.</done>
</task>

<task type="auto">
  <name>Task 2: Fix area filter logic to use neighborhood field</name>
  <files>src/app/properties/page.tsx</files>
  <action>
  In the `filtered` useMemo computation, change the area filter from:
  ```
  if (filters.area) {
    result = result.filter((p) => p.city === filters.area);
  }
  ```
  to:
  ```
  if (filters.area) {
    result = result.filter((p) => p.neighborhood === filters.area);
  }
  ```

  This is a one-line change. The `filters.area` value from URL params now matches the `neighborhood` field on each property (e.g., "Fishtown", "Rittenhouse", "Center City").
  </action>
  <verify>
  `grep "p.neighborhood" src/app/properties/page.tsx` should find the filter line.
  `grep "p.city === filters.area" src/app/properties/page.tsx` should return zero results.
  `npx tsc --noEmit` passes.
  </verify>
  <done>Area filter correctly filters properties by neighborhood field. URL param ?area=Fishtown shows only Fishtown properties.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npm run build` succeeds
- No San Diego references in PropertyFilters.tsx
- Area filter options match neighborhood.propertyFilter values from neighborhoods.ts
- Filter logic uses `p.neighborhood` not `p.city`
</verification>

<success_criteria>
Area filter dropdown shows 15 Philadelphia neighborhoods and correctly filters the property grid/map to show only matching properties. URL-synced: ?area=Fishtown works correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/03-property-pages/03-05-SUMMARY.md`
</output>
