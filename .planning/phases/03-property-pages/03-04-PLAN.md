---
phase: 03-property-pages
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [src/data/properties.ts]
autonomous: true

must_haves:
  truths:
    - "At least 16 curated properties exist with realistic Philadelphia addresses, prices, and neighborhood data"
    - "Properties span at least 10 different Philly neighborhoods matching the neighborhoods.ts data"
    - "Property interface includes a neighborhood field matching the propertyFilter values in neighborhoods.ts"
    - "Every property type (Single Family, Condo, Townhouse, Multi-Family) is represented"
    - "Price range spans from ~$350K to ~$7M covering starter condos through luxury estates"
    - "At least 3 properties have videoUrl and at least 3 have virtualTourUrl for PROP-08 and PROP-09"
  artifacts:
    - path: "src/data/properties.ts"
      provides: "16+ curated Philadelphia property listings with neighborhood field"
      contains: "neighborhood"
  key_links:
    - from: "src/data/properties.ts"
      to: "src/data/neighborhoods.ts"
      via: "property.neighborhood matches neighborhood.propertyFilter"
      pattern: "neighborhood.*Center City|Rittenhouse|Fishtown|Old City"
---

<objective>
Expand property data from 6 to 16+ curated Philadelphia listings with a new `neighborhood` field that links properties to the 15 neighborhood pages.

Purpose: The current data only has 6 properties and the area filter is broken (uses San Diego area names). This plan creates a rich, realistic dataset that covers Philadelphia's premium neighborhoods and enables proper neighborhood filtering.

Output: Updated `src/data/properties.ts` with 16 properties and `neighborhood` field on the Property interface.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/data/properties.ts
@src/data/neighborhoods.ts (reference propertyFilter values for neighborhood field)
@src/data/agents.ts (reference agent names/emails for listing agents)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add neighborhood field to Property interface and expand to 16 listings</name>
  <files>src/data/properties.ts</files>
  <action>
  1. Add `neighborhood: string` field to the `Property` interface (after `city` field).

  2. Update the existing 6 properties to include the `neighborhood` field:
     - id 1 (1820 Rittenhouse Square): neighborhood: "Rittenhouse"
     - id 2 (312 Delancey St): neighborhood: "Old City" (Society Hill is near Old City)
     - id 3 (2401 Pennsylvania Ave): neighborhood: "Center City" (Fairmount area)
     - id 4 (1500 Chestnut St): neighborhood: "Center City"
     - id 5 (734 S Front St): neighborhood: "South Philly" (Queen Village)
     - id 6 (2038 E Hagert St): neighborhood: "Fishtown"

  3. Add 10 NEW properties (ids 7-16) covering these neighborhoods. Use realistic Philadelphia addresses, prices, and GPS coordinates. Each property needs all fields including images (use Unsplash URLs with `?w=1200`), description, features (interior/exterior/community arrays of 5-6 items each), and an agent from the agents.ts data.

  New properties to add:
  - id 7: Northern Liberties - modern loft/condo ~$650K, 2bd/2ba, 1400sf
  - id 8: University City - Single Family near Penn ~$1.1M, 4bd/3ba, 2800sf
  - id 9: Manayunk - Townhouse on Main St ~$525K, 3bd/2ba, 1800sf
  - id 10: Chestnut Hill - Historic Single Family ~$1.8M, 5bd/4ba, 4000sf
  - id 11: Brewerytown - New Construction Townhouse ~$485K, 3bd/3ba, 1700sf
  - id 12: Point Breeze - Renovated Rowhome ~$420K, 3bd/2ba, 1500sf
  - id 13: Kensington - Multi-Family investment ~$375K, 4bd/2ba, 2400sf (duplex)
  - id 14: West Philly - Victorian near Clark Park ~$580K, 4bd/2ba, 2200sf
  - id 15: Rittenhouse - Luxury Condo ~$2.4M, 3bd/3ba, 2600sf, status "New"
  - id 16: Fishtown - Modern Single Family ~$950K, 3bd/3ba, 2000sf, status "Open House"

  Requirements for new properties:
  - Use realistic lat/lng coordinates for each Philly neighborhood
  - Slugs follow pattern: address-slugified (e.g., "127-n-2nd-st-northern-liberties")
  - All have city: "Philadelphia", state: "PA" with correct zip codes
  - Spread agent assignments between Julian Bradley (julian@tauro.realty) and Sofia Martinez (sofia@tauro.realty)
  - At least 3 should have `videoUrl: "https://www.youtube.com/embed/dQw4w9WgXcQ"` (placeholder)
  - At least 3 should have `virtualTourUrl: "https://my.matterport.com/show/?m=SxQL3iGyvkk"` (placeholder)
  - At least 2 should have `openHouse` dates
  - Mix of statuses: Active (majority), New (2-3), Open House (2-3), Pending (1-2)
  - Descriptions should be 2-3 sentences highlighting the property and neighborhood character
  - Use varied Unsplash house/interior photo URLs (4-6 images per property), reusing from existing set is fine

  4. Keep existing helper functions (formatPrice, formatPriceFull, getPropertyBySlug) unchanged.
  </action>
  <verify>
  Run `npx tsc --noEmit` to verify TypeScript compilation passes with the new neighborhood field.
  Verify at least 16 properties exist: `grep -c "id:" src/data/properties.ts` should return 16+.
  Verify neighborhood field present: `grep -c "neighborhood:" src/data/properties.ts` should match property count.
  </verify>
  <done>
  - Property interface has `neighborhood: string` field
  - 16 properties exist with realistic Philadelphia data
  - Properties span at least 10 different neighborhoods matching propertyFilter values
  - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npm run build` succeeds (or fails only for pre-existing reasons unrelated to this change)
- Properties array has 16+ entries
- All neighborhood values match a propertyFilter value from neighborhoods.ts
</verification>

<success_criteria>
16+ curated Philadelphia properties with neighborhood field, spanning 10+ neighborhoods, covering all property types and a realistic price range from ~$350K to ~$7M.
</success_criteria>

<output>
After completion, create `.planning/phases/03-property-pages/03-04-SUMMARY.md`
</output>
