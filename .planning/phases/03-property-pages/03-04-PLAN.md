---
status: COMPLETED
phase: 03-property-pages
plan: 04
type: execute
wave: 3
depends_on: ["03-01", "03-02", "03-05"]
files_modified:
  - src/components/ImageGallery.tsx
  - src/app/properties/[slug]/page.tsx
  - src/app/properties/[slug]/PropertyDetailClient.tsx
autonomous: true

must_haves:
  truths:
    - "Property detail page at /properties/[slug] renders full property information for any valid slug"
    - "Image gallery shows hero image with thumbnail row below; clicking opens fullscreen lightbox with zoom, thumbnails, counter, and fullscreen plugins"
    - "Sticky key-details bar shows price, beds, baths, sqft, property type, and 'Schedule a Showing' CTA"
    - "Property details section shows description, year built, lot size, price/SF, and estimated monthly payment"
    - "Features section displays interior, exterior, and community features in a 3-column grid with checkmark icons"
    - "Video tour iframe renders when property has videoUrl"
    - "3D virtual tour iframe renders when property has virtualTourUrl"
    - "Schedule Showing form collects name, email, phone, message and POSTs to /api/leads with type: showing"
    - "Agent card shows agent photo, name, phone (tel: link), and email (mailto: link)"
    - "Similar properties section shows 3 other listings using PropertyCard"
    - "Page has SSG via generateStaticParams and proper metadata via generateMetadata"
    - "Location map renders using PropertyMap in singleMarker mode"
  artifacts:
    - path: "src/components/ImageGallery.tsx"
      provides: "Image gallery with hero, thumbnails, and lightbox"
      exports: ["default"]
      min_lines: 60
    - path: "src/app/properties/[slug]/page.tsx"
      provides: "Server page with SSG and metadata"
      exports: ["default", "generateStaticParams", "generateMetadata"]
      min_lines: 20
    - path: "src/app/properties/[slug]/PropertyDetailClient.tsx"
      provides: "Client component with full property detail UI"
      exports: ["default"]
      min_lines: 200
  key_links:
    - from: "src/app/properties/[slug]/page.tsx"
      to: "src/data/properties.ts"
      via: "import { properties, formatPriceFull }"
      pattern: "import.*properties.*from.*data/properties"
    - from: "src/app/properties/[slug]/page.tsx"
      to: "PropertyDetailClient"
      via: "server-to-client component render"
      pattern: "<PropertyDetailClient"
    - from: "PropertyDetailClient"
      to: "ImageGallery"
      via: "component render"
      pattern: "<ImageGallery"
    - from: "PropertyDetailClient"
      to: "/api/leads"
      via: "fetch POST on form submit"
      pattern: "fetch.*api/leads"
    - from: "PropertyDetailClient"
      to: "PropertyMap"
      via: "component render with singleMarker"
      pattern: "<PropertyMap"
    - from: "PropertyDetailClient"
      to: "PropertyCard"
      via: "similar properties grid"
      pattern: "<PropertyCard"
---

<objective>
Build the full property detail page with image gallery, lightbox, all property details, video/3D tour embeds, schedule showing form, and agent contact.

Purpose: This is the highest-value page on the site -- where browsing converts to lead capture. It must showcase properties beautifully and make it effortless to schedule a showing or contact the agent.

Output: ImageGallery component, server-side [slug] page with SSG, and PropertyDetailClient with complete detail UI.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/data/properties.ts (Property type and data)
@src/components/PropertyCard.tsx (for similar properties section, from 03-02)
@src/components/PropertyMap.tsx (for location map, from 03-05)
@src/components/JsonLd.tsx (RealEstateListingJsonLd for structured data)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ImageGallery component with lightbox</name>
  <files>src/components/ImageGallery.tsx</files>
  <action>
Create `src/components/ImageGallery.tsx` as a `"use client"` component:

- **Props**: `{ images: string[]; address: string }`
- **Dependencies**: Install `yet-another-react-lightbox` if not already in package.json (`npm install yet-another-react-lightbox`). Import Counter, Fullscreen, Thumbnails, Zoom plugins and their CSS files:
  - `import "yet-another-react-lightbox/styles.css"`
  - `import "yet-another-react-lightbox/plugins/counter.css"`
  - `import "yet-another-react-lightbox/plugins/thumbnails.css"`
- **Layout**:
  - Hero image: `aspect-[16/9] lg:aspect-[21/9] max-h-[500px]` with Next.js Image, `priority` loading, clickable button to open lightbox at index 0. Include aria-label.
  - Thumbnail row: Show up to 4 thumbnails below hero (images[1] through images[4]). Each is `h-16 w-24 flex-shrink-0` with `rounded-lg`. Last thumbnail shows "+N more" overlay (with `bg-black/60`) if total images exceed 5.
  - Thumbnails have `opacity-60 hover:opacity-100 hover:border-gold border-2 border-transparent` effect
- **Lightbox**: `<Lightbox>` component with plugins=[Counter, Fullscreen, Thumbnails, Zoom]. Set `carousel={{ finite: true }}`, dark background `styles={{ container: { backgroundColor: "rgba(0, 0, 0, 0.95)" } }}`. Slides array maps images to `{ src }` objects.
- **Empty state**: If no images or empty array, show centered "No images available" in a `aspect-[16/9]` container
- **State**: `open: boolean` and `index: number` for lightbox control
  </action>
  <verify>Run `npx tsc --noEmit` to verify types. Check that yet-another-react-lightbox is in package.json.</verify>
  <done>ImageGallery renders hero + thumbnails + fullscreen lightbox with zoom, counter, and thumbnail navigation.</done>
</task>

<task type="auto">
  <name>Task 2: Create property detail page with SSG and full client UI</name>
  <files>src/app/properties/[slug]/page.tsx, src/app/properties/[slug]/PropertyDetailClient.tsx</files>
  <action>
**Server page** (`page.tsx`):
- `generateStaticParams()`: Map all properties to `{ slug }` objects for SSG
- `generateMetadata({ params })`: Await params, find property by slug, return title as `"{address}, {city} | {formattedPrice}"` and description from first 160 chars. Return "Property Not Found" title if not found.
- Default export: Await params, find property by slug, return `notFound()` if missing. Get 3 similar properties (filter out current, slice first 3). Render `<RealEstateListingJsonLd property={property} />` and `<PropertyDetailClient property={property} similar={similar} />`

**Client component** (`PropertyDetailClient.tsx`) -- `"use client"`:
- Props: `{ property: Property; similar: Property[] }`
- **Form state**: name, email, phone, message strings + submitting boolean + submitSuccess boolean + submitError string

- **handleSubmit**: Split name into firstName/lastName, POST to `/api/leads` with JSON body containing type "showing", firstName, lastName, email, phone, message, propertyAddress (full), propertyId. Handle res.ok for success (clear form, set success). Catch errors.

- **Layout sections** (top to bottom):
  1. **Back link**: `<Link href="/properties">` with ArrowLeft icon + "Back to Properties"
  2. **Gallery**: `<ImageGallery images={property.images} address={property.address} />`
  3. **Sticky key-details bar** (`sticky top-16 z-40 bg-card/95 backdrop-blur-sm`):
     - Price (gold, font-heading, 2xl/3xl), beds (Bed icon), baths (Bath icon), sqft (Ruler icon), type (Home icon)
     - "Schedule a Showing" button with `href="#schedule"` anchor
  4. **Two-column main** (`lg:grid-cols-[1fr_380px]`):
     - **Left column** (space-y-10):
       a. Address heading + status badge (colored: Active=emerald-600/20, New=gold/20, Open House=blue-600/20, Pending=orange-500/20) + open house notice (Calendar icon, gold text)
       b. Description + 4 stat cards in grid (year built, lot size, price/SF calculated as `Math.round(price/sqft)`, est. monthly payment calculated as 80% LTV at 6.5% APR over 360 months using standard amortization formula)
       c. Video tour (conditional on videoUrl): Play icon heading, aspect-video iframe with allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
       d. 3D virtual tour (conditional on virtualTourUrl): View icon heading, aspect-video iframe with allow="fullscreen; vr", caption "Click and drag to explore the property in 3D"
       e. Features & Amenities: h2 heading, 3-col grid (sm:grid-cols-3) iterating over ["interior", "exterior", "community"], each with uppercase gold label and list of items with Check icon
       f. Location: h2 heading, PropertyMap with `properties={[property]} singleMarker center={[property.lng, property.lat]} zoom={14}` in a `h-64 rounded-xl border` container
       g. Similar Properties: h2 heading, grid of 3 PropertyCards (sm:grid-cols-2 lg:grid-cols-3)
     - **Right column** (`lg:sticky lg:top-36 lg:self-start`, space-y-6):
       a. Agent card: photo (64x64, rounded-full, border-2 border-gold), name (font-heading bold), "Listing Agent", phone as `<a href="tel:...">` with Phone icon, email as `<a href="mailto:...">` with Mail icon
       b. Schedule form (`id="schedule"`): Conditional render -- if submitSuccess, show success state (checkmark circle, "Request Submitted" heading, confirmation text, "Schedule Another" reset button). Otherwise show form with inputs (name required, email required, phone, message textarea), submit button ("Request a Showing" / "Submitting..."), error text if submitError.

- **Input styling**: `rounded-lg border border-border bg-background px-4 py-2.5 text-sm focus:border-gold focus:ring-1 focus:ring-gold disabled:opacity-50`
  </action>
  <verify>
Run `npm run build` to verify SSG generates all property detail pages. Check build output shows `/properties/[slug]` static routes.
  </verify>
  <done>
Property detail pages render at /properties/[slug] with full gallery, lightbox, property details, video/3D tour embeds, schedule showing form with API integration, agent contact card, location map, and similar properties. All pages are statically generated with proper metadata.
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes and generates static pages for all property slugs
- ImageGallery opens lightbox with zoom/fullscreen/thumbnails/counter
- Schedule form POSTs to /api/leads with correct payload
- Video and 3D tour iframes render conditionally (only when URLs exist)
- Agent card has tel: and mailto: links
- Location map renders with PropertyMap in singleMarker mode
- SEO metadata generated per property
</verification>

<success_criteria>
Every property has a rich detail page with gallery, lightbox, full details, video/3D tour support, functional schedule showing form, agent contact, location map, and similar property recommendations. Pages are statically generated for performance.
</success_criteria>

<output>
After completion, create `.planning/phases/03-property-pages/03-04-SUMMARY.md`
</output>
