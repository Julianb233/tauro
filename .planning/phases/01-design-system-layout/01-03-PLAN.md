---
phase: 01-design-system-layout
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/components/footer.tsx
autonomous: true

must_haves:
  truths:
    - "Footer displays 4-column responsive grid: Brand, Quick Links, Neighborhoods, Contact"
    - "Footer shows social media icons (Instagram, Facebook, LinkedIn, Twitter/X) with gold shimmer hover"
    - "Footer shows office map placeholder linking to Google Maps in Contact column"
    - "Footer uses Logo component from logo.tsx in the brand column"
    - "Quick links match site nav: Properties, Agents, Sell, About, Contact"
    - "Phone (215) 555-0100 and email info@taurorealty.com are clickable tel:/mailto: links"
    - "Social icons open in new tabs with rel='noopener noreferrer' and have aria-labels"
  artifacts:
    - path: "src/components/footer.tsx"
      provides: "Site-wide footer with 4-column grid, social icons, map placeholder, contact info"
      exports: ["Footer"]
  key_links:
    - from: "src/components/footer.tsx"
      to: "src/components/logo.tsx"
      via: "Import and render Logo component in brand column"
      pattern: "import.*Logo.*from"
    - from: "src/components/footer.tsx"
      to: "src/components/ui/gold-shimmer.tsx"
      via: "Import GoldShimmer to wrap social icons"
      pattern: "import.*GoldShimmer.*from"
    - from: "src/components/footer.tsx"
      to: "lucide-react"
      via: "Import social and contact icons"
      pattern: "Instagram.*Facebook.*Linkedin"
---

<objective>
Build the site-wide footer with 4-column layout, social media icons with gold shimmer, office map placeholder, contact info, and neighborhood links.

Purpose: Complete the NAV-04 requirement. Footer appears on every page and provides navigation, contact info, social presence, and legal links.
Output: footer.tsx with full-featured responsive footer component
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/components/logo.tsx
@src/components/ui/gold-shimmer.tsx
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build complete Footer component with 4-column grid, social icons, and map placeholder</name>
  <files>src/components/footer.tsx</files>
  <action>
  Create src/components/footer.tsx as a server component (no "use client" needed).

  **Imports:**
  - `Link` from "next/link"
  - `Mail, Phone, MapPin, Instagram, Facebook, Linkedin, Twitter` from "lucide-react"
  - `Logo` from "@/components/logo"
  - `GoldShimmer` from "@/components/ui/gold-shimmer"

  **Data arrays:**
  ```ts
  const quickLinks = [
    { href: "/properties", label: "Properties" },
    { href: "/agents", label: "Agents" },
    { href: "/sell", label: "Sell" },
    { href: "/about", label: "About" },
    { href: "/contact", label: "Contact" },
  ];

  const neighborhoods = [
    "Center City", "Rittenhouse", "Fishtown",
    "Northern Liberties", "Old City", "South Philadelphia",
  ];
  ```

  **Footer structure:**

  Outer: `<footer className="border-t border-border/40 bg-near-black">`
  Inner: `<div className="mx-auto max-w-7xl px-4 py-12 sm:px-6 lg:px-8">`
  Grid: `<div className="grid gap-8 md:grid-cols-2 lg:grid-cols-4">`

  **Column 1 - Brand:**
  - `<Link href="/" className="inline-block"><Logo width={100} height={34} /></Link>`
  - `<p className="max-w-xs text-sm leading-relaxed text-muted-foreground">` with tagline "Premium real estate brokerage serving Philadelphia's most sought-after neighborhoods."

  **Column 2 - Quick Links:**
  - `<h3 className="mb-4 text-sm font-semibold uppercase tracking-wider text-gold">Quick Links</h3>`
  - `<ul className="space-y-2.5">` mapping quickLinks to `<Link>` with `text-sm text-muted-foreground transition-colors hover:text-gold`

  **Column 3 - Neighborhoods:**
  - Same h3 style, "Neighborhoods" header
  - `<ul>` mapping neighborhoods to Links at `/neighborhoods/{slug}` where slug = `area.toLowerCase().replace(/\s+/g, "-")`

  **Column 4 - Contact:**
  - Same h3 style, "Contact" header
  - `<ul className="space-y-3">` with:
    - Phone: `<a href="tel:+12155550100">` with Phone icon (size-4) + "(215) 555-0100", styled `flex items-center gap-2 text-sm text-muted-foreground transition-colors hover:text-gold`
    - Email: `<a href="mailto:info@taurorealty.com">` with Mail icon + "info@taurorealty.com", same style
    - Address: `<span>` (not link) with MapPin icon + "Philadelphia, PA"
  - Map placeholder after the ul:
    ```tsx
    <a href="https://maps.google.com/?q=Philadelphia+PA" target="_blank" rel="noopener noreferrer"
       className="mt-4 block h-32 w-full overflow-hidden rounded-md border border-border/40 bg-midnight transition-colors hover:border-gold/40">
      <div className="flex h-full flex-col items-center justify-center gap-2 text-xs text-muted-foreground">
        <MapPin className="size-5 text-gold" />
        <span>Philadelphia, PA</span>
        <span className="text-gold/70">View on Map</span>
      </div>
    </a>
    ```

  **Bottom bar** (below grid):
  `<div className="mt-10 flex flex-col items-center justify-between gap-4 border-t border-border/40 pt-8 sm:flex-row">`

  Three sections:
  1. Copyright: `<p className="text-xs text-muted-foreground">&copy; {new Date().getFullYear()} Tauro. All rights reserved.</p>`
  2. Social icons: each icon wrapped in `<GoldShimmer>`, containing `<a>` with `target="_blank" rel="noopener noreferrer"`, `aria-label`, icon `size-5`, `text-muted-foreground transition-colors hover:text-gold`:
     - Instagram -> https://instagram.com/taurorealty
     - Facebook -> https://facebook.com/taurorealty
     - LinkedIn -> https://linkedin.com/company/taurorealty
     - Twitter/X -> https://x.com/taurorealty
  3. Legal links: Privacy Policy (/privacy) and Terms of Service (/terms), `text-xs text-muted-foreground hover:text-gold`

  Named export: `export function Footer`
  </action>
  <verify>
  `grep "export function Footer" src/components/footer.tsx` -- component exported
  `grep "Instagram" src/components/footer.tsx` -- social icon present
  `grep "Facebook" src/components/footer.tsx`
  `grep "Linkedin" src/components/footer.tsx`
  `grep "Twitter" src/components/footer.tsx`
  `grep "Logo" src/components/footer.tsx` -- Logo component used
  `grep "View on Map" src/components/footer.tsx` -- map placeholder
  `grep "aria-label" src/components/footer.tsx` -- accessibility
  `grep '"Sell"' src/components/footer.tsx` -- correct quick links
  `grep "GoldShimmer" src/components/footer.tsx` -- shimmer on social icons
  `npm run build` -- passes
  </verify>
  <done>Footer has: Logo in brand column, 5 quick links matching nav, 6 neighborhood links, phone/email/address in contact column, clickable map placeholder, 4 social icons with GoldShimmer wrapper, copyright with dynamic year, privacy/terms legal links. Build passes.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes
2. Footer renders 4-column grid on desktop, stacks on mobile
3. Social icons (Instagram, Facebook, LinkedIn, Twitter) with gold shimmer hover
4. Map placeholder visible in Contact column with "View on Map"
5. Logo component used in brand column
6. Quick links match: Properties, Agents, Sell, About, Contact
7. Phone and email are clickable tel:/mailto: links
8. All social links open in new tab with rel="noopener noreferrer"
9. Each social icon has aria-label for accessibility
</verification>

<success_criteria>
- Footer 4-column grid: Brand, Quick Links, Neighborhoods, Contact
- Social icons with GoldShimmer wrapper show gold shimmer on hover
- Map placeholder links to Google Maps in new tab
- Logo component renders in brand column
- Phone (215) 555-0100 and email info@taurorealty.com are clickable
- All external links have target="_blank" rel="noopener noreferrer"
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system-layout/01-03-SUMMARY.md`
</output>
