---
phase: 01-design-system-layout
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - src/app/(site)/layout.tsx
autonomous: true

must_haves:
  truths:
    - "Site layout wraps all pages with Navbar and Footer"
    - "Main content has min-h-screen so footer stays at bottom on short pages"
    - "Homepage renders correctly with all design system elements (fonts, colors, micro-interactions)"
    - "Header overlays hero transparently and transitions to solid on scroll"
  artifacts:
    - path: "src/app/(site)/layout.tsx"
      provides: "Page shell with min-h-screen on main content"
      contains: "min-h-screen"
  key_links:
    - from: "src/app/(site)/layout.tsx"
      to: "src/components/navbar.tsx"
      via: "import and render"
      pattern: "import.*Navbar"
    - from: "src/app/(site)/layout.tsx"
      to: "src/components/footer.tsx"
      via: "import and render"
      pattern: "import.*Footer"
---

<objective>
Update the site layout shell to ensure footer positioning works correctly on short pages.

Purpose: The homepage already exists with full content, but the layout wrapper needs min-h-screen on the main content area so the footer stays at the bottom even on pages with minimal content. This is the final integration step after navbar and footer updates.
Output: Updated (site)/layout.tsx with proper main element styling.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-design-system-layout/01-RESEARCH.md
@src/app/(site)/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update site layout with min-h-screen on main content</name>
  <files>src/app/(site)/layout.tsx</files>
  <action>
The current (site)/layout.tsx has `<main className="flex-1">`. Update it to include min-h-screen so the footer is pushed to the bottom on pages with short content.

Change:
```tsx
<main className="flex-1">{children}</main>
```
to:
```tsx
<main className="min-h-screen flex-1">{children}</main>
```

Do NOT add pt-16 or any global top padding. Individual pages handle their own spacing relative to the transparent/fixed header. The homepage already has a full-bleed hero that goes behind the header.

Keep the Navbar and Footer imports and rendering unchanged.
  </action>
  <verify>
`grep "min-h-screen" src/app/\(site\)/layout.tsx` -- confirms minimum height on main.
`npm run build` -- must pass.
  </verify>
  <done>Site layout has min-h-screen on main content to push footer down on short pages. No global pt offset applied.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes
2. `grep "min-h-screen" src/app/\(site\)/layout.tsx` confirms the class is present
3. `npm run dev` and visit http://localhost:3000 -- page renders with:
   - Full-viewport hero with gradient background
   - Header transparent over hero, solid after scrolling (requires Plan 02 completion)
   - Footer visible at bottom
   - All design system elements working together
4. Mobile viewport (resize to < 1024px):
   - Hamburger icon visible
   - Opens full-screen overlay (requires Plan 02 completion)
</verification>

<success_criteria>
- Site layout main content has min-h-screen class
- Footer stays at bottom even on short content pages
- Homepage renders correctly with all Phase 1 design elements
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system-layout/01-04-SUMMARY.md`
</output>
