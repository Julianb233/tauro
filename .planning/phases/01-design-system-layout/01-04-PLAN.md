---
phase: 01-design-system-layout
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - src/app/(site)/layout.tsx
  - src/app/(site)/page.tsx
autonomous: true

must_haves:
  truths:
    - "Site layout wraps all pages with Navbar and Footer"
    - "Main content has padding-top to account for fixed header"
    - "Dark mode renders correctly (midnight background, off-white text)"
    - "Homepage shows a visible section confirming typography, colors, and micro-interactions work together"
    - "All micro-interaction classes (shimmer-gold, card-tilt) render correctly when applied"
  artifacts:
    - path: "src/app/(site)/layout.tsx"
      provides: "Page shell with pt offset for fixed header"
      contains: "pt-16"
    - path: "src/app/(site)/page.tsx"
      provides: "Integration test page demonstrating design system"
      contains: "shimmer-gold"
  key_links:
    - from: "src/app/(site)/layout.tsx"
      to: "src/components/navbar.tsx"
      via: "import and render"
      pattern: "import.*Navbar"
    - from: "src/app/(site)/layout.tsx"
      to: "src/components/footer.tsx"
      via: "import and render"
      pattern: "import.*Footer"
---

<objective>
Verify the page shell layout works end-to-end and create an integration test page demonstrating all design system elements.

Purpose: This plan ensures the fixed header doesn't overlap content, Navbar+Footer wrap all pages correctly, and all design tokens/micro-interactions from previous plans work together. The test page serves as a living style guide during development.

Output: Updated site layout with proper spacing and a temporary design system demo homepage.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-design-system-layout/01-RESEARCH.md
@src/app/(site)/layout.tsx
@src/components/navbar.tsx
@src/components/footer.tsx
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update site layout with header offset spacing</name>
  <files>src/app/(site)/layout.tsx</files>
  <action>
The current (site)/layout.tsx has `<main className="flex-1">`. The fixed header (h-16 = 4rem) overlaps the first 64px of content on pages that don't have a full-bleed hero.

Update the main element to:
```tsx
<main className="min-h-screen flex-1">{children}</main>
```

Do NOT add pt-16 here globally -- individual pages will handle their own top spacing. Pages with full-bleed heroes (like the homepage) want the content to start at the top (under the transparent header). Pages without heroes will add their own pt-16 or pt-20.

Add `min-h-screen` so the footer stays at the bottom even on short pages.

Keep the Navbar and Footer imports and rendering unchanged.
  </action>
  <verify>Grep `src/app/(site)/layout.tsx` for "min-h-screen" to confirm minimum height.</verify>
  <done>Site layout has min-h-screen on main content to push footer down on short pages. No global pt offset -- pages manage their own spacing relative to the transparent header.</done>
</task>

<task type="auto">
  <name>Task 2: Create design system integration demo on homepage</name>
  <files>src/app/(site)/page.tsx</files>
  <action>
Replace the contents of `src/app/(site)/page.tsx` with a temporary design system showcase page. This demonstrates all Phase 1 design elements working together and will be replaced by the real homepage in Phase 2.

The page should be a server component (no "use client") with these sections:

```tsx
import Link from "next/link";

export default function HomePage() {
  return (
    <div>
      {/* Hero section - tests transparent header overlay */}
      <section className="flex min-h-screen items-center justify-center bg-gradient-to-br from-near-black via-midnight to-near-black px-4">
        <div className="text-center">
          <h1 className="font-heading text-5xl font-bold tracking-tight text-foreground sm:text-7xl">
            Premium Philadelphia
            <br />
            <span className="text-gold">Real Estate</span>
          </h1>
          <p className="mx-auto mt-6 max-w-2xl text-lg text-muted-foreground">
            Discover luxury homes across Philadelphia&apos;s most sought-after neighborhoods.
          </p>
          <div className="mt-10 flex flex-col items-center gap-4 sm:flex-row sm:justify-center">
            <Link
              href="/properties"
              className="shimmer-gold rounded-md bg-gold px-8 py-3 font-label text-sm font-semibold uppercase tracking-wider text-near-black transition-colors hover:bg-gold-light"
            >
              Browse Properties
            </Link>
            <Link
              href="/contact"
              className="rounded-md border border-gold/30 px-8 py-3 font-label text-sm font-semibold uppercase tracking-wider text-gold transition-colors hover:border-gold hover:bg-gold/10"
            >
              Schedule a Showing
            </Link>
          </div>
        </div>
      </section>

      {/* Typography showcase */}
      <section className="mx-auto max-w-4xl px-4 py-24">
        <h2 className="font-heading text-3xl font-bold text-foreground">Typography System</h2>
        <div className="mt-8 space-y-4">
          <p className="text-lg text-foreground">Body text in DM Sans &mdash; The quick brown fox jumps over the lazy dog.</p>
          <p className="text-muted-foreground">Muted body text for secondary information and descriptions.</p>
          <p className="font-label text-sm font-semibold uppercase tracking-wider text-gold">Label / CTA text in Montserrat</p>
        </div>
      </section>

      {/* Card tilt demo */}
      <section className="mx-auto max-w-4xl px-4 py-24">
        <h2 className="font-heading text-3xl font-bold text-foreground">Micro-interactions</h2>
        <p className="mt-2 text-muted-foreground">Hover over cards to see the 3D tilt effect.</p>
        <div className="mt-8 grid gap-6 sm:grid-cols-3">
          {["Rittenhouse Square", "Fishtown", "Center City"].map((name) => (
            <div
              key={name}
              className="card-tilt shimmer-gold cursor-pointer rounded-lg border border-border/40 bg-card p-6"
            >
              <h3 className="font-heading text-xl font-semibold text-foreground">{name}</h3>
              <p className="mt-2 text-sm text-muted-foreground">
                Explore luxury homes in {name}.
              </p>
              <span className="mt-4 inline-block font-label text-xs font-semibold uppercase tracking-wider text-gold">
                View Listings &rarr;
              </span>
            </div>
          ))}
        </div>
      </section>

      {/* Color palette reference */}
      <section className="mx-auto max-w-4xl px-4 py-24">
        <h2 className="font-heading text-3xl font-bold text-foreground">Brand Colors</h2>
        <div className="mt-8 flex flex-wrap gap-6">
          {[
            { name: "Midnight", cls: "bg-midnight" },
            { name: "Near Black", cls: "bg-near-black border border-border/40" },
            { name: "Gold", cls: "bg-gold" },
            { name: "Gold Light", cls: "bg-gold-light" },
            { name: "Gold Dark", cls: "bg-gold-dark" },
            { name: "Oxblood", cls: "bg-oxblood" },
            { name: "Off White", cls: "bg-off-white" },
          ].map(({ name, cls }) => (
            <div key={name} className="flex flex-col items-center gap-2">
              <div className={`size-16 rounded-md ${cls}`} />
              <span className="text-xs text-muted-foreground">{name}</span>
            </div>
          ))}
        </div>
      </section>
    </div>
  );
}
```

This page serves multiple purposes:
- Full-viewport hero tests transparent header overlay (navbar should be transparent over the gradient)
- Scrolling past hero tests the scroll-to-solid transition
- Typography section validates DM Sans body, Playfair heading, Montserrat labels
- Card section validates card-tilt and shimmer-gold micro-interactions
- Color palette validates all brand token utilities including oxblood and off-white
- shimmer-gold on CTA validates the button shimmer effect

Note: This is a TEMPORARY page. Phase 2 will replace it with the real homepage.
  </action>
  <verify>
`npm run build` -- must compile
`grep "shimmer-gold" src/app/\(site\)/page.tsx` -- confirms shimmer usage
`grep "card-tilt" src/app/\(site\)/page.tsx` -- confirms tilt usage
`grep "font-label" src/app/\(site\)/page.tsx` -- confirms Montserrat usage
`grep "font-heading" src/app/\(site\)/page.tsx` -- confirms Playfair usage
`grep "bg-oxblood" src/app/\(site\)/page.tsx` -- confirms oxblood color usage
  </verify>
  <done>Homepage renders a design system showcase with hero (transparent header test), typography samples, card tilt + shimmer demo, and color palette including all brand colors. All Phase 1 design elements are visually verifiable.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds without errors
2. `npm run dev` and visit http://localhost:3000 -- page renders with:
   - Full-viewport hero with gradient background
   - Header transparent over hero, solid after scrolling
   - Typography section showing 3 distinct fonts
   - Card tilt + shimmer effect on neighborhood cards
   - Color palette swatches including oxblood and off-white
   - Footer visible at bottom with social icons
3. Mobile viewport (resize to < 1024px):
   - Hamburger icon visible
   - Opens full-screen overlay
   - Large centered links
</verification>

<success_criteria>
- Site renders with dark background, off-white text, correct brand colors
- Header overlays hero transparently, transitions to solid on scroll
- DM Sans visible as body font, Playfair Display on headings, Montserrat on labels
- Shimmer and card-tilt effects work on interactive elements
- Footer shows social icons and map placeholder
- Mobile overlay works correctly
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system-layout/01-04-SUMMARY.md`
</output>
