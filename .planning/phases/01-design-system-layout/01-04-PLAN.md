---
phase: 01-design-system-layout
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - src/app/(site)/layout.tsx
  - src/app/(site)/page.tsx
autonomous: false

must_haves:
  truths:
    - "Site layout wraps all pages with Navbar and Footer"
    - "Footer stays at bottom even on short pages (min-h-screen on main)"
    - "Dark mode renders correctly (near-black background, off-white text)"
    - "Homepage uses shimmer-gold on CTAs and demonstrates design system"
    - "Header overlays hero section transparently, transitions to solid on scroll"
    - "Mobile overlay menu works with centered links and escape-to-close"
  artifacts:
    - path: "src/app/(site)/layout.tsx"
      provides: "Page shell with min-h-screen on main content"
      contains: "min-h-screen"
    - path: "src/app/(site)/page.tsx"
      provides: "Homepage with shimmer-gold CTAs demonstrating design system integration"
      contains: "shimmer-gold"
  key_links:
    - from: "src/app/(site)/layout.tsx"
      to: "src/components/navbar.tsx"
      via: "import and render"
      pattern: "import.*Navbar"
    - from: "src/app/(site)/layout.tsx"
      to: "src/components/footer.tsx"
      via: "import and render"
      pattern: "import.*Footer"
---

<objective>
Ensure the page shell layout works end-to-end, add shimmer-gold to homepage CTAs, and visually verify the complete design system.

Purpose: This plan ensures the fixed header doesn't overlap content, Navbar+Footer wrap all pages correctly, and all design tokens/micro-interactions from previous plans work together in production.
Output: Updated site layout with min-h-screen, homepage CTAs enhanced with shimmer-gold, visual verification of all Phase 1 elements.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-design-system-layout/01-RESEARCH.md
@src/app/(site)/layout.tsx
@src/app/(site)/page.tsx
@src/components/navbar.tsx
@src/components/footer.tsx
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update site layout and add shimmer-gold to homepage CTAs</name>
  <files>src/app/(site)/layout.tsx, src/app/(site)/page.tsx</files>
  <action>
**In src/app/(site)/layout.tsx:**

The current layout has `<main className="flex-1">`. Update to:
```tsx
<main className="min-h-screen flex-1">{children}</main>
```

Do NOT add pt-16 globally -- the homepage has a full-bleed hero that should start at the top under the transparent header. Non-hero pages will add their own pt-16 or pt-20.

Keep the Navbar and Footer imports and rendering unchanged.

**In src/app/(site)/page.tsx:**

The homepage is already a full production page with hero, featured properties, neighborhoods, etc. Make these targeted updates:

1. Add `shimmer-gold` class to the Search button in the hero section (the `<button>` with text "Search"):
   Change its className to include `shimmer-gold` -- e.g., `"shimmer-gold rounded-lg bg-gold px-6 py-3 text-sm font-semibold text-near-black transition-all hover:bg-gold-light hover:shadow-lg"`

2. Add `shimmer-gold` class to the "Browse Properties" CTA Link in the buyer CTA section at the bottom.

3. Add `font-label` class to the uppercase tracking-wider label texts (the ones like "Premium Philadelphia Real Estate", "Featured Listings", "Explore Philadelphia", etc.) to use Montserrat. These are the `<p>` elements with `text-sm font-semibold uppercase tracking-[0.2em] text-gold`. Add `font-label` to their classNames.

4. Add `card-tilt` class to the "Why Tauro" feature cards (the 4-card grid with icons) to give them a subtle tilt on hover. Add `card-tilt` to the outer div className of each card.

Do NOT restructure the page. Do NOT replace the page content. Only add the CSS utility classes to existing elements.
  </action>
  <verify>
`grep "min-h-screen" src/app/\(site\)/layout.tsx` -- confirms minimum height
`grep "shimmer-gold" src/app/\(site\)/page.tsx` -- confirms shimmer usage on CTAs
`grep "font-label" src/app/\(site\)/page.tsx` -- confirms Montserrat on labels
`grep "card-tilt" src/app/\(site\)/page.tsx` -- confirms tilt on cards
`npm run build` -- must pass
  </verify>
  <done>Site layout has min-h-screen on main. Homepage CTAs have shimmer-gold effect, label texts use Montserrat via font-label, Why Tauro cards have card-tilt hover effect. All Phase 1 design elements integrated into production page.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Phase 1 design system: brand colors, typography (DM Sans / Playfair / Montserrat), scroll-transparent header, full-screen mobile overlay, footer with social icons and map placeholder, gold shimmer and card tilt micro-interactions.</what-built>
  <how-to-verify>
1. Run `npm run dev` and visit http://localhost:3000
2. **Header**: Should start transparent over hero image, then gain solid dark background after scrolling ~50px
3. **Typography**: Headings should be serif (Playfair Display), body text sans-serif (DM Sans), label/CTA text in Montserrat (uppercase tracking-wider text)
4. **Colors**: Dark background (#0F0F0F), gold accents (#C9A84C), off-white text (#F5F0E8)
5. **Shimmer**: Hover over "Search" button or "Browse Properties" CTA -- gold shimmer sweep should appear
6. **Card tilt**: Hover over "Why Tauro" feature cards -- should have subtle 3D tilt
7. **Navigation**: Links should be Properties, Agents, Sell, About, Contact (no Home, no Neighborhoods)
8. **Mobile** (resize to < 1024px):
   - Hamburger icon visible
   - Click opens full-screen overlay with large centered links
   - Press Escape to close
   - Page should not scroll behind overlay
9. **Footer**: Scroll to bottom
   - Logo component visible
   - Quick links: Properties, Agents, Sell, About, Contact
   - Social icons: Instagram, Facebook, LinkedIn, Twitter/X
   - Map placeholder with "View on Map" text
   - Social icons turn gold on hover
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` succeeds without errors
2. `npm run dev` and visit http://localhost:3000 -- page renders with:
   - Full-viewport hero with background image
   - Header transparent over hero, solid after scrolling
   - Shimmer effect on gold CTA buttons
   - Card tilt on feature cards
   - Footer visible at bottom with social icons and map placeholder
3. Mobile viewport (resize to < 1024px):
   - Hamburger icon visible
   - Opens full-screen overlay with large centered links
   - Escape key closes overlay
   - Body scroll locked while open
</verification>

<success_criteria>
- Site renders with dark background, off-white text, correct brand colors
- Header overlays hero transparently, transitions to solid on scroll
- DM Sans visible as body font, Playfair Display on headings, Montserrat on labels
- Shimmer and card-tilt effects work on interactive elements
- Footer shows social icons and map placeholder
- Mobile overlay works correctly with escape key
- Human visual verification passes
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system-layout/01-04-SUMMARY.md`
</output>
