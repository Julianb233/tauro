---
phase: 01-design-system-layout
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - src/app/(site)/layout.tsx
  - src/app/(site)/page.tsx
autonomous: false
status: COMPLETED

must_haves:
  truths:
    - "Site layout wraps all (site) pages with Navbar at top and Footer at bottom"
    - "Main content area has min-h-screen so footer stays at bottom on short pages"
    - "Dark mode renders correctly: near-black background (#0F0F0F), off-white text (#F5F0E8)"
    - "Homepage hero section starts at top under transparent header (no extra padding)"
    - "Homepage CTAs have shimmer-gold class for gold sweep hover effect"
    - "Homepage label texts use font-label class (Montserrat) for uppercase tracking text"
    - "Header overlays hero transparently then transitions to solid on scroll"
    - "Mobile overlay menu works with centered links and escape-to-close"
    - "Footer renders at bottom with social icons, map placeholder, and all columns"
  artifacts:
    - path: "src/app/(site)/layout.tsx"
      provides: "Page shell wrapping children with Navbar + Footer, min-h-screen on main"
      contains: "min-h-screen"
    - path: "src/app/(site)/page.tsx"
      provides: "Homepage demonstrating full design system: hero, featured properties, neighborhoods, Why Tauro, testimonials, CTAs"
      contains: "shimmer-gold"
  key_links:
    - from: "src/app/(site)/layout.tsx"
      to: "src/components/navbar.tsx"
      via: "Import and render Navbar component"
      pattern: "import.*Navbar"
    - from: "src/app/(site)/layout.tsx"
      to: "src/components/footer.tsx"
      via: "Import and render Footer component"
      pattern: "import.*Footer"
    - from: "src/app/(site)/page.tsx"
      to: "src/components/ui/tilt-card.tsx"
      via: "Import TiltCard for property cards and feature cards"
      pattern: "import.*TiltCard"
---

<objective>
Wire the page shell layout (Navbar + Footer wrapping all pages), enhance the homepage with design system micro-interactions, and visually verify the complete Phase 1 design system end-to-end.

Purpose: This plan ensures all Phase 1 components work together in production -- header overlays hero correctly, footer renders on all pages, and micro-interactions (shimmer, tilt) are visible on the homepage.
Output: Working site layout, homepage with all design system elements demonstrated, human verification of visual quality.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/app/(site)/layout.tsx
@src/app/(site)/page.tsx
@src/components/navbar.tsx
@src/components/footer.tsx
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify site layout shell and enhance homepage with micro-interactions</name>
  <files>src/app/(site)/layout.tsx, src/app/(site)/page.tsx</files>
  <action>
  **src/app/(site)/layout.tsx** -- Verify it has this structure (create if missing):

  ```tsx
  import { Navbar } from "@/components/navbar";
  import { Footer } from "@/components/footer";

  export default function SiteLayout({ children }: { children: React.ReactNode }) {
    return (
      <>
        <Navbar />
        <main className="min-h-screen flex-1">{children}</main>
        <Footer />
      </>
    );
  }
  ```

  Key: `min-h-screen` on main ensures footer stays at bottom on short pages. Do NOT add `pt-16` globally -- the homepage hero should start at viewport top under the transparent header. Individual non-hero pages add their own top padding.

  **src/app/(site)/page.tsx** -- Make targeted enhancements to the existing homepage (do NOT restructure or rewrite):

  1. **shimmer-gold on CTA buttons**: Add `shimmer-gold` class to gold CTA buttons:
     - The "Schedule a Showing" or "Browse Properties" Link buttons in the buyer/seller CTA sections
     - Any gold `bg-gold` button that is a primary action

  2. **font-label on uppercase labels**: Add `font-label` class to the uppercase tracking text labels throughout the page. These are the `<p>` elements with classes like `text-sm font-semibold uppercase tracking-[0.2em] text-gold`. They should also have `font-label` to use Montserrat. Labels include:
     - "Premium Philadelphia Real Estate" (hero subtitle)
     - "Featured Listings"
     - "Explore Philadelphia"
     - "The Tauro Difference"
     - "Client Stories"
     - "For Buyers" / "For Sellers"

  3. **TiltCard wrapping**: Ensure property cards and "Why Tauro" feature cards use `<TiltCard>` wrapper for interactive 3D tilt. Import TiltCard from "@/components/ui/tilt-card" if not already imported.

  These are CSS class additions only. Do NOT change component structure, data, or layout.
  </action>
  <verify>
  `grep "min-h-screen" src/app/\(site\)/layout.tsx` -- min height on main
  `grep "Navbar" src/app/\(site\)/layout.tsx` -- Navbar imported
  `grep "Footer" src/app/\(site\)/layout.tsx` -- Footer imported
  `grep "shimmer-gold" src/app/\(site\)/page.tsx` -- shimmer on CTAs
  `grep "font-label" src/app/\(site\)/page.tsx` -- Montserrat on labels
  `grep "TiltCard" src/app/\(site\)/page.tsx` -- tilt cards used
  `npm run build` -- passes
  </verify>
  <done>Site layout wraps pages with Navbar + Footer. Homepage CTAs have shimmer-gold effect, label texts use Montserrat via font-label, feature/property cards use TiltCard. All Phase 1 design elements integrated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Phase 1 design system: brand colors, typography (DM Sans body, Playfair Display headings, Montserrat labels), scroll-transparent fixed header, full-screen mobile overlay menu, 4-column footer with social icons and map placeholder, gold shimmer and 3D card tilt micro-interactions.</what-built>
  <how-to-verify>
1. Run `npm run dev` and visit http://localhost:3000
2. **Colors**: Background should be near-black (#0F0F0F), text off-white, gold accents on headings and buttons
3. **Header**: Should start transparent over hero image, then gain solid dark background after scrolling ~50px. Links: Properties, Agents, Sell, About, Contact
4. **Typography**: Headings = serif (Playfair Display), body = sans-serif (DM Sans), uppercase labels = Montserrat
5. **Shimmer**: Hover over gold CTA buttons (e.g., "Browse Properties") -- gold sweep animation should play
6. **Card tilt**: Hover over "Why Tauro" feature cards or property cards -- subtle 3D tilt following mouse
7. **Mobile** (resize to < 1024px width):
   - Hamburger icon visible in header
   - Click opens full-screen dark overlay with large centered links
   - Press Escape to close
   - Page should NOT scroll behind overlay
8. **Footer** (scroll to bottom):
   - 4 columns: Brand (with logo), Quick Links, Neighborhoods, Contact
   - Social icons: Instagram, Facebook, LinkedIn, Twitter/X -- should glow gold on hover
   - Map placeholder with "View on Map" text in Contact column
   - Phone and email visible and clickable
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. Homepage renders with full-viewport hero, transparent header, correct typography
3. Shimmer effect visible on gold CTA buttons
4. Card tilt works on feature/property cards
5. Mobile overlay works with escape key and scroll lock
6. Footer renders with all 4 columns, social icons, map placeholder
7. Human visual verification passes
</verification>

<success_criteria>
- Site renders with dark background, off-white text, gold accents
- Header overlays hero transparently, transitions to solid on scroll
- Three font families visible: DM Sans (body), Playfair Display (headings), Montserrat (labels)
- Shimmer and card-tilt effects work on interactive elements
- Mobile overlay menu works correctly
- Footer shows all 4 columns with social icons and map placeholder
- Human visual verification approved
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system-layout/01-04-SUMMARY.md`
</output>
