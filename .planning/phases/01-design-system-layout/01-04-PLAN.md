---
phase: 01-design-system-layout
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - src/app/(site)/layout.tsx
  - src/app/(site)/page.tsx
autonomous: true

must_haves:
  truths:
    - "Site layout wraps all pages with Navbar and Footer"
    - "Main content has padding-top to account for fixed header"
    - "Dark mode renders correctly (midnight background, off-white text)"
    - "Homepage shows a visible section confirming typography, colors, and layout work together"
    - "All micro-interaction classes (shimmer-gold, card-tilt, diagonal-wipe) render correctly when applied"
  artifacts:
    - path: "src/app/(site)/layout.tsx"
      provides: "Page shell with pt offset for fixed header"
      contains: "pt-16"
    - path: "src/app/(site)/page.tsx"
      provides: "Integration test page demonstrating design system"
      contains: "shimmer-gold"
  key_links:
    - from: "src/app/(site)/layout.tsx"
      to: "src/components/navbar.tsx"
      via: "import and render"
      pattern: "import.*Navbar"
    - from: "src/app/(site)/layout.tsx"
      to: "src/components/footer.tsx"
      via: "import and render"
      pattern: "import.*Footer"
---

<objective>
Verify the page shell layout works end-to-end and create an integration test page demonstrating all design system elements.

Purpose: This plan ensures the fixed header doesn't overlap content, Navbar+Footer wrap all pages correctly, and all design tokens/micro-interactions from previous plans work together. The test page serves as a living style guide during development.

Output: Updated site layout with proper spacing and an integration demo page on the homepage.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-design-system-layout/01-RESEARCH.md
@.planning/phases/01-design-system-layout/01-01-SUMMARY.md
@.planning/phases/01-design-system-layout/01-02-SUMMARY.md
@.planning/phases/01-design-system-layout/01-03-SUMMARY.md
@src/app/(site)/layout.tsx
@src/components/navbar.tsx
@src/components/footer.tsx
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update site layout with header offset spacing</name>
  <files>src/app/(site)/layout.tsx</files>
  <action>
  The current (site)/layout.tsx has `<main className="flex-1">`. The fixed header (h-16 = 4rem) overlaps the first 64px of content. Add padding-top to account for this.

  Update the main element:
  ```tsx
  <main className="min-h-screen flex-1 pt-16">{children}</main>
  ```

  `pt-16` = padding-top 4rem = 64px = exactly the header height (h-16).
  `min-h-screen` ensures the footer stays at the bottom even on short pages.

  Keep the Navbar and Footer imports and rendering unchanged.
  </action>
  <verify>Grep `(site)/layout.tsx` for "pt-16" to confirm header offset. Grep for "min-h-screen" to confirm minimum height.</verify>
  <done>Site layout has pt-16 on main content to prevent fixed header overlap, and min-h-screen to push footer down on short pages.</done>
</task>

<task type="auto">
  <name>Task 2: Create design system integration demo on homepage</name>
  <files>src/app/(site)/page.tsx</files>
  <action>
  Create (or replace if exists) `src/app/(site)/page.tsx` as a temporary design system showcase page. This page demonstrates all Phase 1 design elements working together and will be replaced by the real homepage in Phase 2.

  The page should be a server component (no "use client") with these sections:

  ```tsx
  import Link from "next/link";

  export default function HomePage() {
    return (
      <div className="space-y-24 px-4 py-16 sm:px-6 lg:px-8">
        {/* Hero section - tests transparent header overlay */}
        <section className="-mx-4 -mt-32 flex min-h-screen items-center justify-center bg-gradient-to-br from-near-black via-midnight to-near-black px-4 pt-16 sm:-mx-6 lg:-mx-8">
          <div className="text-center">
            <h1 className="font-heading text-5xl font-bold tracking-tight text-foreground sm:text-7xl">
              <span className="shimmer-gold inline-block px-2 py-1">TAURO</span>
            </h1>
            <p className="mx-auto mt-6 max-w-2xl text-lg text-muted-foreground">
              Premium Philadelphia Real Estate
            </p>
            <div className="mt-10 flex flex-col items-center gap-4 sm:flex-row sm:justify-center">
              <Link
                href="/properties"
                className="shimmer-gold rounded-md bg-gold px-8 py-3 font-label text-sm font-semibold uppercase tracking-wider text-near-black transition-colors hover:bg-gold-light"
              >
                Browse Properties
              </Link>
              <Link
                href="/contact"
                className="rounded-md border border-gold/30 px-8 py-3 font-label text-sm font-semibold uppercase tracking-wider text-gold transition-colors hover:border-gold hover:bg-gold/10"
              >
                Schedule a Showing
              </Link>
            </div>
          </div>
        </section>

        {/* Typography showcase */}
        <section className="mx-auto max-w-4xl">
          <h2 className="font-heading text-3xl font-bold text-foreground">Typography System</h2>
          <div className="mt-8 space-y-4">
            <p className="text-lg text-foreground">Body text in DM Sans - The quick brown fox jumps over the lazy dog.</p>
            <p className="text-muted-foreground">Muted body text - Secondary information and descriptions.</p>
            <p className="font-label text-sm font-semibold uppercase tracking-wider text-gold">Label / CTA text in Montserrat</p>
          </div>
        </section>

        {/* Card tilt demo */}
        <section className="mx-auto max-w-4xl">
          <h2 className="font-heading text-3xl font-bold text-foreground">Micro-interactions</h2>
          <div className="mt-8 grid gap-6 sm:grid-cols-3">
            {["Rittenhouse Square", "Fishtown", "Center City"].map((name) => (
              <div
                key={name}
                className="card-tilt cursor-pointer rounded-lg border border-border/40 bg-card p-6"
              >
                <h3 className="font-heading text-xl font-semibold text-foreground">{name}</h3>
                <p className="mt-2 text-sm text-muted-foreground">
                  Explore luxury homes in {name}.
                </p>
                <span className="mt-4 inline-block font-label text-xs font-semibold uppercase tracking-wider text-gold">
                  View Listings
                </span>
              </div>
            ))}
          </div>
        </section>

        {/* Color palette reference */}
        <section className="mx-auto max-w-4xl">
          <h2 className="font-heading text-3xl font-bold text-foreground">Brand Colors</h2>
          <div className="mt-8 flex flex-wrap gap-4">
            <div className="flex flex-col items-center gap-2">
              <div className="size-16 rounded-md bg-near-black border border-border/40" />
              <span className="text-xs text-muted-foreground">Near Black</span>
            </div>
            <div className="flex flex-col items-center gap-2">
              <div className="size-16 rounded-md bg-midnight" />
              <span className="text-xs text-muted-foreground">Midnight</span>
            </div>
            <div className="flex flex-col items-center gap-2">
              <div className="size-16 rounded-md bg-gold" />
              <span className="text-xs text-muted-foreground">Gold</span>
            </div>
            <div className="flex flex-col items-center gap-2">
              <div className="size-16 rounded-md bg-gold-light" />
              <span className="text-xs text-muted-foreground">Gold Light</span>
            </div>
            <div className="flex flex-col items-center gap-2">
              <div className="size-16 rounded-md bg-gold-dark" />
              <span className="text-xs text-muted-foreground">Gold Dark</span>
            </div>
            <div className="flex flex-col items-center gap-2">
              <div className="size-16 rounded-md bg-oxblood" />
              <span className="text-xs text-muted-foreground">Oxblood</span>
            </div>
            <div className="flex flex-col items-center gap-2">
              <div className="size-16 rounded-md bg-off-white" />
              <span className="text-xs text-muted-foreground">Off White</span>
            </div>
          </div>
        </section>

        {/* Diagonal wipe demo */}
        <section className="mx-auto max-w-4xl">
          <h2 className="font-heading text-3xl font-bold text-foreground">Animation Utilities</h2>
          <div className="mt-8 space-y-4">
            <div className="diagonal-wipe rounded-md bg-card p-6 border border-border/40">
              <p className="text-foreground">This section uses the diagonal-wipe entrance animation.</p>
            </div>
            <div className="slash-reveal rounded-md bg-card p-6 border border-border/40">
              <p className="text-foreground">This section uses the slash-reveal entrance animation.</p>
            </div>
          </div>
        </section>
      </div>
    );
  }
  ```

  This page serves multiple purposes:
  - The full-viewport hero tests that the transparent header works correctly (navbar overlays the gradient)
  - Scrolling past the hero tests the scroll-to-solid transition
  - Typography section validates DM Sans body, Playfair heading, Montserrat labels
  - Card tilt section validates the card-tilt micro-interaction
  - Color palette validates all brand token utilities (using updated names: oxblood, off-white)
  - shimmer-gold on elements validates the shimmer hover effect
  - diagonal-wipe and slash-reveal sections validate clip-path animations

  Note: This is a TEMPORARY page that Phase 2 will replace with the real homepage.
  </action>
  <verify>
  1. Run `npm run build` to confirm the page compiles
  2. Grep page.tsx for "shimmer-gold" to confirm shimmer usage
  3. Grep page.tsx for "card-tilt" to confirm tilt usage
  4. Grep page.tsx for "diagonal-wipe" to confirm wipe animation usage
  5. Grep page.tsx for "font-label" to confirm Montserrat usage
  6. Grep page.tsx for "font-heading" to confirm Playfair usage
  7. Grep page.tsx for "bg-oxblood" to confirm updated brand color name
  </verify>
  <done>Homepage renders a design system showcase with hero (transparent header test), typography samples, card tilt demo, shimmer effects, color palette with all brand colors (including oxblood and off-white), and animation utility demos. All Phase 1 design elements are visually verifiable.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds without errors
2. `npx next dev` and visit http://localhost:3000 -- page renders with:
   - Full-viewport hero with gradient background
   - "TAURO" text with gold shimmer animation
   - "Browse Properties" button with shimmer sweep on hover
   - Header transparent over hero, solid after scrolling
   - Typography section showing 3 distinct fonts
   - Card tilt effect on neighborhood cards
   - Color palette swatches (including oxblood and off-white)
   - Diagonal wipe and slash reveal animations
   - Footer visible at bottom with social icons
3. Mobile viewport (resize to < 1024px):
   - Hamburger icon visible
   - Opens full-screen overlay
   - Large centered links
   - Body scroll locked
</verification>

<success_criteria>
- Site renders with dark background (#1A1A1A midnight), off-white text (#F5F0E8), correct brand colors
- Header overlays hero transparently, transitions to solid on scroll
- DM Sans visible as body font, Playfair Display on headings, Montserrat on labels
- Gold shimmer animates on hover
- Cards tilt on hover
- Diagonal wipe and slash reveal animations play
- Footer shows social icons and map placeholder
- Mobile overlay works correctly
- `npm run build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system-layout/01-04-SUMMARY.md`
</output>
