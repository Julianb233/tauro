---
phase: 09-seo-performance-deploy
plan: 03
type: execute
wave: 2
depends_on: ["09-01", "09-02"]
files_modified:
  - src/components/PropertyCard.tsx
  - src/app/properties/[slug]/PropertyDetailClient.tsx
  - next.config.ts
autonomous: true

must_haves:
  truths:
    - "PropertyCard uses next/image instead of raw img tags"
    - "Build completes successfully with zero errors"
    - "Static generation works for property and neighborhood pages"
    - "Site is ready for Vercel deployment"
  artifacts:
    - path: "src/components/PropertyCard.tsx"
      provides: "Property card with optimized images"
      contains: "next/image"
    - path: "next.config.ts"
      provides: "Production-ready Next.js config"
  key_links:
    - from: "src/components/PropertyCard.tsx"
      to: "next/image"
      via: "Image component import"
      pattern: "import Image from.*next/image"
---

<objective>
Optimize images, verify SSG works, and ensure the build is clean for Vercel deployment.

Purpose: SEO-03 (SSG), SEO-04 (image optimization), SEO-05 (Core Web Vitals), DEPLOY-01, DEPLOY-02. This is the final pre-launch verification and optimization pass.

Output: Optimized images via next/image, verified SSG, clean production build ready for Vercel deploy.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/components/PropertyCard.tsx
@src/app/properties/[slug]/PropertyDetailClient.tsx
@next.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert PropertyCard to use next/image</name>
  <files>src/components/PropertyCard.tsx</files>
  <action>
    In `src/components/PropertyCard.tsx`:
    - Add `import Image from "next/image"` at the top
    - Replace the raw `<img>` tag (currently using eslint-disable) with Next.js `<Image>` component:
      - `src={property.images[0]}`
      - `alt={property.address}`
      - `fill` prop (since it's inside a relative container with aspect-[4/3])
      - `className="object-cover transition-transform duration-500 group-hover:scale-105"`
      - `sizes="(max-width: 768px) 100vw, (max-width: 1200px) 50vw, 33vw"`
      - `loading="lazy"` (below-fold cards)
    - Remove the eslint-disable comment for @next/next/no-img-element

    Also check `src/app/properties/[slug]/PropertyDetailClient.tsx` for any raw `<img>` tags in the property detail gallery. If found, convert those to next/image as well using the same pattern (fill + sizes). If the detail client uses complex gallery/lightbox logic where next/image would break functionality, leave it and add a comment explaining why.
  </action>
  <verify>Run `npx next build 2>&1 | tail -30` to confirm build succeeds. Run `grep -rn "eslint-disable.*no-img-element" src/` to confirm no more disabled img warnings (or document any intentional exceptions).</verify>
  <done>PropertyCard renders images through next/image with lazy loading and responsive sizing. No unnecessary raw img tags remain.</done>
</task>

<task type="auto">
  <name>Task 2: Verify SSG, clean build, and deployment readiness</name>
  <files>next.config.ts</files>
  <action>
    Run a full production build and verify:

    1. **SSG verification**: Run `npx next build` and check the build output for:
       - Property pages (`/properties/[slug]`) show as static (SSG) with filled circles
       - Neighborhood pages (`/neighborhoods/[slug]`) show as static (SSG) with filled circles
       - If any show as dynamic (lambda), investigate and fix (likely missing generateStaticParams)

    2. **Build health**: Confirm zero errors and zero warnings (or only acceptable warnings)

    3. **Next.js config**: Review `next.config.ts` for production readiness:
       - Unsplash remote pattern is already configured (good)
       - Do NOT add `output: "standalone"` (not needed for Vercel)
       - Ensure no development-only settings leak into production

    4. **Deployment prep**: Verify these files exist and are correct:
       - `.vercelignore` exists (already has `.beads`)
       - `package.json` has correct `build` script
       - No `.env` files in git (check `.gitignore`)

    If any SSG pages are incorrectly dynamic, fix by adding `generateStaticParams` or removing dynamic API usage. Do NOT change pages that are intentionally dynamic (e.g., pages with search params).

    Note: Actual Vercel deployment (DEPLOY-01, DEPLOY-02) requires the user to run `vercel` CLI or connect the repo via Vercel dashboard. This task ensures the codebase is deployment-ready. The user will handle the actual deploy.
  </action>
  <verify>Run `npx next build` and capture full output. Verify: (1) exit code 0, (2) property and neighborhood slugs appear as static routes, (3) no TypeScript errors. Run `ls -la .next/` to confirm build artifacts exist.</verify>
  <done>Production build succeeds with SSG for property and neighborhood pages. Zero build errors. Codebase is deployment-ready for Vercel.</done>
</task>

</tasks>

<verification>
- `npx next build` exits 0 with no errors
- Build output shows property/neighborhood pages as static (SSG)
- No raw `<img>` tags remain (all converted to next/image)
- `.vercelignore` and build config are production-ready
</verification>

<success_criteria>
- SEO-03 COMPLETE: Area and property pages are statically generated
- SEO-04 COMPLETE: Images optimized via next/image
- SEO-05 PARTIAL: CLS/LCP improved via image optimization and SSG (full measurement requires live site)
- DEPLOY-01 READY: Build succeeds, codebase ready for `vercel deploy`
- DEPLOY-02 READY: Standard Vercel setup supports preview deployments automatically
</success_criteria>

<output>
After completion, create `.planning/phases/09-seo-performance-deploy/09-03-SUMMARY.md`
</output>
