---
phase: 09-seo-performance-deploy
plan: 01
type: execute
wave: 1
depends_on: []
status: COMPLETED
files_modified:
  - src/app/sitemap.ts
  - src/app/robots.ts
  - src/app/(site)/page.tsx
  - src/app/(site)/about/page.tsx
  - src/app/(site)/contact/page.tsx
  - src/app/(site)/sell/page.tsx
  - src/app/(site)/join/page.tsx
  - src/app/(site)/book-tour/page.tsx
  - src/app/(site)/neighborhoods/[slug]/page.tsx
  - src/app/(site)/agents/[slug]/page.tsx
  - src/app/properties/page.tsx
  - src/app/properties/[slug]/page.tsx
  - src/app/layout.tsx
autonomous: true

must_haves:
  truths:
    - "Every page has a unique title tag and meta description"
    - "Property detail pages include JSON-LD RealEstateListing structured data"
    - "Organization JSON-LD is present on the root layout"
    - "sitemap.xml includes all static pages, property pages, neighborhood pages, and agent pages"
    - "robots.txt allows crawling of all public pages and disallows /api/ and /proposal/"
    - "All pages have Open Graph tags (og:title, og:description, og:image)"
  artifacts:
    - path: "src/app/sitemap.ts"
      provides: "Dynamic sitemap with all routes"
      exports: ["default"]
    - path: "src/app/robots.ts"
      provides: "Robots.txt configuration"
      exports: ["default"]
    - path: "src/app/layout.tsx"
      provides: "Root layout with Organization JSON-LD and default OG metadata"
      contains: "OrganizationJsonLd"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/JsonLd.tsx"
      via: "OrganizationJsonLd import"
      pattern: "OrganizationJsonLd"
    - from: "src/app/properties/[slug]/page.tsx"
      to: "src/components/JsonLd.tsx"
      via: "RealEstateListingJsonLd import"
      pattern: "RealEstateListingJsonLd"
    - from: "src/app/sitemap.ts"
      to: "src/data/properties.ts"
      via: "property data import for dynamic routes"
      pattern: "import.*properties"
---

<objective>
Ensure every page has proper SEO metadata, JSON-LD structured data is in place, sitemap and robots.txt are complete, and Open Graph tags are set.

Purpose: SEO optimization is the final polish that ensures the site ranks for Philadelphia real estate keywords. Every page needs unique metadata, property pages need structured data for rich snippets, and the sitemap ensures complete crawl coverage.

Output: All pages have metadata, JSON-LD is verified, sitemap/robots are complete, OG tags are present.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/app/sitemap.ts
@src/app/robots.ts
@src/app/layout.tsx
@src/components/JsonLd.tsx
@src/data/properties.ts
@src/data/neighborhoods.ts
@src/data/agents.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and add metadata to all pages missing it</name>
  <files>src/app/(site)/contact/page.tsx, src/app/(site)/sell/page.tsx, src/app/(site)/join/page.tsx, src/app/(site)/book-tour/page.tsx, src/app/(site)/neighborhoods/[slug]/page.tsx, src/app/(site)/agents/[slug]/page.tsx, src/app/properties/page.tsx, src/app/properties/[slug]/page.tsx, src/app/layout.tsx</files>
  <action>
  Audit EVERY page.tsx in the app for metadata exports. Pages that already have metadata (buyers-guide, sellers-guide, market-insights, why-join, about, agents/page, neighborhoods/page, homepage, privacy, terms) should be left alone.

  **Pages known to be missing metadata** (they are "use client" or have metadata in layout.tsx instead):

  For pages that are "use client" and cannot export metadata directly, check if their parent directory has a layout.tsx with metadata. If so, verify it has proper title and description. If not, create a layout.tsx with metadata.

  Specific metadata to add/verify:

  | Page | Title | Description |
  |------|-------|-------------|
  | /properties | "Philadelphia Properties for Sale - Tauro Realty" | "Browse luxury homes and properties..." |
  | /properties/[slug] | Dynamic: "{address} - Tauro Realty" | Dynamic: property description first 155 chars |
  | /agents/[slug] | Dynamic: "{name} - Tauro Realty Agent" | Dynamic: agent bio first 155 chars |
  | /neighborhoods/[slug] | Dynamic: "{name} Homes for Sale - Tauro Realty" | Dynamic: neighborhood description |
  | /contact | "Contact Us - Tauro Realty" | "Get in touch with Tauro Realty..." |
  | /sell | "Sell Your Home - Tauro Realty" | "List your Philadelphia home with Tauro..." |
  | /join | "Join Our Team - Tauro Realty" | "Apply to join Tauro Realty..." |
  | /book-tour | "Book a Tour - Tauro Realty" | "Schedule a property tour..." |

  For dynamic routes ([slug] pages), use `generateMetadata` function that reads the data and returns metadata. If the page is "use client", the metadata must go in a layout.tsx or a separate server-component wrapper.

  For the properties/[slug]/page.tsx which already has generateStaticParams, add a `generateMetadata` function if not present. Same for neighborhoods/[slug] and agents/[slug].

  **Open Graph tags**: In src/app/layout.tsx, add default `openGraph` to the root metadata that includes:
  ```typescript
  openGraph: {
    type: "website",
    siteName: "Tauro Realty",
    locale: "en_US",
    url: "https://tauro.realty",
    images: [{ url: "https://tauro.realty/og-image.jpg", width: 1200, height: 630 }],
  }
  ```
  This provides defaults. Individual pages will override title/description via their own metadata.

  Do NOT touch pages that already have correct metadata. Only add what's missing.
  </action>
  <verify>
  Run a comprehensive check:
  ```bash
  echo "=== Pages with metadata ===" && grep -rl "export const metadata\|generateMetadata\|export const metadata" src/app/ --include="*.tsx" | sort
  echo "=== Pages without metadata ===" && for f in $(find src/app -name "page.tsx"); do grep -qE "metadata|generateMetadata" "$f" || echo "$f"; done
  echo "=== Layouts with metadata ===" && grep -rl "metadata" src/app/ --include="layout.tsx" | sort
  ```
  Every page.tsx should either export metadata/generateMetadata directly, or have a sibling layout.tsx that does.
  </verify>
  <done>
  Every page has unique title and meta description (either via page.tsx metadata export, generateMetadata function, or layout.tsx). Root layout has default Open Graph tags. Dynamic routes use generateMetadata for unique titles.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify and enhance sitemap, robots.txt, and JSON-LD</name>
  <files>src/app/sitemap.ts, src/app/robots.ts</files>
  <action>
  **Sitemap (src/app/sitemap.ts):**
  The sitemap already exists and includes static pages, property pages, neighborhood pages, and agent pages. Verify completeness:

  - Static pages must include ALL routes: /, /properties, /agents, /join, /contact, /book-tour, /sell, /neighborhoods, /buyers-guide, /sellers-guide, /market-insights, /faq, /home-value, /why-join, /about, /privacy, /terms
  - Property pages: all property slugs from properties data
  - Neighborhood pages: all neighborhood slugs from neighborhoods data
  - Agent pages: all agent slugs from agents data
  - Priority: homepage = 1.0, property detail = 0.9, neighborhood = 0.8, other = 0.7
  - Do NOT include /proposal in sitemap (it's a private client page)

  If the current sitemap is missing any pages (check the static pages array), add them. If it's complete, leave it alone.

  **Robots.txt (src/app/robots.ts):**
  Already exists and disallows /api/ and /proposal/. Verify:
  - Allow: / (all public pages)
  - Disallow: /api/, /proposal/
  - Sitemap: https://tauro.realty/sitemap.xml
  If correct, leave it alone.

  **JSON-LD verification:**
  - Verify src/components/JsonLd.tsx has both OrganizationJsonLd and RealEstateListingJsonLd (already confirmed -- both exist)
  - Verify OrganizationJsonLd is rendered in src/app/layout.tsx (already confirmed)
  - Verify RealEstateListingJsonLd is rendered in src/app/properties/[slug]/page.tsx (already confirmed)
  - No changes needed if all are in place

  This task is primarily verification with minor additions to sitemap if routes are missing.
  </action>
  <verify>
  `npx next build 2>&1 | tail -20` compiles clean. Verify sitemap completeness:
  ```bash
  grep -c "url:" src/app/sitemap.ts  # should reference all static routes
  grep "privacy\|terms\|faq\|home-value\|why-join" src/app/sitemap.ts  # verify newer pages included
  ```
  Verify robots: `grep "disallow" src/app/robots.ts`
  Verify JSON-LD: `grep "RealEstateListingJsonLd" src/app/properties/\[slug\]/page.tsx`
  </verify>
  <done>
  Sitemap includes all public routes (static + dynamic). Robots.txt correctly allows public pages and blocks /api/ and /proposal/. JSON-LD structured data is on property detail pages and Organization schema is on root layout.
  </done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with no errors
- Every page.tsx has metadata (directly or via layout.tsx)
- `curl localhost:3000/sitemap.xml` returns all routes (after dev server start)
- `curl localhost:3000/robots.txt` shows correct allow/disallow rules
- Property pages render JSON-LD script tags in HTML
- Root layout renders Organization JSON-LD
</verification>

<success_criteria>
- SEO-01: Every page has unique title tag and meta description
- SEO-02: Property pages have JSON-LD RealEstateListing structured data
- Open Graph tags present via root layout defaults
- Sitemap covers all public routes
- Robots.txt blocks /api/ and /proposal/
</success_criteria>

<output>
After completion, create `.planning/phases/09-seo-performance-deploy/09-01-SUMMARY.md`
</output>
