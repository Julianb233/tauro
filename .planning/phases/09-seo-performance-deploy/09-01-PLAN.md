---
phase: 09-seo-performance-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/sitemap.ts
  - src/app/robots.ts
  - src/app/(site)/contact/layout.tsx
  - src/app/(site)/faq/layout.tsx
  - src/app/(site)/sell/layout.tsx
  - src/app/(site)/book-tour/layout.tsx
  - src/app/(site)/home-value/layout.tsx
  - src/app/(site)/join/layout.tsx
  - src/app/properties/layout.tsx
  - src/app/(site)/agents/[slug]/layout.tsx
autonomous: true

must_haves:
  truths:
    - "Every page has a unique title tag and meta description"
    - "Search engines can discover all pages via /sitemap.xml"
    - "robots.txt allows crawling of all public pages"
  artifacts:
    - path: "src/app/sitemap.ts"
      provides: "Dynamic XML sitemap"
      contains: "MetadataRoute"
    - path: "src/app/robots.ts"
      provides: "robots.txt configuration"
      contains: "MetadataRoute"
  key_links:
    - from: "src/app/sitemap.ts"
      to: "src/data/properties.ts"
      via: "import properties for dynamic URLs"
      pattern: "import.*properties"
---

<objective>
Add sitemap.ts, robots.ts, and fill metadata gaps on all pages missing title/description.

Purpose: SEO-01 (unique metadata per page) is the foundation for search visibility. sitemap.xml and robots.txt are table-stakes for crawlability.

Output: Every route has unique metadata, sitemap.xml lists all pages, robots.txt is properly configured.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/app/layout.tsx
@src/data/properties.ts
@src/data/neighborhoods.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create sitemap.ts and robots.ts</name>
  <files>src/app/sitemap.ts, src/app/robots.ts</files>
  <action>
    Create `src/app/sitemap.ts` using Next.js MetadataRoute.Sitemap:
    - Base URL: `https://tauro.com` (placeholder, will be overridden by env)
    - Static routes: /, /properties, /agents, /neighborhoods, /contact, /book-tour, /sell, /home-value, /faq, /buyers-guide, /sellers-guide, /market-insights, /join, /why-join
    - Dynamic property routes: import `properties` from `@/data/properties` and map to `/properties/${slug}`
    - Dynamic neighborhood routes: import neighborhood slugs from `@/data/neighborhoods` and map to `/neighborhoods/${slug}`
    - Dynamic agent routes: import agents from `@/data/agents` and map to `/agents/${slug}`
    - Set changeFrequency: 'weekly' for dynamic pages, 'monthly' for static
    - Set priority: 1.0 for homepage, 0.9 for properties/neighborhoods, 0.7 for static pages

    Create `src/app/robots.ts` using MetadataRoute.Robots:
    - Allow all user agents on all paths
    - Disallow /api/* and /proposal/*
    - Include sitemap URL: `https://tauro.com/sitemap.xml`
  </action>
  <verify>Run `npx next build 2>&1 | tail -20` and confirm sitemap.ts and robots.ts are included in the build output without errors. Also: `grep -r "MetadataRoute" src/app/sitemap.ts src/app/robots.ts` confirms proper typing.</verify>
  <done>sitemap.xml and robots.txt are auto-generated by Next.js at build time with all routes listed.</done>
</task>

<task type="auto">
  <name>Task 2: Add missing metadata to all pages</name>
  <files>
    src/app/(site)/contact/layout.tsx,
    src/app/(site)/faq/layout.tsx,
    src/app/(site)/sell/layout.tsx,
    src/app/(site)/book-tour/layout.tsx,
    src/app/(site)/home-value/layout.tsx,
    src/app/(site)/join/layout.tsx,
    src/app/properties/layout.tsx,
    src/app/(site)/agents/[slug]/layout.tsx
  </files>
  <action>
    The following pages are "use client" and cannot export metadata directly. For each, create a sibling `layout.tsx` (server component) that exports metadata. If a layout.tsx already exists at that route, add the metadata export to it instead.

    Pages missing metadata (all are client components):

    1. **contact** - Create `src/app/(site)/contact/layout.tsx`:
       title: "Contact Us", description: "Get in touch with Tauro's Philadelphia real estate team. Schedule a consultation, ask about listings, or start your home search today."

    2. **faq** - Create `src/app/(site)/faq/layout.tsx`:
       title: "Frequently Asked Questions", description: "Answers to common questions about buying, selling, and investing in Philadelphia real estate with Tauro."

    3. **sell** - Create `src/app/(site)/sell/layout.tsx`:
       title: "Sell Your Home", description: "List your Philadelphia home with Tauro. Get expert pricing, professional marketing, and a dedicated agent to maximize your sale."

    4. **book-tour** - Create `src/app/(site)/book-tour/layout.tsx`:
       title: "Book a Tour", description: "Schedule a private showing of any Philadelphia property. Choose your preferred date and time, and a Tauro agent will be there."

    5. **home-value** - Create `src/app/(site)/home-value/layout.tsx`:
       title: "What's My Home Worth?", description: "Get a free home valuation for your Philadelphia property. Our agents provide accurate market analysis based on local expertise."

    6. **join** - Create `src/app/(site)/join/layout.tsx`:
       title: "Join Tauro", description: "Apply to join Tauro's team of Philadelphia real estate professionals. Competitive splits, mentorship, and cutting-edge tools."

    7. **properties listing** - Create `src/app/properties/layout.tsx`:
       title: "Properties for Sale", description: "Browse luxury homes and properties for sale in Philadelphia. Filter by price, neighborhood, beds, and more."

    8. **agents/[slug]** - This page is "use client" and uses useParams. Create `src/app/(site)/agents/[slug]/layout.tsx` with a generic metadata:
       title: "Agent Profile", description: "Meet your Tauro real estate agent. View their listings, experience, and specialties in Philadelphia real estate."

    Each layout.tsx should be minimal: export metadata + a children passthrough layout function. Use the `%s | Tauro` template pattern from root layout (just export title as string, the root template will append " | Tauro").

    Example pattern:
    ```tsx
    import type { Metadata } from "next";

    export const metadata: Metadata = {
      title: "Contact Us",
      description: "Get in touch with Tauro's Philadelphia real estate team...",
    };

    export default function ContactLayout({ children }: { children: React.ReactNode }) {
      return children;
    }
    ```
  </action>
  <verify>Run `npx next build 2>&1 | tail -30` and confirm zero errors. Grep for metadata exports: `grep -rn "export const metadata" src/app/ --include="*.tsx" | wc -l` should show all pages covered (existing + new = 15+ files with metadata).</verify>
  <done>Every route in the app has a unique title and meta description. No page inherits only the default root metadata.</done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- `/sitemap.xml` route exists in build output
- `/robots.txt` route exists in build output
- All 20+ pages have unique metadata (title + description)
</verification>

<success_criteria>
- SEO-01 COMPLETE: Every page has unique title tag and meta description
- sitemap.xml lists all static and dynamic routes
- robots.txt properly configured with sitemap reference
</success_criteria>

<output>
After completion, create `.planning/phases/09-seo-performance-deploy/09-01-SUMMARY.md`
</output>
