---
status: COMPLETED
phase: 09-seo-performance-deploy
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/PropertyCard.tsx
  - src/components/AgentCard.tsx
  - src/components/ImageGallery.tsx
  - src/app/(site)/neighborhoods/page.tsx
  - src/app/(site)/neighborhoods/[slug]/page.tsx
  - src/app/(site)/sell/page.tsx
  - src/app/(site)/home-value/page.tsx
  - src/app/(site)/book-tour/page.tsx
  - src/app/(site)/agents/[slug]/AgentProfileClient.tsx
  - src/app/properties/[slug]/PropertyDetailClient.tsx
autonomous: true

must_haves:
  truths:
    - "All images on the site use Next.js Image component instead of raw <img> tags"
    - "Images have explicit width/height or fill prop to prevent CLS"
    - "Hero/LCP images use priority prop for preloading"
  artifacts:
    - path: "src/components/PropertyCard.tsx"
      provides: "Optimized property card image"
      contains: "next/image"
    - path: "src/components/AgentCard.tsx"
      provides: "Optimized agent card image"
      contains: "next/image"
    - path: "src/components/ImageGallery.tsx"
      provides: "Optimized gallery images with priority hero"
      contains: "next/image"
    - path: "src/app/(site)/neighborhoods/page.tsx"
      provides: "Optimized neighborhood grid images"
      contains: "next/image"
    - path: "src/app/(site)/neighborhoods/[slug]/page.tsx"
      provides: "Optimized neighborhood hero image"
      contains: "next/image"
  key_links:
    - from: "all converted files"
      to: "next/image"
      via: "Image import"
      pattern: "from \"next/image\""
---

<objective>
Convert all 11 raw `<img>` tags across 10 files to Next.js `<Image>` component for automatic image optimization.

Purpose: Satisfies SEO-04 (image optimization via Next/Image). Provides automatic WebP/AVIF conversion, responsive srcsets, lazy loading, and explicit dimensions to prevent CLS — directly improving Core Web Vitals (SEO-05).
Output: Zero raw `<img>` tags remaining in the codebase. All images use next/image.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@next.config.ts

Key context:
- next.config.ts already has remotePatterns for images.unsplash.com — no config changes needed
- The homepage (src/app/(site)/page.tsx) already uses next/image — follow the same patterns
- There are exactly 11 `<img>` tags across 10 files that need converting:
  - src/components/PropertyCard.tsx (1 img)
  - src/components/AgentCard.tsx (1 img)
  - src/components/ImageGallery.tsx (2 imgs — hero + thumbnails)
  - src/app/(site)/neighborhoods/page.tsx (1 img)
  - src/app/(site)/neighborhoods/[slug]/page.tsx (1 img)
  - src/app/(site)/sell/page.tsx (1 img)
  - src/app/(site)/home-value/page.tsx (1 img)
  - src/app/(site)/book-tour/page.tsx (1 img)
  - src/app/(site)/agents/[slug]/AgentProfileClient.tsx (1 img — NOTE: this is the client component, not page.tsx)
  - src/app/properties/[slug]/PropertyDetailClient.tsx (1 img)
- For images inside containers with CSS-defined dimensions, use `fill` prop with `className="object-cover"` and ensure parent has `relative` positioning
- For images with known fixed dimensions, use explicit `width` and `height` props
- Add `sizes` prop for responsive images to help browser select correct srcset
- Add `priority` only to above-the-fold hero/LCP images
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert card and list component images to next/image</name>
  <files>
    src/components/PropertyCard.tsx
    src/components/AgentCard.tsx
    src/components/ImageGallery.tsx
    src/app/(site)/neighborhoods/page.tsx
    src/app/(site)/neighborhoods/[slug]/page.tsx
  </files>
  <action>
In each file, replace `<img>` with `<Image>` from "next/image". Add `import Image from "next/image"` at the top of each file.

**PropertyCard.tsx** — Card image in a container with `relative aspect-[4/3]`. Use `fill`:
- Replace `<img src={...} alt={...} className="...">` with `<Image src={...} alt={...} fill className="object-cover transition-transform duration-500 group-hover:scale-105" sizes="(max-width: 768px) 100vw, (max-width: 1200px) 50vw, 33vw" />`
- Parent div already has `relative` and `aspect-[4/3]` — no parent changes needed
- Remove the eslint-disable comment

**AgentCard.tsx** — Agent headshot in a container. Use `fill` with `object-cover`:
- Same pattern — `fill` + `object-cover` + `sizes="(max-width: 768px) 50vw, 25vw"`
- Ensure parent has `relative` positioning
- Remove the eslint-disable comment

**ImageGallery.tsx** — Has 2 `<img>` tags (main gallery image + thumbnails). This is the LCP element on property detail pages:
- Main/hero image (the large one inside the button): `<Image src={...} alt={...} fill className="object-cover" priority sizes="(max-width: 768px) 100vw, 66vw" />` — use `priority` because this is the LCP element
- Move the aspect-ratio classes (`aspect-[16/9] max-h-[500px] lg:aspect-[21/9]`) from the old `<img>` to the parent button element
- Thumbnail strip images: `<Image src={...} alt={...} fill className="object-cover" sizes="80px" />` — no priority, parent already has `relative h-16 w-24`
- Remove both eslint-disable comments

**neighborhoods/page.tsx** — Neighborhood grid card images. Use `fill`:
- `<Image src={...} alt={...} fill className="object-cover" sizes="(max-width: 768px) 100vw, (max-width: 1200px) 50vw, 33vw" />`
- Ensure parent has `relative` + defined height via aspect-ratio
- Remove eslint-disable comment

**neighborhoods/[slug]/page.tsx** — Neighborhood hero image. Use `fill` + `priority`:
- `<Image src={...} alt={...} fill className="object-cover" priority sizes="100vw" />` — priority because it's the hero/LCP
- Ensure parent has `relative` + defined height
- Remove eslint-disable comment

IMPORTANT: Do NOT pass `loading="lazy"` — Next/Image handles this automatically.
  </action>
  <verify>
Run `grep -rn "<img" src/components/PropertyCard.tsx src/components/AgentCard.tsx src/components/ImageGallery.tsx` — should return ZERO matches.
Run `grep -rn "next/image" src/components/PropertyCard.tsx src/components/AgentCard.tsx src/components/ImageGallery.tsx` — should show imports in all 3.
  </verify>
  <done>5 files with 7 img tags converted to Next/Image. Hero images marked with priority. All have sizes prop.</done>
</task>

<task type="auto">
  <name>Task 2: Convert remaining page images and verify zero raw img tags</name>
  <files>
    src/app/(site)/sell/page.tsx
    src/app/(site)/home-value/page.tsx
    src/app/(site)/book-tour/page.tsx
    src/app/(site)/agents/[slug]/AgentProfileClient.tsx
    src/app/properties/[slug]/PropertyDetailClient.tsx
  </files>
  <action>
Convert the remaining 5 files. All are client components ("use client") — next/image works fine in client components.

**sell/page.tsx** — Hero/decorative image. Use `fill`:
- `<Image src={...} alt={...} fill className="object-cover" sizes="(max-width: 768px) 100vw, 50vw" />`
- Ensure parent has `relative` positioning and defined dimensions
- Remove eslint-disable comment

**home-value/page.tsx** — Hero/decorative image. Use `fill`:
- `<Image src={...} alt={...} fill className="object-cover" sizes="(max-width: 768px) 100vw, 50vw" />`
- Parent needs `relative` + defined dimensions
- Remove eslint-disable comment

**book-tour/page.tsx** — Property selector thumbnail:
- If in a small container (thumbnail): use `width={80} height={60}` with `className="object-cover rounded"`
- If in a larger container: use `fill` pattern instead
- Adapt based on the existing img's context
- Remove eslint-disable comment

**AgentProfileClient.tsx** — Property listing images in the agent profile:
- `<Image src={...} alt={...} fill className="object-cover" sizes="(max-width: 768px) 100vw, 33vw" />`
- Ensure parent has `relative` + defined dimensions
- Remove eslint-disable comment

**PropertyDetailClient.tsx** — Similar property card images:
- `<Image src={...} alt={...} fill className="object-cover" sizes="(max-width: 768px) 100vw, 33vw" />`
- Ensure parent has `relative` + defined dimensions
- Remove eslint-disable comment

After all conversions, run: `grep -rn "<img" src/` — must return ZERO matches.

If the build has issues, fix them. Common problems:
- Missing `relative` on parent div when using `fill`
- Image component in a parent without defined height when using `fill`
- Need to remove `style` props that set width/height (convert to width/height props instead)
  </action>
  <verify>
Run `grep -rn "<img" src/` — should return ZERO matches across the entire src directory.
Run `npx next build 2>&1 | tail -30` — build should succeed.
  </verify>
  <done>All 10 files converted. Zero raw img tags remain in src/. All images use Next/Image with proper sizing, fill, sizes, and priority attributes.</done>
</task>

</tasks>

<verification>
1. `grep -rn "<img" src/` returns zero matches
2. All 10 converted files import from "next/image"
3. Hero images (ImageGallery main, neighborhood/[slug] hero) have `priority` prop
4. Build compiles without errors: `npx next build 2>&1 | tail -20`
</verification>

<success_criteria>
- All raw `<img>` tags replaced with Next.js `<Image>` component (SEO-04 complete)
- Hero/LCP images marked with `priority` for preloading (contributes to SEO-05 LCP target)
- All images have explicit dimensions (width/height or fill) to prevent CLS (contributes to SEO-05 CLS target)
- All images have `sizes` prop for optimal responsive loading
- Build passes without new errors
</success_criteria>

<output>
After completion, create `.planning/phases/09-seo-performance-deploy/09-04-SUMMARY.md`
</output>
