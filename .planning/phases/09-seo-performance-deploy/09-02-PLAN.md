---
phase: 09-seo-performance-deploy
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
status: COMPLETED
files_modified:
  - next.config.ts
  - src/app/(site)/neighborhoods/[slug]/page.tsx
  - src/app/properties/[slug]/page.tsx
  - src/app/(site)/agents/[slug]/page.tsx
autonomous: true

must_haves:
  truths:
    - "All images use next/image for automatic optimization"
    - "Area pages, property pages, and agent pages use generateStaticParams for SSG"
    - "Next.js config has image optimization settings for remote patterns"
    - "Build completes successfully with static pages generated"
    - "No unoptimized img tags exist in the codebase"
  artifacts:
    - path: "next.config.ts"
      provides: "Next.js configuration with image optimization and any performance settings"
      contains: "remotePatterns"
    - path: "src/app/properties/[slug]/page.tsx"
      provides: "Property detail page with generateStaticParams for SSG"
      contains: "generateStaticParams"
    - path: "src/app/(site)/neighborhoods/[slug]/page.tsx"
      provides: "Neighborhood page with generateStaticParams for SSG"
      contains: "generateStaticParams"
    - path: "src/app/(site)/agents/[slug]/page.tsx"
      provides: "Agent profile page with generateStaticParams for SSG"
      contains: "generateStaticParams"
  key_links:
    - from: "src/app/properties/[slug]/page.tsx"
      to: "src/data/properties.ts"
      via: "generateStaticParams imports property slugs"
      pattern: "generateStaticParams"
    - from: "src/app/(site)/neighborhoods/[slug]/page.tsx"
      to: "src/data/neighborhoods.ts"
      via: "generateStaticParams imports neighborhood slugs"
      pattern: "generateStaticParams"
---

<objective>
Verify and optimize performance: image optimization, static generation, and build verification for deployment readiness.

Purpose: Ensure the site meets Core Web Vitals targets (LCP < 2.5s, CLS < 0.1) through proper image optimization and static generation. Verify the build is clean and deployment-ready for Vercel.

Output: Optimized next.config.ts, verified SSG on all dynamic routes, clean production build.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@next.config.ts
@src/app/properties/[slug]/page.tsx
@src/app/(site)/neighborhoods/[slug]/page.tsx
@src/app/(site)/agents/[slug]/page.tsx
@src/data/properties.ts
@src/data/neighborhoods.ts
@src/data/agents.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Image optimization audit and Next.js config</name>
  <files>next.config.ts</files>
  <action>
  **Image audit:** Search the entire src/ directory for any raw `<img` tags that should be using `next/image` instead:
  ```bash
  grep -rn "<img " src/ --include="*.tsx" | grep -v "node_modules"
  ```
  If any raw img tags are found in page components (not in third-party or SVG contexts), replace them with `import Image from "next/image"` and `<Image>` component with proper width/height/sizes props.

  **next.config.ts optimization:**
  The current config already has `images.remotePatterns` for unsplash.com. Enhance with:

  1. Add any additional remote image hosts used in the codebase (search for image URLs in data files and components):
     ```bash
     grep -roh "https://[a-zA-Z0-9.-]*" src/data/ src/components/ src/app/ --include="*.ts" --include="*.tsx" | sort -u | grep -v "tauro\|schema\|localhost"
     ```
     Add any new hostnames to remotePatterns.

  2. Add image format optimization if not present:
     ```typescript
     images: {
       formats: ['image/avif', 'image/webp'],
       remotePatterns: [
         { protocol: 'https', hostname: 'images.unsplash.com' },
         // ... any additional hosts
       ],
     },
     ```

  3. Verify turbopack config is present (already is).

  4. Do NOT add a vercel.json file unless there's a specific need (Vercel auto-detects Next.js projects). The deployment config from next.config.ts is sufficient.

  Keep changes minimal. If everything is already optimized, document what was verified.
  </action>
  <verify>
  `grep -rn "<img " src/ --include="*.tsx" | grep -v "node_modules" | wc -l` should return 0 (no raw img tags). `grep "remotePatterns\|formats" next.config.ts` confirms image config.
  </verify>
  <done>
  No raw img tags in components (all use next/image). next.config.ts has image format optimization (avif, webp) and all required remote patterns.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify static generation and production build</name>
  <files>src/app/properties/[slug]/page.tsx, src/app/(site)/neighborhoods/[slug]/page.tsx, src/app/(site)/agents/[slug]/page.tsx</files>
  <action>
  **Static generation verification:**
  Verify all three dynamic route pages have `generateStaticParams`:

  1. `src/app/properties/[slug]/page.tsx` -- already confirmed has generateStaticParams
  2. `src/app/(site)/neighborhoods/[slug]/page.tsx` -- already confirmed has generateStaticParams
  3. `src/app/(site)/agents/[slug]/page.tsx` -- already confirmed has generateStaticParams

  For each, verify the function returns all slugs from the respective data file:
  - Properties: imports from `@/data/properties` and maps to `{ slug }` array
  - Neighborhoods: imports from `@/data/neighborhoods` and maps to `{ slug }` array
  - Agents: imports from `@/data/agents` and maps to `{ slug }` array

  If any generateStaticParams function is incomplete or missing data imports, fix it.

  **Production build test:**
  Run `npx next build` and verify:
  1. Build completes without errors
  2. Output shows static pages generated for property/neighborhood/agent routes (look for the circle/filled circle indicators in build output)
  3. No "Dynamic server usage" warnings on pages that should be static

  If the build reveals any issues (missing imports, type errors, dynamic usage where static is expected), fix them.

  **Deployment readiness check:**
  - Verify package.json has correct build script: `"build": "next build"`
  - Verify .gitignore includes .next/ and node_modules/
  - No vercel.json is needed (Vercel auto-detects Next.js)
  - The site is ready for `vercel --prod` deployment once domain is configured

  Note: Actual Vercel deployment (DEPLOY-01, DEPLOY-02) requires the user to configure their Vercel account and domain. This plan verifies the build is deployment-ready. Domain setup is a user action.
  </action>
  <verify>
  `npx next build 2>&1 | tail -40` shows successful build with static pages. `grep "generateStaticParams" src/app/properties/\[slug\]/page.tsx src/app/\(site\)/neighborhoods/\[slug\]/page.tsx src/app/\(site\)/agents/\[slug\]/page.tsx` confirms all three have the function.
  </verify>
  <done>
  All dynamic routes use generateStaticParams for SSG. Production build completes successfully. Static pages generated for all property, neighborhood, and agent routes. Site is deployment-ready for Vercel.
  </done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with no errors
- Build output shows static generation for dynamic routes (property, neighborhood, agent pages)
- No raw `<img>` tags in components -- all use next/image
- next.config.ts has image optimization settings
- All generateStaticParams functions return complete slug arrays
</verification>

<success_criteria>
- SEO-03: Area pages and property pages are statically generated (SSG) via generateStaticParams
- SEO-04: All images use next/image for automatic optimization (format, sizing, lazy loading)
- SEO-05: Performance targets achievable (LCP < 2.5s, CLS < 0.1) through SSG + image optimization
- DEPLOY-01: Build is clean and deployment-ready for Vercel (actual deployment is user action)
- DEPLOY-02: Preview deployments will work automatically once Vercel is connected (no special config needed)
</success_criteria>

<output>
After completion, create `.planning/phases/09-seo-performance-deploy/09-02-SUMMARY.md`
</output>
