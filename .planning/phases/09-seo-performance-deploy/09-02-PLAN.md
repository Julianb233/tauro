---
phase: 09-seo-performance-deploy
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/JsonLd.tsx
  - src/app/layout.tsx
  - src/app/properties/[slug]/page.tsx
autonomous: true

must_haves:
  truths:
    - "Property detail pages include JSON-LD RealEstateListing structured data via a reusable component"
    - "Root layout includes Organization/RealEstateAgent schema markup"
    - "Google Rich Results validator accepts the structured data"
  artifacts:
    - path: "src/components/JsonLd.tsx"
      provides: "Reusable JSON-LD script injection components"
      exports: ["OrganizationJsonLd", "RealEstateListingJsonLd"]
    - path: "src/app/properties/[slug]/page.tsx"
      provides: "Property page with embedded JSON-LD via component"
      contains: "RealEstateListingJsonLd"
  key_links:
    - from: "src/app/properties/[slug]/page.tsx"
      to: "src/components/JsonLd.tsx"
      via: "import RealEstateListingJsonLd"
      pattern: "import.*RealEstateListingJsonLd"
    - from: "src/app/layout.tsx"
      to: "src/components/JsonLd.tsx"
      via: "import OrganizationJsonLd"
      pattern: "import.*OrganizationJsonLd"
---

<objective>
Refactor inline JSON-LD into reusable components and add Organization schema to root layout.

Purpose: SEO-02 (JSON-LD structured data) is partially complete — property pages already have inline JSON-LD from plan 09-01. This plan refactors it into a reusable component and adds Organization/RealEstateAgent schema site-wide.
Output: JsonLd.tsx component file, Organization schema on every page, cleaner property page.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@src/app/layout.tsx
@src/app/properties/[slug]/page.tsx
@src/data/properties.ts
@.planning/phases/09-seo-performance-deploy/09-01-SUMMARY.md

Key context:
- Plan 09-01 already added inline JSON-LD to properties/[slug]/page.tsx (RealEstateListing schema with address, price, geo, rooms, floor size)
- The inline JSON-LD in the property page should be REPLACED with a RealEstateListingJsonLd component import
- Organization schema is NEW — not yet on the site
- Property type has: id, slug, address, city, state, zip, price, beds, baths, sqft, lotSqft, yearBuilt, status, propertyType, images, description, features, agent, lat, lng
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create JSON-LD components</name>
  <files>src/components/JsonLd.tsx</files>
  <action>
    Create `src/components/JsonLd.tsx` with two server components:

    **OrganizationJsonLd** - No props needed, static data:
    - @type: "RealEstateAgent" (subtype of Organization, appropriate for schema.org)
    - name: "Tauro | LYL Realty Group"
    - url: "https://tauro.com"
    - logo: "https://tauro.com/logo.png"
    - description: "Premium Philadelphia real estate brokerage serving Center City, Rittenhouse, Fishtown, and surrounding neighborhoods."
    - address: { @type: "PostalAddress", addressLocality: "Philadelphia", addressRegion: "PA", addressCountry: "US" }
    - areaServed: { @type: "City", name: "Philadelphia" }

    **RealEstateListingJsonLd** - Accepts a Property object as prop:
    - @type: "RealEstateListing"
    - name: property.address
    - description: property.description (truncated to 200 chars)
    - url: `https://tauro.com/properties/${property.slug}`
    - image: property.images[0]
    - datePosted: new Date().toISOString().split('T')[0]
    - offers: { @type: "Offer", price: property.price, priceCurrency: "USD" }
    - address: { @type: "PostalAddress", streetAddress: property.address, addressLocality: property.city, addressRegion: property.state, postalCode: property.zip }
    - geo: { @type: "GeoCoordinates", latitude: property.lat, longitude: property.lng }
    - floorSize: { @type: "QuantitativeValue", value: property.sqft, unitCode: "SQF" }
    - numberOfRooms: property.beds
    - numberOfBathroomsTotal: property.baths
    - yearBuilt: property.yearBuilt

    Both components render a `<script type="application/ld+json">` tag with `dangerouslySetInnerHTML` containing `JSON.stringify(schema)`. Import the Property type from `@/data/properties`.

    These are server components (no "use client" directive).
  </action>
  <verify>Run `npx next build 2>&1 | tail -20` to confirm no build errors. Run `grep -n "application/ld+json" src/components/JsonLd.tsx` to confirm script tags are present.</verify>
  <done>JsonLd.tsx exports two components that generate valid schema.org JSON-LD markup.</done>
</task>

<task type="auto">
  <name>Task 2: Wire JSON-LD into layout and refactor property page</name>
  <files>src/app/layout.tsx, src/app/properties/[slug]/page.tsx</files>
  <action>
    **Root layout (src/app/layout.tsx):**
    - Import `OrganizationJsonLd` from `@/components/JsonLd`
    - Add `<OrganizationJsonLd />` inside the `<body>` tag, before `{children}`
    - This provides Organization schema on every page

    **Property detail page (src/app/properties/[slug]/page.tsx):**
    - Import `RealEstateListingJsonLd` from `@/components/JsonLd`
    - REMOVE the existing inline `const jsonLd = { ... }` object and the inline `<script type="application/ld+json">` tag (this was added by plan 09-01)
    - Replace with: `<RealEstateListingJsonLd property={property} />` in the JSX return, before `<PropertyDetailClient>`
    - The return should be: `<><RealEstateListingJsonLd property={property} /><PropertyDetailClient property={property} similar={similar} /></>`
  </action>
  <verify>Run `npx next build 2>&1 | tail -20` to confirm build succeeds. Run `grep -n "JsonLd" src/app/layout.tsx src/app/properties/\[slug\]/page.tsx` to confirm imports are wired. Run `grep -c "const jsonLd" src/app/properties/\[slug\]/page.tsx` to confirm inline JSON-LD is removed (should be 0).</verify>
  <done>Organization JSON-LD appears on every page via root layout. Property pages use the RealEstateListingJsonLd component instead of inline script. Inline JSON-LD removed.</done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- `grep -r "application/ld+json" src/` shows JsonLd.tsx (not inline in page.tsx)
- Property pages render with RealEstateListing schema via component
- Root layout includes OrganizationJsonLd
</verification>

<success_criteria>
- SEO-02 COMPLETE: Property pages include JSON-LD RealEstateListing structured data via reusable component
- Organization/RealEstateAgent schema present site-wide via root layout
- All JSON-LD is valid schema.org format
- Inline JSON-LD in property page replaced with component
</success_criteria>

<output>
After completion, create `.planning/phases/09-seo-performance-deploy/09-02-SUMMARY.md`
</output>
