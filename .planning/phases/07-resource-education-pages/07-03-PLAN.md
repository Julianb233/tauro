---
phase: 07-resource-education-pages
plan: 03
type: execute
wave: 1
depends_on: []
status: COMPLETED
files_modified:
  - src/app/(site)/faq/page.tsx
  - src/app/(site)/home-value/page.tsx
autonomous: true

must_haves:
  truths:
    - "Visitor can read answers to common buyer/seller questions"
    - "FAQ items expand/collapse when clicked"
    - "Visitor can enter their address on the home valuation page"
    - "Home valuation form submits to /api/leads with type seller"
    - "Form shows success confirmation after submission"
  artifacts:
    - path: "src/app/(site)/faq/page.tsx"
      provides: "FAQ page with expandable accordion"
      min_lines: 120
    - path: "src/app/(site)/home-value/page.tsx"
      provides: "Home valuation lead capture page"
      min_lines: 120
  key_links:
    - from: "src/app/(site)/home-value/page.tsx"
      to: "/api/leads"
      via: "fetch POST on form submit"
      pattern: "fetch.*api/leads"
    - from: "src/app/(site)/faq/page.tsx"
      to: "/contact"
      via: "CTA link"
      pattern: "href.*contact"
---

<objective>
Create the FAQ page with expandable accordion and the "What's My Home Worth" lead capture page.

Purpose: FAQ captures organic search traffic and reduces support inquiries. Home valuation page is a high-intent seller lead capture mechanism with a simple address-focused form.

Output: Two pages at /faq (interactive accordion) and /home-value (lead capture form).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Reference for form pattern (state management, /api/leads submission, success state):
@src/app/(site)/sell/page.tsx
@src/app/api/leads/route.ts

Reference for page layout:
@src/app/(site)/contact/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FAQ page with accordion</name>
  <files>src/app/(site)/faq/page.tsx</files>
  <action>
Create src/app/(site)/faq/page.tsx as a "use client" component (needs useState for accordion toggle).

Export metadata via a separate generateMetadata function or remove metadata export (since it is "use client", metadata must be handled differently). IMPORTANT: Since this is a client component, do NOT export metadata directly. Instead, just focus on the page content. If SEO metadata is needed, create a layout.tsx in the same folder that exports metadata: title "Frequently Asked Questions | Tauro Realty", description about common real estate questions.

Actually, the simpler approach: create the FAQ page as a server component that renders a client FaqAccordion component. Create the page at src/app/(site)/faq/page.tsx as a SERVER component that exports metadata and renders a client component for the interactive accordion.

Structure:

**src/app/(site)/faq/page.tsx** (server component):
- Export metadata: title "Frequently Asked Questions | Tauro Realty"
- Import and render FaqAccordion client component
- Hero section, FAQ section, CTA section

But to keep it to one file (simpler), use this approach instead:

**src/app/(site)/faq/page.tsx** as "use client":
- No metadata export (metadata will need a layout.tsx or just skip for now)

REVISED APPROACH - keep it simple, one file, "use client":

Create src/app/(site)/faq/page.tsx with "use client" directive.

Page structure:

1. **Hero section** - bg-near-black pb-16 pt-32. Eyebrow "FAQ" in gold. H1: "Frequently Asked Questions". Subtitle: "Answers to common questions about buying, selling, and working with Tauro."

2. **FAQ Accordion** - bg-midnight py-20. Create two categories of questions:

   **For Buyers** (5-6 questions):
   - "How much do I need for a down payment?" - Explain conventional (3-20%), FHA (3.5%), VA (0%), and PA assistance programs.
   - "How long does the home buying process take?" - Typically 30-60 days from accepted offer. Pre-approval expedites.
   - "What credit score do I need to buy a home?" - 620+ for conventional, 580+ for FHA, explain impact on rates.
   - "Do I need a real estate agent to buy?" - Yes, explain buyer agent commission structure (paid by seller), and value of representation.
   - "What are closing costs?" - Typically 2-5% of purchase price, itemize common costs.
   - "What's the difference between pre-qualification and pre-approval?" - Explain both, why pre-approval is stronger.

   **For Sellers** (5-6 questions):
   - "How much is my home worth?" - Explain CMA process, link to /home-value for free valuation.
   - "How long will it take to sell my home?" - Average DOM in Philly (~28 days), factors that affect timing.
   - "What does Tauro charge in commission?" - Explain competitive commission, value proposition.
   - "Should I make repairs before listing?" - High-ROI improvements, Tauro's concierge guidance.
   - "What happens if I get multiple offers?" - Explain review process, negotiation strategy.
   - "Can I sell my home if I still have a mortgage?" - Yes, explain proceeds calculation.

   Accordion implementation: Use useState with an openIndex (or Set for multiple open). Each FAQ item is a button that toggles visibility of the answer div. Use a ChevronDown icon from lucide-react that rotates 180deg when open (transition-transform duration-200).

   Styling: Each FAQ item is a div with border-b border-border/30. Button is w-full flex items-center justify-between py-5 text-left. Question in font-medium text-white. Answer in pb-5 text-sm text-muted-foreground leading-relaxed with a max-w-3xl. Category headings in font-heading text-xl font-bold text-gold mb-6. Wrap each category in its own div. Overall max-w-3xl mx-auto.

3. **CTA section** - bg-near-black py-16. "Still Have Questions?" with buttons: "Contact Us" -> /contact (gold bg), "Get Free Valuation" -> /home-value (gold border outline).

Allow multiple FAQ items to be open simultaneously (use a Set<number> in state, not a single index).
  </action>
  <verify>Run `npx next build` and confirm no errors. Verify the /faq route builds.</verify>
  <done>FAQ page renders with hero, 12 expandable Q&A items in two categories (Buyers/Sellers), accordion toggle works, and CTA section links to /contact and /home-value.</done>
</task>

<task type="auto">
  <name>Task 2: Create "What's My Home Worth" lead capture page</name>
  <files>src/app/(site)/home-value/page.tsx</files>
  <action>
Create src/app/(site)/home-value/page.tsx as a "use client" component. This is a focused lead capture page with a prominent address input form.

Follow the exact form submission pattern from src/app/(site)/sell/page.tsx: useState for FormData and FormState, handleChange, handleSubmit posting to /api/leads, success/error states.

Page structure:

1. **Hero section** - bg-near-black pb-20 pt-32 with a background image overlay (same pattern as sell page). Eyebrow "Free Home Valuation" in gold. H1: "What's My Home Worth?" with a gold-accented second line: "Find Out in 24 Hours." Subtitle: "Get a no-obligation market analysis from a Tauro agent who knows your neighborhood."

2. **Value Proposition strip** - bg-midnight py-12. Three inline stats/features in a horizontal row (flex or grid-cols-3): "Free, No Obligation" (Gift icon), "24-Hour Response" (Clock icon), "Local Market Expert" (MapPin icon). Each with icon + bold label + short description. Styled as compact cards.

3. **Lead Capture Form** - bg-near-black py-20. Centered form (max-w-2xl mx-auto). This form is SIMPLER than the /sell page form -- it focuses on capturing the lead quickly.

   FormData interface:
   ```
   firstName: string
   lastName: string
   email: string
   phone: string
   homeAddress: string
   message: string
   ```

   Form fields:
   - Property Address (required, full width) - this is the PRIMARY field, make it visually prominent with slightly larger text or a Home icon prefix
   - First Name + Last Name (2-col grid)
   - Email (full width)
   - Phone (full width)
   - "Anything else we should know?" textarea (optional, 2 rows)
   - Submit button: "Get My Free Valuation" (gold bg, full width)

   Submit payload: type "seller" (reuses existing seller lead type in LeadPayload), with homeAddress, firstName, lastName, email, phone, message fields.

   Success state: Same pattern as sell page. Show CheckCircle icon, "Valuation Request Received!" heading, message about 24-hour response, "Submit Another" button and "Browse Listings" link to /properties.

   Error state: Same pattern as sell page (AlertCircle with error message).

   Input styling: Same as sell/contact pages - rounded-lg border border-border/40 bg-midnight px-4 py-2.5 text-sm text-white. Form wrapper: rounded-2xl border border-border/40 bg-midnight p-8 shadow-xl.

4. **How It Works section** - bg-midnight py-16. Three steps in a horizontal layout:
   - Step 1: "Submit Your Address" (with FormInput icon)
   - Step 2: "We Analyze the Market" (with BarChart3 icon)
   - Step 3: "Receive Your Report" (with FileText icon)
   Each step: number in gold, title in white, one-sentence description. Connected visually (optional: a subtle divider or arrow between them).

5. **Privacy note** at bottom of form: "By submitting, you agree to our Privacy Policy" linking to /privacy, matching existing pattern.
  </action>
  <verify>Run `npx next build` and confirm no errors. Test that the form type "seller" matches the existing LeadPayload type in src/app/api/leads/route.ts (it does - homeAddress is already a field). Verify the /home-value route builds.</verify>
  <done>The /home-value page renders with hero, value proposition strip, simplified lead capture form, how-it-works section. Form submits to /api/leads with type "seller" and shows success/error states correctly.</done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- /faq and /home-value routes exist in build output
- FAQ accordion toggles open/close on click
- Home valuation form posts to /api/leads with type "seller"
- Home valuation form shows success state after submission
- Both pages follow dark theme with gold accents
</verification>

<success_criteria>
- FAQ page has 12 expandable Q&A items covering buyer and seller topics
- FAQ accordion allows multiple items open simultaneously
- Home valuation page captures address + contact info and submits as seller lead
- Form reuses existing LeadPayload type with type "seller"
- Both pages have CTAs linking to relevant other pages
</success_criteria>

<output>
After completion, create `.planning/phases/07-resource-education-pages/07-03-SUMMARY.md`
</output>
