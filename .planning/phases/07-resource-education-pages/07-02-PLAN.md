---
phase: 07-resource-education-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(site)/market-insights/page.tsx
  - src/app/(site)/why-join/page.tsx
  - src/app/(site)/home-value/page.tsx
  - src/app/(site)/home-value/layout.tsx
autonomous: true

must_haves:
  truths:
    - "Market Insights page shows Philadelphia real estate stats and trends by neighborhood"
    - "Why Join Tauro page presents agent value proposition with commission info, tools, culture, and application CTA"
    - "What's My Home Worth page captures seller leads with address input form that submits to /api/leads"
    - "All three pages have proper metadata for SEO"
  artifacts:
    - path: "src/app/(site)/market-insights/page.tsx"
      provides: "Market stats, neighborhood price trends, market analysis content"
      contains: "export const metadata"
    - path: "src/app/(site)/why-join/page.tsx"
      provides: "Agent opportunity page with commission structure, tools, training, culture, application CTA"
      contains: "export const metadata"
    - path: "src/app/(site)/home-value/page.tsx"
      provides: "Home valuation lead capture with address form"
    - path: "src/app/(site)/home-value/layout.tsx"
      provides: "Metadata for home value page"
      contains: "export const metadata"
  key_links:
    - from: "src/app/(site)/home-value/page.tsx"
      to: "/api/leads"
      via: "fetch POST"
      pattern: "fetch.*api/leads"
    - from: "src/app/(site)/why-join/page.tsx"
      to: "/join"
      via: "application CTA link"
      pattern: "href.*join"
    - from: "src/app/(site)/market-insights/page.tsx"
      to: "/neighborhoods"
      via: "neighborhood links"
      pattern: "href.*neighborhoods"
---

<objective>
Build the Market Insights, Why Join Tauro, and What's My Home Worth pages.

Purpose: Market Insights drives organic traffic from people researching Philadelphia real estate data. Why Join Tauro recruits agents to the brokerage. What's My Home Worth captures high-intent seller leads via a home valuation request form.

Output: Three fully-built pages covering EDU-03, EDU-04, and EDU-06.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/app/(site)/layout.tsx
@src/app/(site)/market-insights/page.tsx
@src/app/(site)/why-join/page.tsx
@src/app/(site)/home-value/page.tsx
@src/app/(site)/home-value/layout.tsx
@src/app/api/leads/route.ts
@src/data/neighborhoods.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance Market Insights page</name>
  <files>src/app/(site)/market-insights/page.tsx</files>
  <action>
  The Market Insights page already exists with key stats and metadata. Review and enhance to fully satisfy EDU-03:

  **Required sections (verify all present, add if missing):**
  1. Hero section -- "Philadelphia Market Insights" heading with gold accent
  2. Key market stats grid -- median home price, avg days on market, inventory, YoY price change (already has this)
  3. Neighborhood price breakdown table/grid -- show median price, YoY trend, and avg DOM for at least 8 Philadelphia neighborhoods (Center City, Rittenhouse, Fishtown, Northern Liberties, Old City, South Philly, University City, Manayunk). Use static data. Style as a responsive card grid or table with dark bg, gold headers.
  4. Market trends commentary section -- 2-3 paragraphs analyzing the current Philadelphia market (buyer's vs seller's market, inventory trends, interest rate impact). This is static marketing content, not live data.
  5. "When to Buy" and "When to Sell" split sections with brief guidance
  6. CTA: "Want to know what your home is worth?" linking to /home-value, and "Browse neighborhoods" linking to /neighborhoods

  Import neighborhood data from `@/data/neighborhoods.ts` if it has price data to populate the neighborhood breakdown. If not, use inline static data.

  Styling: server component (no "use client"), dark bg, gold accents, Playfair headings, lucide-react icons for stat cards. Keep existing metadata export.
  </action>
  <verify>
  `npx next build 2>&1 | tail -20` compiles clean. `grep "export const metadata" src/app/\(site\)/market-insights/page.tsx` confirms metadata.
  </verify>
  <done>
  Market Insights page has key stats, neighborhood price breakdown, market trends commentary, buy/sell timing guidance, and CTAs to /home-value and /neighborhoods.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhance Why Join Tauro and What's My Home Worth pages</name>
  <files>src/app/(site)/why-join/page.tsx, src/app/(site)/home-value/page.tsx, src/app/(site)/home-value/layout.tsx</files>
  <action>
  Both pages already exist with substantial content. Review and enhance to fully satisfy EDU-04 and EDU-06:

  **Why Join Tauro (EDU-04)** -- currently at src/app/(site)/why-join/page.tsx with metadata. Verify it includes:
  - Hero section with "Why Join Tauro" heading
  - Commission structure overview (competitive split info, no exact numbers needed -- use "industry-leading" language)
  - Tools and technology provided (CRM, website, marketing, transaction management)
  - Training and mentorship program description
  - Culture and growth opportunity section
  - Stats section (sales volume, agents, etc. -- already has this)
  - Application CTA linking to /join (the join/application form page)
  - If any of the above sections are missing, add them. Keep existing metadata.

  **What's My Home Worth (EDU-06)** -- currently at src/app/(site)/home-value/page.tsx as a "use client" component with a working lead capture form that POSTs to /api/leads. Verify it includes:
  - Hero section with "What's My Home Worth?" heading
  - Address input form with fields: address, name, email, phone (already has this)
  - Form submits to /api/leads with type "seller" (already does this)
  - Success state after submission (already has this)
  - Value proposition section (free, no obligation, 24-hour response -- already has this)
  - How it works steps (already has this)
  - Metadata: check src/app/(site)/home-value/layout.tsx -- if it has metadata, verify title is "What's My Home Worth | Tauro Realty" and description mentions free home valuation. If layout.tsx exists but has no metadata, add it.

  For both pages: if existing content already satisfies requirements, make only minor polish (consistency of styling, ensure gold accent CTAs, verify all links work). Do not rewrite working pages.
  </action>
  <verify>
  `npx next build 2>&1 | tail -20` compiles clean. Verify Why Join has application CTA: `grep -i "join" src/app/\(site\)/why-join/page.tsx | head -5`. Verify Home Value form submits to API: `grep "api/leads" src/app/\(site\)/home-value/page.tsx`.
  </verify>
  <done>
  Why Join Tauro has commission overview, tools/tech, training, culture, stats, and /join application CTA. What's My Home Worth has address form, /api/leads submission, success state, value props, and proper metadata.
  </done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with no errors
- Market Insights renders at /market-insights with stats and neighborhood data
- Why Join Tauro renders at /why-join with agent value proposition and application CTA
- What's My Home Worth renders at /home-value with working lead capture form
- All pages have unique metadata (title + description)
</verification>

<success_criteria>
- EDU-03: Market Insights has Philadelphia stats, neighborhood price trends, and market analysis
- EDU-04: Why Join Tauro has commission overview, tools, training, culture, and application CTA
- EDU-06: What's My Home Worth captures seller leads via address input form to /api/leads
- All pages use Tauro brand styling (dark mode, gold accents, Playfair headings)
</success_criteria>

<output>
After completion, create `.planning/phases/07-resource-education-pages/07-02-SUMMARY.md`
</output>
