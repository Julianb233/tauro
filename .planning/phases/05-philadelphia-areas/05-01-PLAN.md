---
phase: 05-philadelphia-areas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/neighborhoods.ts
  - src/app/(site)/neighborhoods/[slug]/page.tsx
autonomous: true

must_haves:
  truths:
    - "All 15 required neighborhoods are defined with unique, authentic Philadelphia content"
    - "Each neighborhood has description, selling points, lifestyle info, market stats, images, and map coordinates"
    - "Dynamic route /neighborhoods/[slug] renders each neighborhood with SSG via generateStaticParams"
    - "Properties are filtered by neighborhood.propertyFilter matching property data"
    - "Title tags follow '[Neighborhood] Homes for Sale' pattern (root layout appends '| Tauro')"
  artifacts:
    - path: "src/data/neighborhoods.ts"
      provides: "Neighborhood interface and data for all 15 neighborhoods"
      exports: ["Neighborhood", "neighborhoods", "getNeighborhoodBySlug", "getFeaturedNeighborhoods"]
    - path: "src/app/(site)/neighborhoods/[slug]/page.tsx"
      provides: "Individual neighborhood detail page with SSG and SEO metadata"
      exports: ["generateStaticParams", "generateMetadata", "default"]
  key_links:
    - from: "src/data/neighborhoods.ts"
      to: "src/data/properties.ts"
      via: "neighborhood.propertyFilter matches property city for filtering"
      pattern: "propertyFilter"
    - from: "src/app/(site)/neighborhoods/[slug]/page.tsx"
      to: "src/data/neighborhoods.ts"
      via: "getNeighborhoodBySlug import for page data"
      pattern: "getNeighborhoodBySlug"
    - from: "src/app/(site)/neighborhoods/[slug]/page.tsx"
      to: "src/data/properties.ts"
      via: "filter properties array by neighborhood.propertyFilter"
      pattern: "properties\\.filter"
    - from: "src/app/(site)/neighborhoods/[slug]/page.tsx"
      to: "src/components/PropertyCard.tsx"
      via: "renders filtered properties using PropertyCard component"
      pattern: "PropertyCard"
---

<objective>
Create the complete neighborhood data layer for all 15 Philadelphia neighborhoods and the dynamic [slug] detail page with static generation.

Purpose: This is the data foundation AND primary page for the neighborhood SEO strategy. Each of the 15 neighborhoods gets a rich, unique page with authentic Philadelphia content that positions Tauro as the local expert. These pages are core to organic search traffic for "[Neighborhood] homes for sale" queries.

Output: Enhanced `src/data/neighborhoods.ts` with all 15 neighborhoods, and `src/app/(site)/neighborhoods/[slug]/page.tsx` with SSG, SEO metadata, and all content sections.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/data/properties.ts (Property interface with city field for filtering)
@src/data/neighborhoods.ts (existing Neighborhood interface and data - already has all 15)
@src/app/(site)/neighborhoods/[slug]/page.tsx (existing implementation)
@src/app/properties/[slug]/page.tsx (pattern for generateStaticParams + generateMetadata with Promise params)
@src/components/PropertyCard.tsx (reuse for listing cards on neighborhood pages)
@src/app/(site)/page.tsx (homepage design patterns, dark theme, gold accents)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance neighborhoods data with complete content for all 15 neighborhoods</name>
  <files>src/data/neighborhoods.ts</files>
  <action>
Update `src/data/neighborhoods.ts` to ensure all 15 Philadelphia neighborhoods have complete, unique, SEO-rich content. The file already contains the Neighborhood interface and all 15 entries. Review and enhance if needed:

The `Neighborhood` interface must include:
- `id`, `name`, `slug`, `tagline` (one-line hook)
- `description` (2-3 paragraphs, 150-250 words, with real Philly landmarks, streets, and cultural references)
- `sellingPoints` (4-6 bullet items per neighborhood)
- `lifestyle` object with `vibe`, `dining`, `transit`, `parks` (each 1-2 sentences)
- `stats` object with `medianPrice`, `avgPricePerSqft`, `avgDaysOnMarket`, `inventoryLevel`
- `image` (hero image, Unsplash ?w=1200&q=80), `cardImage` (thumbnail, ?w=800&q=80)
- `mapCenter` with accurate `lat`/`lng` for the neighborhood center
- `propertyFilter` matching the neighborhood name for property data filtering
- `featured` boolean (true for: Center City, Rittenhouse, Fishtown, Northern Liberties, Old City, Chestnut Hill)

All 15 neighborhoods with required slugs:
1. Center City (`center-city`) 2. Rittenhouse (`rittenhouse`) 3. Fishtown (`fishtown`)
4. Northern Liberties (`northern-liberties`) 5. Old City (`old-city`) 6. South Philly (`south-philly`)
7. University City (`university-city`) 8. Manayunk (`manayunk`) 9. Chestnut Hill (`chestnut-hill`)
10. Mt. Airy (`mt-airy`) 11. Germantown (`germantown`) 12. West Philly (`west-philly`)
13. Kensington (`kensington`) 14. Brewerytown (`brewerytown`) 15. Point Breeze (`point-breeze`)

Export helper functions: `getNeighborhoodBySlug(slug)` and `getFeaturedNeighborhoods()`.

Every description must reference REAL Philadelphia landmarks, real restaurant names, real transit lines (SEPTA Broad Street Line, Market-Frankford Line, Regional Rail), and real parks. No generic placeholder text.
  </action>
  <verify>
Run `npx tsc --noEmit` to verify TypeScript compiles. Count neighborhoods: `grep -c '"id":' src/data/neighborhoods.ts` or `grep -c 'id: "' src/data/neighborhoods.ts` should return 15.
  </verify>
  <done>
- neighborhoods.ts exports Neighborhood interface, 15-entry neighborhoods array, getNeighborhoodBySlug, getFeaturedNeighborhoods
- All 15 neighborhoods present with unique, authentic Philly content
- Each has: description (150-250 words), 4-6 selling points, lifestyle (4 categories), stats, images, mapCenter
- TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Create neighborhood detail page with SSG, SEO metadata, and all content sections</name>
  <files>src/app/(site)/neighborhoods/[slug]/page.tsx</files>
  <action>
Create or update `src/app/(site)/neighborhoods/[slug]/page.tsx` as a SERVER component (no "use client").

Follow Next.js 15 App Router conventions where `params` is `Promise<{ slug: string }>` (must be awaited).

**generateStaticParams:** Return array of `{ slug }` for all 15 neighborhoods.
**generateMetadata:** Title: `${neighborhood.name} Homes for Sale` (root layout appends "| Tauro"). Description includes tagline and search-oriented copy.

**Page sections (top to bottom):**

1. **Hero** (min-h-[50vh]): Full-width neighborhood.image via next/image with `fill` + `object-cover`. Dark gradient overlay (`from-near-black via-near-black/60 to-near-black/30`). Breadcrumb: Home > Neighborhoods > {Name} using ChevronRight separators. H1 with `font-heading text-4xl sm:text-5xl md:text-6xl`. Tagline below in `text-white/70`.

2. **About section**: Label "About the Neighborhood" in gold (`font-label text-sm uppercase tracking-[0.2em] text-gold`). H2 "Living in {name}" with `font-heading`. Split description on `\n\n` to render paragraphs in `text-muted-foreground leading-relaxed`.

3. **Selling Points**: Label "Why {name}" in gold. H2 "Selling Points". 2-column grid (`sm:grid-cols-2`). Each point has CheckCircle icon in `text-gold` with the text beside it.

4. **Lifestyle**: Label "Local Life" in gold. H2 "Lifestyle & Culture". 2x2 grid of cards (`rounded-xl border border-border/40 bg-card p-6`). Icons: Sparkles (vibe), Coffee (dining), TrainFront (transit), Trees (parks). Icon in `bg-gold/10` circle, `text-gold`. All from lucide-react.

5. **Market Stats**: Label "Market Data" in gold. H2 "{name} by the Numbers". 4-column grid of stat cards (`sm:grid-cols-2 lg:grid-cols-4`). Icons: DollarSign (medianPrice), TrendingUp (avgPricePerSqft), Clock (avgDaysOnMarket), BarChart3 (inventoryLevel). Values in `text-gold font-heading text-2xl font-bold`.

6. **Properties**: Filter `properties` where `property.city.toLowerCase() === neighborhood.propertyFilter.toLowerCase()`. Render matches with `PropertyCard` in `sm:grid-cols-2 lg:grid-cols-3` grid. Empty state: MapPin icon, "New listings in {name} coming soon", link to /contact with gold button "Get Notified".

7. **Map Placeholder**: Aspect-video container with MapPin icon, "{name}" text, "Interactive map coming soon".

8. **CTA section**: "Interested in {name}?" with gold "Contact an Agent" button (link to /contact) and gold-outline "Browse All Properties" button (link to /properties). Both with ArrowRight icon.

Alternate section backgrounds between `bg-near-black` and `bg-midnight` with `border-t border-border/40` between some sections to create visual rhythm.

Use `notFound()` from next/navigation if slug doesn't match any neighborhood.
  </action>
  <verify>
1. `npx tsc --noEmit` passes with zero errors
2. `npm run build` succeeds and reports 15 static neighborhood pages generated
3. Verify generateStaticParams produces 15 entries
4. Check title tag pattern: grep for "Homes for Sale" in generateMetadata
  </verify>
  <done>
- All 15 neighborhood pages statically generated via generateStaticParams
- Each page has unique SEO title: "{Neighborhood} Homes for Sale" (layout adds "| Tauro")
- Each page shows: hero with breadcrumb, about, selling points, lifestyle grid, market stats, properties (or empty state), map placeholder, CTA
- Properties filtered by neighborhood.propertyFilter
- Dark theme with gold accents, alternating bg-near-black/bg-midnight sections
- TypeScript compiles, build succeeds
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm run build` succeeds and outputs 15 static neighborhood pages plus the index
3. Visit /neighborhoods/center-city -- shows full detail page with all 8 sections
4. Visit /neighborhoods/point-breeze -- shows full detail page (last neighborhood works)
5. Check page source for title tag containing "Homes for Sale"
6. All homepage neighborhood card links resolve to working neighborhood pages
</verification>

<success_criteria>
- src/data/neighborhoods.ts has typed, complete data for all 15 Philadelphia neighborhoods with unique, authentic content
- /neighborhoods/{slug} pages render with hero, about, selling points, lifestyle, stats, properties, map placeholder, CTA
- SEO title tags follow "{Neighborhood} Homes for Sale" pattern
- Properties section shows filtered listings or graceful empty state
- All 15 pages statically generated (SSG) via generateStaticParams
- Build passes with no TypeScript or build errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-philadelphia-areas/05-01-SUMMARY.md`
</output>
