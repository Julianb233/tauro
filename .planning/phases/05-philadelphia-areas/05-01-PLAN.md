---
phase: 05-philadelphia-areas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/neighborhoods.ts
autonomous: true
status: COMPLETED

must_haves:
  truths:
    - "All 15 required neighborhoods are defined with unique, authentic Philadelphia content"
    - "Each neighborhood has description, selling points, lifestyle info, market stats, images, and map coordinates"
    - "Helper functions getNeighborhoodBySlug and getFeaturedNeighborhoods are exported"
  artifacts:
    - path: "src/data/neighborhoods.ts"
      provides: "Neighborhood interface and data for all 15 neighborhoods"
      exports: ["Neighborhood", "neighborhoods", "getNeighborhoodBySlug", "getFeaturedNeighborhoods"]
  key_links:
    - from: "src/data/neighborhoods.ts"
      to: "src/data/properties.ts"
      via: "neighborhood.propertyFilter matches property.neighborhood for filtering"
      pattern: "propertyFilter"
---

<objective>
Create the complete neighborhood data model and seed data for all 15 Philadelphia neighborhoods.

Purpose: This is the data foundation for the entire area pages SEO strategy. Each neighborhood needs rich, authentic Philadelphia content that positions Tauro as the local expert. These pages target "[Neighborhood] homes for sale" organic search queries.

Output: Fully populated `src/data/neighborhoods.ts` with 15 neighborhoods and helper functions.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/data/properties.ts (Property interface with neighborhood field for filtering)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create neighborhood data model and seed all 15 neighborhoods</name>
  <files>src/data/neighborhoods.ts</files>
  <action>
Create `src/data/neighborhoods.ts` with the `Neighborhood` interface and data for all 15 Philadelphia neighborhoods.

**Neighborhood interface fields:**
- `id` (string), `name` (string), `slug` (string), `tagline` (one-line hook)
- `description` (2-3 paragraphs, 150-250 words, referencing REAL Philly landmarks, restaurants, streets, cultural references)
- `sellingPoints` (4-6 bullet items per neighborhood)
- `lifestyle` object: `vibe`, `dining`, `transit`, `parks` (each 1-2 sentences with real place names)
- `stats` object: `medianPrice` (string, formatted), `avgPricePerSqft` (string), `avgDaysOnMarket` (number), `inventoryLevel` (string: Low/Moderate/High)
- `image` (Unsplash URL with ?w=1200&q=80), `cardImage` (Unsplash URL with ?w=800&q=80)
- `mapCenter` with accurate `lat`/`lng` for the neighborhood center
- `propertyFilter` matching the neighborhood name used in property data
- `featured` boolean (true for: Center City, Rittenhouse, Fishtown, Northern Liberties, Old City, Chestnut Hill)

**All 15 neighborhoods with slugs:**
1. Center City (`center-city`) 2. Rittenhouse (`rittenhouse`) 3. Fishtown (`fishtown`)
4. Northern Liberties (`northern-liberties`) 5. Old City (`old-city`) 6. South Philly (`south-philly`)
7. University City (`university-city`) 8. Manayunk (`manayunk`) 9. Chestnut Hill (`chestnut-hill`)
10. Mt. Airy (`mt-airy`) 11. Germantown (`germantown`) 12. West Philly (`west-philly`)
13. Kensington (`kensington`) 14. Brewerytown (`brewerytown`) 15. Point Breeze (`point-breeze`)

**Export helper functions:**
- `getNeighborhoodBySlug(slug: string): Neighborhood | undefined`
- `getFeaturedNeighborhoods(): Neighborhood[]`

Every description must reference REAL Philadelphia landmarks, restaurant names, transit lines (SEPTA Broad Street Line, Market-Frankford Line, Regional Rail), and parks. No generic placeholder text.
  </action>
  <verify>
Run `npx tsc --noEmit` to verify TypeScript compiles. Count neighborhoods: `grep -c 'id: "' src/data/neighborhoods.ts` should return 15. Verify exports: `grep "export" src/data/neighborhoods.ts`.
  </verify>
  <done>
- neighborhoods.ts exports Neighborhood interface, 15-entry neighborhoods array, getNeighborhoodBySlug, getFeaturedNeighborhoods
- All 15 neighborhoods present with unique, authentic Philly content
- Each has: description (150-250 words), 4-6 selling points, lifestyle (4 categories), stats, images, mapCenter
- TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. 15 neighborhoods in the data file
3. Every neighborhood has all required fields populated
4. Helper functions return correct results
</verification>

<success_criteria>
- AREA-01: All 15 Philadelphia neighborhoods defined with unique content
- Data model supports all fields needed for area detail pages and index page
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/05-philadelphia-areas/05-01-SUMMARY.md`
</output>
