---
phase: 08-proposal-payment
plan: 01
type: execute
wave: 1
depends_on: []
status: COMPLETED
files_modified:
  - src/app/proposal/page.tsx
  - src/app/proposal/layout.tsx
  - src/components/proposal-scope.tsx
  - src/components/onboarding-steps.tsx
autonomous: true

must_haves:
  truths:
    - "Proposal page displays full build scope with deliverables organized by pillar (Website, CRM, Marketing, Automations)"
    - "Proposal page shows 4-week timeline with milestone breakdown"
    - "Proposal page has working payment link to FanBasis checkout that opens in new tab"
    - "Proposal page displays investment pricing ($5,500 deposit + $977/mo) with comparison to industry standard"
    - "Post-payment onboarding steps are clearly presented (5 steps from deposit to launch)"
    - "Proposal page has its own navigation (not site nav) with section anchors"
  artifacts:
    - path: "src/app/proposal/page.tsx"
      provides: "Complete proposal page with scope, timeline, pricing, payment CTA, onboarding"
      contains: "PAYMENT_URL"
    - path: "src/app/proposal/layout.tsx"
      provides: "Proposal layout with metadata, separate from site layout (no Navbar/Footer)"
      contains: "metadata"
    - path: "src/components/proposal-scope.tsx"
      provides: "Reusable scope card component with expandable deliverables list"
      exports: ["ProposalScope"]
    - path: "src/components/onboarding-steps.tsx"
      provides: "Onboarding steps component showing post-payment flow"
      exports: ["OnboardingSteps"]
  key_links:
    - from: "src/app/proposal/page.tsx"
      to: "https://www.fanbasis.com/agency-checkout/Aiacrobatics/TAURO"
      via: "payment link (target=_blank)"
      pattern: "fanbasis.com"
    - from: "src/app/proposal/page.tsx"
      to: "src/components/proposal-scope.tsx"
      via: "component import"
      pattern: "import.*ProposalScope"
    - from: "src/app/proposal/page.tsx"
      to: "src/components/onboarding-steps.tsx"
      via: "component import"
      pattern: "import.*OnboardingSteps"
---

<objective>
Refactor and polish the client-facing proposal page with extracted components for scope and onboarding.

Purpose: This is the sales page for LYL Realty Group to view the build scope, understand deliverables, pay the deposit via FanBasis, and see post-payment onboarding steps. It must look premium and professional -- this is what closes the deal.

Output: Polished proposal page with extracted ProposalScope and OnboardingSteps components.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/app/proposal/page.tsx
@src/app/proposal/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract ProposalScope and OnboardingSteps components</name>
  <files>src/components/proposal-scope.tsx, src/components/onboarding-steps.tsx</files>
  <action>
  The proposal page at src/app/proposal/page.tsx already has a fully-built proposal with scope cards, timeline, pricing, and onboarding steps -- all inline. Extract two reusable components:

  **src/components/proposal-scope.tsx:**
  - Extract the ScopeCard component and scopePillars data from proposal/page.tsx
  - Create a `ProposalScope` component that renders the full scope section (heading + grid of expandable scope cards)
  - Props: none needed (data is static, can be inline or co-located)
  - This is a "use client" component (needs useState for accordion expand/collapse)
  - Keep the existing visual design: expandable cards with checkmark lists, gold accents, border hover effects, chevron rotation animation

  **src/components/onboarding-steps.tsx:**
  - Extract the onboardingSteps data and rendering from proposal/page.tsx
  - Create an `OnboardingSteps` component that renders the numbered steps section
  - Props: none needed (static data)
  - Server component (no interactivity needed)
  - Keep the existing visual design: numbered gold circles, step title, description, hover border effect

  Both components should match the exact visual output of the current inline implementation. Export each as a named export.
  </action>
  <verify>
  `ls src/components/proposal-scope.tsx src/components/onboarding-steps.tsx` confirms files exist. `grep "export" src/components/proposal-scope.tsx src/components/onboarding-steps.tsx` confirms exports.
  </verify>
  <done>
  ProposalScope component renders expandable scope cards. OnboardingSteps component renders the 5-step onboarding flow. Both match the existing visual design.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor proposal page to use extracted components and verify completeness</name>
  <files>src/app/proposal/page.tsx, src/app/proposal/layout.tsx</files>
  <action>
  Update the proposal page to import and use the extracted components, and verify all PROPOSAL requirements are met:

  **src/app/proposal/page.tsx:**
  - Import `ProposalScope` from `@/components/proposal-scope`
  - Import `OnboardingSteps` from `@/components/onboarding-steps`
  - Replace the inline scope section with `<ProposalScope />`
  - Replace the inline onboarding section with `<OnboardingSteps />`
  - Keep all other sections inline (hero, stats bar, timeline, investment/pricing, after launch, final CTA, footer)
  - Keep the PAYMENT_URL constant pointing to `https://www.fanbasis.com/agency-checkout/Aiacrobatics/TAURO`
  - Keep the floating nav with section anchor links (#scope, #timeline, #investment, #onboarding)
  - Ensure the "use client" directive remains (needed for ProposalScope's expand/collapse)

  **Verify PROPOSAL-01 (scope/deliverables/timeline):**
  - Scope section shows 4 pillars: Custom Website, GoHighLevel CRM, Marketing Engine, Automations
  - Each pillar has expandable deliverables list
  - Timeline section shows 4-week build phases

  **Verify PROPOSAL-02 (payment link):**
  - "Get Started" button in nav links to PAYMENT_URL (target="_blank")
  - "Accept Proposal" hero CTA links to PAYMENT_URL
  - "Pay $5,500 Deposit" button in investment section links to PAYMENT_URL
  - "Pay $5,500 Deposit and Get Started" final CTA links to PAYMENT_URL
  - All payment links open in new tab with rel="noopener noreferrer"

  **Verify PROPOSAL-03 (onboarding steps):**
  - 5 onboarding steps: Secure Spot, Kickoff Call, Asset Collection, Build Begins, Review and Launch
  - Steps are clearly numbered and visually distinct

  **src/app/proposal/layout.tsx:**
  - Verify it has metadata: title "Proposal | Tauro" and description about the build proposal
  - Verify it does NOT include Navbar/Footer (proposal has its own standalone nav and footer)
  - If metadata is missing, add it

  The page should remain visually identical after refactoring -- the only change is component extraction for cleanliness.
  </action>
  <verify>
  `npx next build 2>&1 | tail -20` compiles clean. Verify component imports: `grep "ProposalScope\|OnboardingSteps" src/app/proposal/page.tsx`. Verify payment URL: `grep "fanbasis" src/app/proposal/page.tsx`. Verify metadata: `grep "metadata" src/app/proposal/layout.tsx`.
  </verify>
  <done>
  Proposal page uses extracted ProposalScope and OnboardingSteps components. All payment links point to FanBasis checkout. Scope shows 4 pillars with deliverables. Timeline shows 4 weeks. Onboarding shows 5 steps. Layout has metadata. Page is visually identical to before.
  </done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with no errors
- Proposal page renders at /proposal with standalone nav (not site nav)
- Scope section displays 4 expandable pillars
- Timeline shows 4-week breakdown
- Investment section shows $5,500 deposit + $977/mo pricing
- All "Get Started" / "Accept Proposal" / "Pay Deposit" buttons link to FanBasis URL
- Onboarding shows 5 clear steps
- Page has proper metadata via layout.tsx
</verification>

<success_criteria>
- PROPOSAL-01: Build scope, deliverables, and 4-week timeline are displayed
- PROPOSAL-02: FanBasis payment link is functional and accessible from multiple CTAs
- PROPOSAL-03: Post-payment onboarding steps are clearly presented
- Components extracted for cleanliness (ProposalScope, OnboardingSteps)
</success_criteria>

<output>
After completion, create `.planning/phases/08-proposal-payment/08-01-SUMMARY.md`
</output>
